# dist

只存放经过检查的本地交付候选包或经授权的正式发布物。每个版本应包含：

- 插件压缩包；
- 安装、`NETLOAD` 和命令使用说明；
- 支持范围、已知限制和测试摘要；
- SHA-256 校验清单。

V0.1.0 当前交付物：

- `TileLayout-0.1.0/`：解压目录；
- `TileLayout-0.1.0.zip`：最小插件包；
- `TileLayout-0.1.0-sha256.txt`：压缩包及包内文件校验值。

本地生成交付物不等于已经对外发布。未经用户明确授权，不创建远程 Release、不发布或部署。
