# inputs

保存用户提供的原始、脱敏或受限输入资料，例如：

- 四线矩形房间的脱敏 DWG；
- 前期项目策划书；
- AutoCAD“关于”窗口截图；
- 用户提供的业务规则说明。

规则：

- 原件只读保留，不直接修改。
- 本目录默认被 Git 忽略，仅保留本说明文件。
- 需要自动测试的数据必须再次脱敏并复制到 `tests/fixtures`。
- 复制重要输入后记录文件大小和 SHA-256。
