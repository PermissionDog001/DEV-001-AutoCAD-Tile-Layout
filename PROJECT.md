# PROJECT — DEV-001 AutoCAD瓷砖自动排版插件

## 1. 基本信息

- 项目编号：DEV-001
- 项目名称：AutoCAD瓷砖自动排版插件
- 创建日期：2026-07-19
- 项目目录：`E:\Workspace\Development\DEV-001-AutoCAD瓷砖自动排版插件`
- 当前状态：M1～M9、SC1、OR1/OR2、DR1～DR5-S 和 DOR2～DOR7-G2 的冻结范围均已有实现与证据；DR5 动态块路线因真实正样本为 0 保持暂停。DOR7-G3 已实现 270° 反射角锚定、实际分格线命中、同组 Pareto 取舍和同源只读诊断；DOR8 正式写回已完成编码、Core/适配自动验证和静态边界审计，待一次 AutoCAD 2021 DWG 副本实机收口。项目绝对下限新增“按图面确认/不设置数值绝对下限”互斥模式：只允许明确选择后进入专项提醒、同源预览和最终确认，不改 G3 算法。版本、历史 `dist` 和既有未提交改动保持不变。
- 路线图与测试预算：`docs/development-roadmap.md`
- DOR9-A 规则草案：`docs/dor9-grout-polyline-plaster.md`
- 已实现阶段总账：`docs/implementation-ledger.md`
- 主要使用者：瓷砖排版与土建创优工作使用者
- 关联业务来源：`Project-002-F地块创优策划`

## 下一独立任务：灰缝、闭合多段线与抹灰完成面支持（待规则冻结）

下一阶段暂定按以下顺序推进：

1. 先完成 DOR8 AutoCAD 2021 DWG 副本实机收口，并记录一次 `UNDO`、取消保护、关闭不保存和四个既有命令取消冒烟结果。
2. 盘点当前 working tree，区分源码/测试/文档、构建产物和临时文件；经用户确认后建立可追溯 Git 基线，不清理或覆盖未确认内容。
3. 冻结灰缝宽度、闭合多段线支持范围、抹灰厚度及完成面边界写入规则，再实现宿主无关 Core。
4. 接入 `TILEORTHOUI` 的设置、预览和正式写回，并保持 `TILE600`、`TILELAYOUT`、`TILEORTHO`、`TILEDOORRECT` 既有流程兼容。
5. 完成 Core/Adapter 自动测试、AutoCAD 2021 副本实机验收、正式版本打包和发布检查；GitHub 同步放在全部验收通过后执行。

灰缝默认值暂记为 `1.5 mm`，抹灰厚度暂记为 `0 mm` 以保持兼容；闭合多段线首期建议限定为 WCS 轴对齐、无圆弧 bulge、单外环。上述三项属于会改变产品行为的规则，编码前仍需单独确认。

## 2. 背景

当前瓷砖排版依赖人工在 CAD 图纸中布置分格。V0.1 不追求复杂优化，只验证 AutoCAD 插件能否可靠读取一个最简单的四线矩形房间，并按固定规则自动生成 600 mm 地砖分格。

## 3. 项目目标

开发一个可在 Autodesk AutoCAD 2021 中加载的 Managed .NET 插件，完成以下闭环：

1. 用户选择四条 `LINE`。
2. 插件验证其构成轴对齐闭合矩形。
3. 识别西南角和房间宽高。
4. 按固定 600 mm 模数生成分格。
5. 在新图层中写回结果。
6. 整次操作可一次撤销。

## 4. 非目标

- 不开发独立 CAD 查看器或独立 EXE。
- 不支持非 AutoCAD 宿主。
- 不处理旋转房间、自定义 UCS、异形边界或多个房间。
- 不计算灰缝、排版优化、材料损耗或施工下料。
- 不在 V0.1 建设复杂 UI、自动更新或商业授权系统。

## 5. 运行环境

- 操作系统：Windows 10 x64。
- 宿主：Autodesk AutoCAD 2021 简体中文，版本 `R24.0.47.0.0`。
- 语言：C#。
- 目标框架：.NET Framework 4.8。
- 预计开发工具：Visual Studio，具体版本与组件待环境检查。
- AutoCAD SDK/API：AutoCAD 2021 Managed .NET API。

## 6. V0.1 固定需求

### 6.1 输入

- 模型空间中的四条 `LINE`。
- 四线首尾闭合，构成矩形。
- 矩形边与 WCS X/Y 轴平行。
- 图纸单位为毫米。
- 四条线代表房间完成面内边界。

### 6.2 排版规则

- 瓷砖：600 × 600 mm。
- 灰缝：0 mm，完全忽略。
- 起铺点：房间西南角。
- 起铺定义：第一块砖西南角与房间西南角重合。
- 顺序：从西向东、从南向北。
- 东侧和北侧余量直接保留为切砖尺寸。
- 不对窄砖警告，不移动、不居中、不优化。

### 6.3 计算定义

对于房间宽度 `W`、高度 `H`：

- 竖向内部分格线：`westX + 600 × n`，且结果严格小于 `eastX`。
- 横向内部分格线：`southY + 600 × n`，且结果严格小于 `northY`。
- 东侧余量：`W mod 600`。
- 北侧余量：`H mod 600`。
- 尺寸整除时，对应方向不产生边缘余量。

### 6.4 输出

- 新图层：`TILE_LAYOUT_600`。
- 图层中生成矩形内部的瓷砖分格线。
- 命令行报告房间宽高、完整 600 mm 模数数量、东侧余量和北侧余量。
- 正式命令：`TILE600`。

### 6.5 原图保护

- 不修改、移动、删除或改变原四条墙线属性。
- 插件不自动保存图纸。
- 全部新增对象属于一次可撤销操作。

## 7. 验收标准

### 7.1 技术探针验收

- AutoCAD 2021 可执行 `NETLOAD` 并加载插件。
- 插件命令可选择四条 `LINE`。
- 正确读取端点并计算矩形宽高和西南角。
- 能在测试图层创建一条线。
- 一次 `UNDO` 可撤销本次新增内容。

### 7.2 V0.1 功能验收

- 600 mm 整除房间不产生额外边缘分格。
- 非整除房间的东、北余量计算正确。
- 小于 600 mm 的房间仍能正确输出结果而不崩溃。
- 非闭合、非矩形、非轴对齐或选择数量错误时拒绝生成，并给出明确提示。
- 重复执行不修改原墙线。
- 核心算法测试不依赖启动 AutoCAD。

## 8. 里程碑

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M0 | 项目目录、需求基线、Git 初始化 | 已完成 |
| M1 | AutoCAD 2021 `NETLOAD` 最小技术探针 | 已完成：2026-07-19 实机加载、选线、尺寸计算、写回和一次撤销均通过 |
| M2 | 独立核心矩形与 600 mm 网格算法及测试 | 已完成：2026-07-19，核心不引用 Autodesk 程序集，24 项自动测试通过 |
| M3 | AutoCAD 选择、验证、图层、写回与撤销集成 | 已完成：2026-07-20，自动验证及全部宿主实机验收通过 |
| M4 | 脱敏 DWG 实机验收与错误处理 | 已完成：2026-07-20，脱敏副本链、16 项实机矩阵和最终自动回归全部通过，无需产品代码修复 |
| M5 | V0.1.0 打包、说明、哈希与发布检查 | 已完成：Release 27/27、最小包、SHA-256 和一次 AutoCAD 冒烟全部通过 |
| M6 | V0.2 参数化核心契约与安全计数 | 已完成：2026-07-20，Debug/Release 各 48/48，完整解决方案双配置重建通过 |
| M7 | `TILELAYOUT` AutoCAD 适配 | 已完成：2026-07-20，参数提示、共享选择/事务路径、`TILE_LAYOUT` 和三类宿主无关消息已接入，Debug/Release 各 51/51 |
| M8 | V0.2 自动质量门 | 已完成：2026-07-20，锁定恢复、Debug/Release 各 51/51、完整解决方案双配置备用目录重建及产物保护检查均通过 |
| M9 | V0.2 脱敏 DWG 实机验收 | 已完成：2026-07-21，AutoCAD 2021 数值、方向、取消/错误、图层、超限、撤销、原图保护和不保存均通过 |
| M10 | V0.2.0 发布检查 | 未开始 |
| SC1 | 工程起铺角控制 | 已完成：施工语义、核心、AutoCAD 适配、Debug 56/56、隔离插件编译和 AutoCAD 2021 最小实机均通过 |
| OR1 | 正交简单房间边界与裁切 | 已完成：需求/接口、`TILEORTHO`、聚焦代码审查、Debug/Release 81/81、完整解决方案隔离重建和 AutoCAD 2021 最小实机均通过 |
| DR1 | 门洞控制的正交矩形工程排版 | 已完成规则阶段：两个样例、0.42 硬下限、大小砖分配、核心算法和 AutoCAD 交互计划已冻结；宿主无关实现见 DR2 |
| DR2 | 宿主无关矩形门洞核心与候选模型基座 | 已完成：DR1 算法、默认/居中翻转候选、实际砖块/分类、诊断和未加权指标输入；Debug/Release 97/97，完整解决方案双配置隔离重建通过 |
| DR3 | AutoCAD 两点门洞、摘要、预览/翻转与接受后写回 | 自动实现已完成：`TILEDOORRECT` / `TILE_LAYOUT_DOOR_RECT`、宿主无关点适配/消息/状态测试、Debug/Release 116/116 和完整解决方案双配置重建通过 |
| DR4 | AutoCAD 2021 脱敏副本实机验收 | 已完成：12/12 通过；候选/翻转、零写入拒绝、4 条写回、一次撤销、10001 条图层前拒绝、既有命令兼容和关闭不保存均通过 |
| DR5 | 可选门块/门图元辅助识别 | 首轮动态块辅助识别编码和 Debug/Release 140/140 已完成；真实动态样本为 0，路线和宿主清单暂停，等待未来新样本 |
| DR5-S | 顶层静态门块辅助识别 | 已完成：G3、受控产品接入、Debug/Release 163/163、AutoCAD 2021 连续实机 16/16 |
| OR2 | 四边及以上复杂正交房间复核 | 已完成：既有 `TILEORTHO` 覆盖多凹凸、阶梯、狭通道和同线多片段；Debug/Release 170/170、AutoCAD 2021 步骤 1～10 全部通过 |
| DOR2 | 复杂正交房间工程拍砖候选核心 | 已完成：整房/显式主次区候选、实际异形砖、突出带独立/吸收、策略未决出口和 10,000 上限；Debug/Release 187/187，不含 AutoCAD 产品接入 |
| DOR3 | 复杂房人机协同决策与项目策略核心 | 已完成：分层问题、候选状态、研究/生产 `DecisionRecord` 和稳定顺序；Debug/Release 195/195，不含 AutoCAD 产品接入 |
| DOR4 | 复杂房 PaletteSet UI 接入 | 已完成：`TILEORTHOUI`、只读决策/候选详情、唯一自动或已记录人工决定的预览许可、零写入边界；Debug/Release 201/201，最小面板实机冒烟通过，完整宿主链由 DOR5 验收覆盖 |
| DOR5 | 复杂房 PaletteSet 输入绑定与只读决策展示 | 已完成：受控边界/策略/语义/候选记录输入、真实 DOR3 结果绑定、零写入预览请求；Debug/Release 208/208，AutoCAD 2021 实机功能项通过，工作副本前后 SHA-256 一致 |
| DOR6 | 复杂房引导式 Palette 交互重构 | 已完成：六步中文引导、图面选择桥接、候选分组、面板内人工原因与失效提示、连接边/整墙门洞恢复动作和零写入；Debug/Release 221/221，AutoCAD 2021 完整实机清单通过，工作副本前后 SHA-256 一致 |
| DOR7 | 普通用户产品化交互与同源图面可视预览 | 已完成：四阶段界面、同源绘图计划、确定性 SVG、零写入临时预览；Debug/Release 235/235，集中实机与末次定向复核通过，DWG 哈希不变 |
| DOR8 | 确认方案正式写回与一次撤销闭环 | 已实现编码与自动验证，待 AutoCAD 2021 DWG 副本实机收口：只把已验收的同源绘图计划写入专用图层，不另建排版算法 |

## 9. 技术决策

- 采用 AutoCAD 2021 Managed .NET 插件路线。
- 使用 C# 与 .NET Framework 4.8。
- 核心算法与 AutoCAD 适配分离。
- 先完成技术探针，再创建完整解决方案。
- 第一版不引入第三方几何或优化依赖。

详细记录见 `docs/adr/0001-autocad-2021-managed-net-plugin.md`。

## 10. 风险

- AutoCAD 具体行业版本未知，可能影响 API 或插件加载。
- `NETLOAD`、可信路径或安全加载策略可能阻止程序集加载。
- 开发机可能缺少 Visual Studio、.NET Framework 4.8 Developer Pack 或 AutoCAD 2021 SDK。
- DWG 中的 UCS、单位、端点微误差和墙线语义可能与假设不一致。
- 未签名程序集或安装包可能触发 Windows/安全软件提示。

## 11. 环境与输入确认

- Windows 10 x64、AutoCAD 2021 简体中文 `R24.0.47.0.0` 已确认；
- `NETLOAD`、Visual Studio 2022、MSBuild、VSTest 和 .NET Framework 4.8 开发组件均已确认可用；
- 最终只读脱敏 DWG 夹具已建立并完成 M4 实机验收；
- 四条边界已确认是模型空间、WCS 轴对齐、闭合且代表本次测试房间边界的独立 `LINE`；
- M4、M5 范围内无待输入项；真正发布授权仍需用户另行明确。

## 12. 版本与发布

- 技术探针版本：`0.0.1`。
- 首个可用原型：`0.1.0`。
- 正式发布物进入 `dist`，包含插件包、说明、已知限制和 SHA-256。
- 未经明确授权，不推送远程仓库、不创建 Release、不部署。

## 13. M2 完成记录

- 已建立 `TileLayout.sln`、`TileLayout.Core` 和 `TileLayout.Core.Tests`。
- 公共模型包括三维点、三维线段、WCS 轴对齐矩形、验证结果和分格结果。
- 坐标公差集中为 `1e-6 mm`，等于公差时视为相等，超过公差时拒绝或保留实际余量。
- 四线验证覆盖数量错误、非有限坐标、不同高程、退化线、非轴对齐、非闭合、重复边和缺失边。
- 网格算法固定从西南角开始，先由西向东生成竖向内部分格线，再由南向北生成横向内部分格线；东侧、北侧直接截断。
- 2026-07-19：锁定模式依赖恢复、Debug/Release 完整解决方案重建和两套各 24 项核心测试均通过；M1 探针成功写入标准 Debug/Release 输出目录，未复制 Autodesk DLL。
- 首次验证时 AutoCAD 正在占用原 `build\probe\Debug` 探针 DLL；用户正常关闭 AutoCAD 后，标准输出目录重建通过。未强制结束 AutoCAD 进程。
- M3 已于 2026-07-20 完成；正式 DLL 加载、完整宿主行为和一次撤销均已由用户实机验证通过。

## 14. M3 完成记录

- 已新增正式 `TileLayout.AutoCAD` 项目，输出 `TileLayout.AutoCAD.dll`；M1 `TileLayout.AutoCAD.Probe.dll` 保留且未改为正式插件。
- 正式命令名冻结为 `TILE600`，专用图层为 `TILE_LAYOUT_600`。
- 命令限制为当前图纸模型空间和毫米单位，只接受恰好四条模型空间 `LINE`。
- 四条墙线只以只读方式打开，端点复制为核心 `Point3D`/`LineSegment3D` 后调用 `RectangleValidator` 和 `TileGridCalculator`；AutoCAD 项目没有重复矩形或 600 mm 网格算法。
- 核心验证失败发生在创建图层之前；图层创建和全部分格线写入处于同一事务，异常时不提交。
- 成功消息报告宽高、完整列/行、东/北余量及分格线数；插件代码不调用 DWG 保存操作。
- 增加 3 项不引用 Autodesk 程序集的适配文本纯函数测试。2026-07-19，Debug/Release 两套测试均为 27/27 通过，其中原 M2 24 项继续通过。
- 2026-07-19，NuGet 锁定模式恢复及 Debug/Release 完整解决方案重建通过；正式插件两个输出目录均未复制 `AcCoreMgd.dll`、`AcDbMgd.dll` 或 `AcMgd.dll`。
- 自动验证期间 AutoCAD 未运行，未发生 DLL 锁定；没有启动或关闭 AutoCAD，也未读取、修改或保存任何 DWG/DOCX。
- 2026-07-20 用户实机确认：正式 DLL 可加载，`TILE600` 可执行；3600 × 3000 与 4250 × 3100 分格线生成正确；500 × 500 正确报告 0 列、0 行、东/北余量各 500 mm 和 0 条内部分格线。
- 2026-07-20 用户继续实机确认：选择数量错误、非闭合和非轴对齐输入均被拒绝，给出正确提示且无错误生成；模型空间和毫米单位限制正确；原墙线不变；一次 `U`/`UNDO` 完整撤销；插件未自动保存图纸。
- M3 自动检查与宿主实机检查全部通过。详细记录见 `docs/autocad-integration-m3.md`；M4 和 M5 后续也均已完成。

## 15. M4 启动与完成记录

- 2026-07-20 已完整复核 Git 基线、全局/项目规则、V0.1 需求、M1/M2/M3 文档、ADR、项目配置、全部现有源码和自动测试；未提交 M2/M3 文件是用户认可的工作区状态，继续保留。
- M4 启动时 `inputs` 只有策划书、`tests/fixtures` 只有说明文件；随后用户提供 `inputs/test.dwg`，所有结果均来自实际文件和宿主操作，没有伪造 AutoCAD 实机结果。
- 已建立 `docs/dwg-acceptance-m4.md`，规定原始输入、脱敏工作副本和最终夹具的隔离方式、SHA-256 证据、初学者实机步骤、16 项验收矩阵及缺陷处理门。
- M4 已检查匿名图层、实际非原点坐标、统一/不同高程、邻近对象选择、错误回滚、既有/锁定输出图层、重复执行、一次撤销和不自动保存。
- 全部实机结果、原始 DWG 前后哈希和宿主证据均已记录；未发现可复现产品缺陷，未修改 M2/M3 产品源码。
- 2026-07-20 自动质量门：锁定模式恢复通过；Debug/Release 各 27/27 测试通过；因 AutoCAD 正在运行，正式插件和完整解决方案使用 `build/plugin-m4-verify`、`build/solution-m4-verify` 备用目录完成 Debug/Release 重建，输出未复制 Autodesk DLL。未启动或关闭 AutoCAD。
- 2026-07-20 已只读核验 `inputs/test.dwg`：31,890 字节、只读、SHA-256 为 `646A3A7A22CF40E5EC0B9CF8621A17AFAB09BB27928772C05D9CB3F4202DDA75`；已复制到被 Git 忽略的 `work/m4-redaction/test-working.dwg`，初始哈希一致，并仅对工作副本解除只读属性。原始文件未打开或修改。
- 2026-07-20 用户确认工作副本无敏感文字、无外部参照/底图，已抽查边界为 `LINE`，`INSUNITS=4`、`TILEMODE=1`；已建立只读 `tests/fixtures/m4-real-room-sanitized.dwg` 及同名说明，最终夹具大小 31,890 字节且 SHA-256 与原始基线一致。DWG 未获 Git 授权，继续由 `.gitignore` 排除。
- 2026-07-20 M4-01 基线通过：四条边界均为模型空间图层 `0` 的独立 `LINE`，句柄 `29a/29b/298/297`，统一高程 Z=0，组成 5600 × 8600 mm 轴对齐闭合矩形；颜色/线型/线宽均为 `ByLayer`；人工预期为完整列/行 9/14、东/北余量 200/200 mm、内部线 23 条。
- 2026-07-20 M4-03 数值和输出通过：四条墙线颜色/线型/线宽均为 `ByLayer`；`TILE600` 实机报告 5600 × 8600 mm、完整列/行 9/14、东/北余量 200/200 mm，并生成 23 条；`_QSELECT` 独立确认正好 23 个项目，后续 M4-15 交叉确认执行前同名图层不存在。
- 2026-07-20 M4-15 与墙线保护通过：一次 `U` 同时移除本次创建的 `TILE_LAYOUT_600` 图层和 23 条分格线；四条墙线句柄、图层、模型空间、起终点、长度、方向和 Z 均与 M4-01 完全一致。非当前/锁定墙线图层变体随后由 M4-02 验证通过。
- 2026-07-20 M4-13 通过：同一房间连续执行两次后，`_QSELECT` 确认同一输出图层共有 46 个项目。首次 M4-14 流程受中间 `_QSELECT` 干扰，不作产品判定；随后按干净流程复测通过。
- 2026-07-20 M4-14 复测通过：关闭且不保存、干净重开后连续执行两次 `TILE600`，第二次成功后未插入其他命令而立即执行一次 `U`；`_QSELECT` 确认输出从 46 条恢复为 23 条，第一次结果和输出图层保留。
- 2026-07-20 M4-04 通过：把既有 `TILE_LAYOUT_600` 设为红色、`Continuous`、默认线宽后再次执行，项目由 23 增至 46；命令复用同一图层且执行前后颜色、线型和线宽保持不变。
- 2026-07-20 M4-12 通过：AutoCAD 2021 Managed API 允许向锁定的 `TILE_LAYOUT_600` 追加实体，命令完整成功，项目由 46 精确增至 69，无部分写入；执行后仍保持锁定、红色、`Continuous`、默认线宽。V0.1 未规定锁定输出图层必须拒绝，因此不判定为产品缺陷、不修改代码。
- 2026-07-20 M4-09 通过：取消选择时正确提示未完成选择；选择五条时明确报告本次选择 5 条并拒绝，且无输出图层。
- 2026-07-20 M4-10 通过：三条后按 Enter、非闭合四线、含斜线的非轴对齐四线分别得到准确提示，均未生成任何对象；用户最终确认无输出图层残留。
- 2026-07-20 用户明确确认全部选择/几何错误测试后不存在 `TILE_LAYOUT_600`，M4-10 完整通过。M4-05 实际非原点坐标也已通过：西南角 `(915.5972, 1270.2809, 0)` 的 5600 × 8600 mm 房间生成结果与人工计算完全一致。
- 2026-07-20 M4-06 通过：干净重开后把四条墙线整体移动到统一 Z=100，`TILE600` 仍正确报告 5600 × 8600 mm、9/14、余量 200/200 mm并生成 23 条；`_QSELECT` 和属性面板确认全部输出线 Z=100。
- 2026-07-20 M4-07 通过：干净重开后只把一条墙线移动到 Z=100，`TILE600` 准确提示四条线必须位于同一 WCS 高程，未生成任何对象且无输出图层残留。
- 2026-07-20 M4-02 通过：干净重开后把四条墙线移入非当前且锁定的 `WALL_TEST_ANON`；`TILE600` 仍正确生成 23 条，用户确认墙线图层、句柄、坐标、Z 和锁定状态均与预期一致，输入对象未被改写。
- 2026-07-20 M4-08 通过：在房间内临时加入圆和未分解矩形并一并框选时，选择器只找到四条 `LINE`，命令完成后 `_QSELECT` 确认 23 条输出；用户确认两个邻近对象仍存在且未变化。
- 2026-07-20 M4-11 通过：布局空间和 `INSUNITS=Inches` 均在选择前被拒绝并提示明确，无输出残留。M4-16 通过：关闭不保存并重开后 `INSUNITS=4`，无输出图层、临时圆或临时矩形，原四线仍在；原始只读 DWG 哈希仍与初始基线一致。
- 文件系统复核当前 AutoCAD 进程版本为 `R24.0.47.0.0`，DWG 文件头为 `AC1032`（AutoCAD 2018 DWG 格式系列）。用户最终确认 `TILEMODE=1` 并执行 World UCS。
- 2026-07-20 M4 最终回归：NuGet 锁定模式恢复通过；Debug/Release 自动测试各 27/27 通过；完整解决方案 Debug/Release 在 `build/solution-m4-final` 备用目录重建通过，未复制 Autodesk DLL；`git diff --check` 通过。

## 16. M5 发布检查记录

- 2026-07-20 开始 M5 前确认分支为 `main`、Git 基线为 `546663c`；M2、M3、M4 已验证但未提交的改动完整保留，没有清理、回退或覆盖。
- AutoCAD 2021 仍在运行，因此完整解决方案 Release 使用 `build/solution-m5/Release` 备用输出目录重建通过，没有覆盖标准插件目录或强制关闭宿主。
- NuGet 锁定模式恢复通过；Release 自动测试 27/27 通过。
- `TileLayout.AutoCAD.dll` 和 `TileLayout.Core.dll` 的 AssemblyVersion/FileVersion 均为 `0.1.0.0`。
- 已建立 `dist/TileLayout-0.1.0/` 和 `dist/TileLayout-0.1.0.zip`；压缩包只含两个运行 DLL 与 `使用说明.md`，不含 Autodesk DLL、探针、测试组件、PDB、DWG、缓存或日志。
- 压缩包最终 SHA-256 为 `322077112229CA8E0EDFB0CEE0B1F3F192A24EAC2CC23D11DCEA413CC3431141`，包外 `dist/TileLayout-0.1.0-sha256.txt` 同时记录压缩包和三个包内文件的校验值。
- 安装、`NETLOAD`、`TILE600`、支持范围、已知限制和验证摘要已写入包内说明；完整发布检查见 `docs/release-v0.1.0.md`。
- M5 没有发现可复现产品缺陷，未修改产品源码、未增加无关测试。
- 2026-07-20 用户完成唯一一次发布包冒烟：从 Release 交付包加载正式 DLL 成功，在脱敏夹具执行 `TILE600` 正确生成 23 条，并可一次撤销。未重复 M4 错误矩阵。
- M5 全部验收项已通过；没有未验证项。
- 2026-07-20 经用户明确授权，已创建私有仓库 `PermissionDog001/DEV-001-AutoCAD-Tile-Layout`，推送 `main`，在验收提交 `b31bc13` 上创建 `v0.1.0` 标签，并发布包含 ZIP 与 SHA-256 清单的正式 GitHub Release；尚未公开发布或部署。
- M4-01 至 M4-16 全部通过，未发现违反 V0.1 的可复现产品缺陷，因此未修改 M2/M3 产品源码。原始 `inputs/test.dwg` 最终仍为只读、31,890 字节，SHA-256 与初始基线一致；DWG 未获 Git 授权，继续忽略。

## 17. V0.2 范围冻结记录

- 2026-07-20 用户确认 V0.2 主目标为“自定义方砖/矩形砖规格，其他几何和优化能力暂缓”。
- 新命令冻结为 `TILELAYOUT`，按次输入砖宽和砖高，默认均为 600 mm；宽沿 WCS X、高沿 WCS Y，不自动旋转。
- 新命令固定写入 `TILE_LAYOUT`；现有 `TILE600` 和 `TILE_LAYOUT_600` 保持 V0.1 行为，作为兼容性回归基线。
- 房间输入继续限制为模型空间、毫米单位、同一高程、WCS 轴对齐的四条独立 `LINE`；西南角起铺、灰缝 0、东/北直接截断规则保持不变。
- 单次新命令最多允许生成 10,000 条内部分格线；超限必须在创建图层前拒绝，避免溢出、卡死或海量对象分配。
- V0.2 不处理旋转矩形、多段线输入、异形、洞口/柱、居中/窄砖优化、灰缝、多房间、旧结果替换、参数窗口或参数持久化。
- 开发顺序冻结为 M6 参数化核心、M7 AutoCAD 适配、M8 自动质量门、M9 脱敏 DWG 实机验收、M10 V0.2.0 发布检查；M7 完成前保持串行。
- 完整需求见 `docs/requirements-v0.2.md`，技术方案和验收顺序见 `docs/technical-plan-v0.2.md`。
- 本次范围任务只修改需求和项目状态文档，未修改产品源码、测试、构建配置、程序集版本或发布物。

## 18. M6 完成记录

- 新增宿主无关、不可变的 `TileLayoutParameters`，统一验证砖宽和砖高必须有限且严格大于 `GeometryTolerance.Coordinate`。
- `TileGridCalculator` 新增参数化入口；既有固定 600 × 600 mm 入口作为兼容包装，与参数化入口共用单一算法来源。
- `TileLayoutResult` 现在保留实际参数，同时保持既有房间、完整列/行、余量和只读分格线契约。
- 参数化入口先用数值路径统计内部线数，通过 10,000 条上限后才创建集合；10,001 条和极小合法规格的超大结果会抛出专用异常，不进行海量分配或开放式循环。
- 新增参数验证、五个冻结样例、矩形砖方向、小数/偏移/高程/公差、超限边界、极小规格和固定 600 mm 逐线兼容/不限额测试；测试总数由 27 增至 48。
- 2026-07-20 锁定模式恢复、核心测试项目 Debug/Release 重建及两套 48/48 测试均通过；完整解决方案 Debug/Release 使用 `build/solution-m6` 备用输出目录重建通过，未复制 Autodesk Managed DLL。
- 验证期间 AutoCAD 2021 正在运行，未被启动、关闭或操作；未修改 DWG、`dist`、V0.1.0 Release、程序集版本或远端状态。
- M7 尚未开始：没有新增 AutoCAD 命令、参数提示、`TILE_LAYOUT` 图层、宿主消息或实机写回。详细验收见 `docs/core-parameterization-m6.md`。

## 19. M7 完成记录

- 新增正式命令 `TILELAYOUT`，按砖宽、砖高顺序提示，每次执行默认均为 600 mm；砖宽沿 WCS X、砖高沿 WCS Y，不自动旋转。
- 零值、负值及不满足 M6 参数契约的尺寸会显示明确错误并继续提示；任一参数取消后在进入四线选择前停止。
- `TILE600` 与 `TILELAYOUT` 共用模型空间和毫米单位检查、四条模型空间 `LINE` 过滤、只读核心快照、矩形验证、事务写回、异常回滚和原图保护提示，没有复制完整命令实现。
- `TILE600` 继续调用不限额的固定 600 × 600 mm 核心入口并写入 `TILE_LAYOUT_600`；既有成功、验证、选择、环境和失败消息保持兼容。
- `TILELAYOUT` 调用 M6 参数化核心并固定写入 `TILE_LAYOUT`。核心在生成结果集合前执行 10,000 条检查，适配层仅在计算成功后才创建或复用图层；既有同名图层只读取其 ID，不修改属性。
- 新增宿主无关的参数化成功、参数错误和超限消息测试；连同既有回归，Debug/Release 自动测试各 51/51 通过。
- 锁定模式恢复及完整解决方案 Debug/Release 重建通过。验证时 AutoCAD 2021 正在运行，因此输出使用 `build/solution-m7/Debug` 和 `build/solution-m7/Release` 备用目录，未覆盖已加载 DLL，未强制关闭宿主；输出未包含 Autodesk Managed DLL。
- 本里程碑未操作任何 DWG，未修改 `dist`、V0.1.0 Release、程序集版本、Git 标签或远端状态，也未提交或推送。
- M7 没有宣称完成宿主实机验收。参数默认/重输/取消、矩形砖方向、`TILE_LAYOUT` 属性保持、超限图层前拒绝、一次撤销、失败回滚、原墙线保护和不自动保存证据当时留给 M9；这些宿主证据现已由 M9 补充。详细记录见 `docs/autocad-adapter-m7.md`。

## 20. M8 完成记录

- 2026-07-20 已完整复核 M6/M7 需求、方案、实现、项目配置和全部自动测试；`TILE600` 兼容、`TILELAYOUT` 参数/取消顺序、共享选择与事务路径、`TILE_LAYOUT` 复用、10,000 条图层前拒绝及三类宿主无关消息均符合冻结设计。
- 未发现由 M6/M7 引入且可通过当前自动环境复现的产品缺陷；M8 没有修改产品源码或增加回归测试。
- NuGet 锁定模式恢复通过；Debug/Release 测试项目重建通过，自动测试各 51/51 通过。
- AutoCAD 2021 正在运行，因此完整解决方案 Debug/Release 使用新的 `build/solution-m8` 和 `build/obj-solution-m8` 隔离目录重建通过；未覆盖标准插件 DLL，未启动、关闭或操作宿主。
- 两套完整输出均未复制 `AcCoreMgd.dll`、`AcDbMgd.dll` 或 `AcMgd.dll`；核心项目只引用 `System` 和 `System.Core`。
- 核心与正式插件的 AssemblyVersion/FileVersion 均保持 `0.1.0.0`；`dist/TileLayout-0.1.0.zip` 哈希和本地 `v0.1.0` 标签保持既有基线，没有执行远端写入。
- 原始 `inputs/test.dwg` 仍为只读、31,890 字节，SHA-256 为 `646A3A7A22CF40E5EC0B9CF8621A17AFAB09BB27928772C05D9CB3F4202DDA75`；没有执行任何 DWG 操作。
- M8 详细证据见 `docs/automated-quality-gate-m8.md`。其后 M9 AutoCAD 2021 脱敏 DWG 实机验收已完成，M10 V0.2.0 发布检查仍未开始。

## 21. M9 完成记录

- 2026-07-20 至 2026-07-21，使用 `work/m9-acceptance/m9-working.dwg` 在 AutoCAD 2021 简体中文 `R24.0.47.0.0` 完成 V0.2 实机验收；原始 `inputs/test.dwg` 未被打开或修改。
- 首次会话复用了已加载的旧 `build/plugin/Debug` 同版本程序集，导致 `TILELAYOUT` 未注册；用户正常退出且不保存后，新会话只加载 `build/solution-m9/Debug` 隔离 DLL。该宿主加载状态已用文件锁核对，不判定为产品缺陷。
- 当前 V0.2 DLL 下，`TILE600` 仍为 9/14、余量 200/200 mm、23 条并写入 `TILE_LAYOUT_600`；`TILELAYOUT 600×600` 统计相同并写入 `TILE_LAYOUT`，逐线自动回归继续证明两者几何一致。
- 600 × 1200 为 9/7、16 条；交换为 1200 × 600 后为 4/14、18 条，确认宽沿 WCS X、高沿 WCS Y 且不自动旋转；800 × 800 与 700 × 1200 的统计及第一条竖线关键坐标均实机一致。
- 零值/负值重输、砖宽取消、砖高取消、选择取消和含斜线的无效四线均正确停止且无输出残留。
- 0.5 × 9000 mm 预估 11,199 条，在 `TILE_LAYOUT` 不存在和已存在两种状态下都于图层创建/修改前拒绝；既有对象数和图层属性不变。
- `TILE_LAYOUT` 预设红色、`Continuous`、0.30 mm、锁定后可完整追加，属性和锁定状态保持；多规格累计对象数与逐次新增数完全一致。
- `TILELAYOUT` 与 `TILE600` 成功结果均可一次 `U` 撤销；原墙线句柄 `29a/29b/298/297`、图层和端点与 M4 基线一致。
- 关闭工作副本并选择“不保存”后，原始输入、只读夹具和 M9 工作副本均为 31,890 字节，SHA-256 均为 `646A3A7A22CF40E5EC0B9CF8621A17AFAB09BB27928772C05D9CB3F4202DDA75`。
- 用户明确要求停止低收益重复实机步骤。未在宿主生成恰好 10,000 条，未人为注入 `AppendEntity`/`Commit` 异常，也未再重复一次界面重开核对；自动边界、无残留失败场景和关闭后同哈希证据均已记录，不伪造未执行结果。
- M9 启动时重新完成锁定恢复、Debug/Release 各 51/51 自动测试、`build/solution-m9` 完整解决方案双配置隔离重建和 Autodesk DLL 泄漏检查，全部通过。
- 详细证据见 `docs/dwg-acceptance-m9.md`。M10 未开始；未修改程序集版本、`dist`、V0.1.0 Release、Git 标签或远端状态，未提交或推送。

## 22. SC1 工程起铺控制记录

- 2026-07-21 在完整保留 M6–M9 未提交工作树的前提下启动；M9 证据继续作为唯一宿主基线，M10 没有开始。
- 已比较“四个房间角”“分别选择 X/Y 起排方向”和“任意基准点偏移”。当前采用四角方案：一次操作覆盖两个方向的四种组合，施工表达直接，且余量和坐标确定。
- `TileLayoutParameters` 增加 WCS 起铺角，旧的宽/高构造器仍默认西南；`TileLayoutResult` 在保持西南角兼容属性的同时提供四侧实际余量。
- 核心对东侧起铺使用 `eastX - TileWidth × n`，对北侧起铺使用 `northY - TileHeight × n`；始终先竖线后横线，并按起铺方向排序。
- `TILELAYOUT` 交互冻结为砖宽、砖高、`SW/SE/NW/NE` 起铺角、四线选择；默认 `SW`。角选择取消发生在边界选择和任何写入之前。
- `TILE600` 保持固定西南角、600 × 600 mm、原提示和 `TILE_LAYOUT_600`，不增加角选择。
- Debug 测试项目重建通过，56/56 自动测试通过；正式 AutoCAD 项目在 `build/start-control/Debug` 隔离输出编译通过，均为 0 警告、0 错误，且未复制 Autodesk Managed DLL。
- 自动阶段没有把编译结果冒充宿主证据；随后用户在新 AutoCAD 2021 会话加载 `build/start-control/Debug` 隔离 DLL，并使用脱敏工作副本完成最小实机。
- `TILELAYOUT 600×600 NE` 报告 9/14、西/南余量 200/200 mm、23 条；首条竖线 X=`5915.5972`，首条横线 Y=`9270.2809`，均与预期一致。
- 起铺角 Esc 在选择前取消且无输出；`TILE600` 无新增提示，仍为 9/14、东/北余量 200/200 mm、23 条，并可在无中间命令时一次 `U` 全部撤销。
- 最终关闭不保存后，原始输入、只读夹具和工作副本均为 31,890 字节，SHA-256 均为 `646A3A7A22CF40E5EC0B9CF8621A17AFAB09BB27928772C05D9CB3F4202DDA75`。未发现可复现产品缺陷。
- 任意基准点暂缓；非矩形房间只完成边界/依赖评估。下一独立里程碑建议先做单一、无洞、无自交的 WCS 正交闭合 `LINE` 区域，前提是先形成可用 Git 基线并重新冻结非矩形起铺锚点。
- 详细定义见 `docs/start-control.md`，非矩形风险和建议见 `docs/non-rectangular-room-assessment.md`。
- 未修改程序集版本、`dist`、V0.1.0 Release、Git 标签或远端状态，未提交或推送。

## 23. OR1 正交简单房间边界与裁切记录

- 2026-07-21 从唯一 Git 基线 `main@6aa51d9` 启动，工作树干净且仅本地领先 `origin/main` 1 个既有提交；保持串行，未创建并行任务或 worktree。
- 首个里程碑冻结为同一模型空间、同一 WCS 高程、4 条及以上 WCS X/Y 轴对齐独立 `LINE`，且必须形成单一、无洞、无自交、无重叠、无 T 形分叉和无分离环的简单闭环。
- 比较包围盒四角、边界顶点和任意 WCS 点后，采用“WCS 包围盒四角网格锚点”。它复用 `SW/SE/NW/NE` 的方向组合，但只定义网格相位，允许位于凹房间外，不再宣称是第一块砖的房内角；边界顶点和任意基准点暂缓。
- 新增 `AxisAlignedOrthogonalPolygon`、验证结果/错误、确定性端点归并和端点图验证。链式公差歧义拒绝；相邻共线碎段合并；规范化环固定为逆时针并从最小 X/Y 顶点开始，与选择顺序和线方向无关。
- 新增正交网格裁切：半开顶点规则、奇偶区间、凹角多片段、区间合并、共线边界扣除和小于等于集中公差的数值碎片丢弃。先计最终片段再创建结果，恰好 10,000 条允许，超过即拒绝。
- 新增独立 `TILEORTHO` 和 `TILE_LAYOUT_ORTHO`。交互为砖宽、砖高、包围盒网格锚点、4 条及以上 `LINE`；验证和片段上限均在图层创建前完成，原线只读，全部输出仍在一个事务中写入。`TILE600` 和 `TILELAYOUT` 保持既有四线矩形入口和消息。
- NuGet 锁定模式恢复通过；聚焦审查后 Debug/Release 测试项目及完整解决方案隔离重建通过；自动测试均为 81/81。正式插件双配置隔离编译通过，全部隔离输出未包含 Autodesk Managed DLL。
- 2026-07-21 聚焦代码审查复核了拓扑、公差归并、共线合并、扫描线顶点规则、边界扣除、最终片段上限、AutoCAD 只读输入和单事务写回。审查发现绝对 WCS 坐标直接参与鞋带面积公式时，大坐标偏移会因浮点消减把合法房间误判为零面积；现改为相对首顶点计算并增加 `1e12` WCS 偏移回归。未发现其他与冻结契约冲突的问题。
- 核心与正式插件程序集版本仍为 `0.1.0.0`；`dist/TileLayout-0.1.0.zip` 哈希仍为 `322077112229CA8E0EDFB0CEE0B1F3F192A24EAC2CC23D11DCEA413CC3431141`。
- 自动阶段没有把编译结果冒充宿主证据。随后用户在独立脱敏工作副本加载隔离 Debug DLL：共线分段矩形准确报告 3600 × 3000、6/5、0/0 和 9 条；L 形准确报告 1800 × 1800、3/3、0/0 和 4 条，且凹口内及共线边界无误生成；100 mm 断口被准确拒绝且既有结果不变。
- 共线分段矩形成功后未插入其他命令，立即一次 `U` 移除全部 9 条结果，原 5 条边界保留。最终关闭工作副本并选择不保存；原始输入、只读夹具和工作副本均为 31,890 字节，SHA-256 均为 `646A3A7A22CF40E5EC0B9CF8621A17AFAB09BB27928772C05D9CB3F4202DDA75`。
- 任意斜边、旋转网格、洞口/柱、多外环和多房间通缝不进入 OR1；M10 未启动，未修改版本、`dist`、V0.1.0 Release、Git 标签或远端状态，未提交或推送。

## 24. DR1 门洞控制的正交矩形工程排版规划

- 2026-07-22 在 `main@a9f6767` 上完成正交矩形业务规则整理；本阶段只修改规划文档，没有修改产品源码、测试、构建配置、DWG、版本或发布物。
- 门洞按整个洞口处理，双扇门不区分开启门扇；门洞控制区域决定大小砖分配，但不等同于几何起铺角。
- 任一边界砖采用对应砖尺寸 `0.42` 倍的全局绝对下限。自然末排余量满足下限时保持单边方案；低于下限时把“一块整砖 + 窄余量”重分配为“半砖 + 较大过渡砖”，中间继续排整砖。
- 进深方向将较大过渡砖放在门洞墙一侧、半砖放在对面；沿门方向将半砖放在离门洞较近一侧、过渡砖放在较远一侧。门洞居中时两种镜像均可，程序使用固定优先级并允许预览翻转。
- 核心计划新增宿主无关的门洞、轴向边界带和工程排版结果模型，不扩充现有四角枚举；四面门洞通过同一局部坐标算法作对称变换。
- AutoCAD 首版建议先实现可靠的门洞两端点确认、识别摘要和 `[接受/翻转/重选/取消]` 预览流程；门块/图元自动识别以后作为可回退的增强能力。
- 详细规则、两个样例、算法步骤和验收矩阵见 `docs/door-controlled-rectangular-layout.md`。
- 后续先完成 L 形、“凹”字形和“凸”字形正交房间的门洞控制规则发现，再进入宿主无关 DR2；这两项后续状态分别见第 25、26 节。
- M10 未启动，版本、`dist`、V0.1.0 Release、Git 标签和远端状态保持不变。

## 25. DOR1 门洞控制的正交异形房间第一轮规则发现

- 2026-07-23 完成 L-01、L-03、L-04 A～E、L-05和多凹凸P-01截图/DWG口述样例的第一轮整理；每个样例均记录几何及门洞、人工尺寸关系、确认规则、待确认问题、算法假设和预期验收结果。
- 已确认候选至少包括“整房单一网格相位后裁切”和“门洞主区矩形规则 + 次区连接组合”两类；房型本身不能预先决定必须全局排版还是分区排版。
- `0.42T` 是普通独立边界砖带的默认下限；连续异形砖的局部腿不逐腿检查。突出深度低于下限时先尝试与相邻砖合并且单轴总宽不超过一砖，无法吸收时才进入受控窄带例外。
- L-04和L-05确认存在第二绝对下限例外，200 mm和250 mm样例可以直接生成；但下限的准确数值、固定毫米/比例形式和等号规则尚未提供，不得在代码中猜测常量。
- 候选整体质量需要同时观察内部非整砖、内部过渡/断缝、边界砖质量、长缝连续、门口观感和关键墙砖对缝。墙砖对缝不是硬约束，也不能仅靠对缝数量抵消明显更差的中央版面。
- P-01进一步确认同一全局网格可以组合“转折自然对缝、合法边界砖裁切、连续异形砖收口”；左下150 × 100 mm缺口属于一块连续异形砖，不是两条独立违规窄砖。
- 详细证据、尺寸、截图哈希、跨样例矩阵和待确认项见 `docs/door-controlled-orthogonal-layout.md`。
- 下一独立编码任务冻结为“DR1矩形门洞核心 + DR2候选模型基座”：只修改宿主无关核心和自动测试，先冻结候选、实际砖块、诊断及排序输入模型；不接AutoCAD、不识别墙砖图元、不猜测第二绝对下限、不启动M10。该任务现已完成，见第 26 节。
- DR1自动测试和候选模型验收后，再串行进入异形候选生成：全局相位裁切、主次区组合、突出带吸收、实际砖块分类和可解释排序。综合美观仍无法唯一判定时应保留多个候选及原因，不得用未经确认的权重伪造唯一答案。
- 本轮只更新规则和开发计划文档；未修改产品源码、测试、构建配置、DWG、版本、`dist`、Git标签或远端状态，未提交或推送。

## 26. DR2 宿主无关矩形门洞核心与候选模型基座

- 2026-07-23 从用户指定的规则基线 `main@81aff90` 启动；开工时工作树干净，`main` 比 `origin/main` 领先 3 个既有提交。
- 新增 `DoorOpening`、`EngineeringRectangularLayoutParameters`、`EngineeringRectangularLayoutCalculator` 和 `EngineeringRectangularLayoutResult`。门洞范围使用沿墙 WCS 坐标表达，并可报告到沿墙两端的净距；核心不引用 Autodesk 程序集。
- 每个轴由 `BoundaryBandPlan` 明确标注门洞法向或沿墙角色、控制边、施工起铺边、自然余量、半砖/过渡砖所在边、内部整砖数和从 WCS 低端到高端的全部砖带宽度。
- 自然余量为 0、恰好或高于 `0.42T` 时保持自然方案；只有 `0 < r < 0.42T` 且至少存在一块整砖时才使用 `T + r = 0.5T + (0.5T + r)`。无法重分配的极小轴返回带结构化淘汰原因的失败候选，不静默生成。
- 门洞法向固定为门侧过渡砖、对侧半砖；沿墙方向固定为近门侧半砖、远门侧过渡砖。东、西、南、北门洞共用同一方位映射；居中默认北优先于南、西优先于东，并只为不对称的沿墙方案增加翻转候选。
- 新增可复用 `LayoutCandidate`、`AxisBoundaryBand`、`TileFootprint`、内部/边界分类、候选诊断和淘汰原因。矩形砖块轮廓按需生成，避免仅为候选模型预先分配大量砖对象。
- `LayoutCandidateMetrics` 只保存后续排序所需的原始指标：内部非整砖块数/面积、内部过渡缝长度、边界非整砖、低于默认下限的边界砖、最小边界带、相位重置、连续异形砖、首视线和关键对缝；没有总分、权重或唯一美观排序。
- 第二绝对下限仍未配置；当前只通过通用策略诊断码和原始指标建立扩展边界，不猜测固定毫米值、比例或等号规则。
- 新增 16 项测试方法，使完整自动测试由 81 增至 97。覆盖两个矩形人工样例、四面门洞旋转/镜像、`0.42T` 等号和略低边界、自然余量、整除、半砖重分配、矩形砖、居中默认/翻转、极小房间淘汰、实际砖块分类、候选指标、大 WCS、门洞范围和 10,000 条上限。
- Debug/Release 测试项目均为 0 警告、0 错误，自动测试各 97/97 通过。AutoCAD 2021 正在运行，因此完整解决方案 Debug/Release 写入 `build/dr2-solution` 隔离目录重建，未覆盖已加载 DLL；隔离输出未包含 Autodesk Managed DLL。
- 本阶段没有修改 `TileGridCalculator`、`OrthogonalTileGridCalculator` 或 AutoCAD 源码；`TILE600`、`TILELAYOUT`、`TILEORTHO` 继续由原路径构建，未新增命令、图层或 DWG 写回。
- 未读取、修改或生成 DWG，未启动 M10，未修改版本、`dist`、V0.1.0 Release、Git 标签或远端状态，未提交或推送。

## 27. DR3 AutoCAD 两点门洞交互与写回

- 2026-07-24 从用户指定基线 `main@1297f7e` 启动；开工时工作树干净，`main` 比 `origin/main` 领先 4 个既有提交，全程串行且未创建 worktree。
- 新命令冻结为 `TILEDOORRECT`，专用输出图层冻结为 `TILE_LAYOUT_DOOR_RECT`；`TILE600`、`TILELAYOUT`、`TILEORTHO` 的命令名、提示、算法入口和图层语义未改变。
- 交互顺序为“选择四条矩形边界 `LINE` → 输入砖宽/砖高 → 选择门洞两端点 → 门洞识别摘要 → 候选摘要和临时矢量预览 → 接受/翻转/重选/取消”。砖宽/高默认 600 mm，继续沿用现有尺寸验证。
- 新增宿主无关 `DoorOpeningPointAdapter`：两点必须为有限 WCS 坐标、与房间同高程、在集中公差内匹配同一面墙、投影后不同且不超出墙段；墙端公差内数值夹到墙端，超过公差、分属两墙或重合均拒绝。
- 新增宿主无关 `DoorLayoutPreviewSession`：默认只选择 DR2 的 `DefaultCandidate`；只有 DR2 提供 `FlippedCandidate` 时才能翻转，翻转在两个等价候选间切换；接受、重选和取消是明确终态。
- 门洞摘要报告墙面、洞宽、偏向、进门方向和到沿墙两端净距；候选摘要报告 X/Y 方案、四边实际砖宽、施工起铺方向以及自然余量、重分配、居中默认/翻转和淘汰诊断。
- 临时预览使用 `Editor.DrawVector` 和重生成清除，不向 AutoCAD 数据库加入临时实体。选择、两点、计算、摘要、翻转和重选阶段不访问正式输出图层。
- `EngineeringRectangularLayoutCalculator` 仍是唯一门洞排版算法入口；AutoCAD 适配没有复制半砖、过渡砖、居中优先级或候选生成规则。
- DR2 的 10,000 条检查发生在候选与图层创建前；接受写回前适配层再次核对所选候选线数。只有接受后才启动单个写事务，在其中创建/复用专用图层并追加全部分格线；异常不提交。
- 四条边界始终只读打开，门洞两点只是输入坐标；插件没有保存调用。成功写回设计为紧接一次 `U`/`UNDO` 整体撤销。
- 新增 19 项宿主无关测试，使完整套件由 97 增至 116。覆盖公差投影、同墙/异墙、超墙段、重合、不同高程、非有限坐标、居中翻转限制、接受/重选/取消终态，以及门洞/候选/失败/写回消息。
- NuGet 锁定模式恢复通过；完整解决方案 Debug/Release 重建均成功，自动测试各 116/116 通过；正式 AutoCAD 项目另在 `build/dr3-plugin/Debug` 隔离目录编译通过，AutoCAD 2021 API 的点提示、临时矢量预览和写回调用均通过编译。
- 自动阶段没有操作 DWG，也没有把编译结果冒充宿主证据。其后的 DR4 AutoCAD 2021 脱敏副本结果见第 28 节。
- DR3 未启动 DR4、DR5 或 M10；未修改程序集版本、`dist`、V0.1.0 Release、Git 标签或远端状态，未提交、推送、发布或创建 PR。

## 28. DR4 AutoCAD 2021 脱敏副本实机验收

- 2026-07-28，用户在新的脱敏工作副本中按 `docs/door-controlled-rectangular-layout.md` 第 11.4 节完成步骤 1～12；通过 12，未通过 0，未执行 0。
- 首次 `NETLOAD` 后 `TILEDOORRECT` 显示未知命令。只读文件锁核对确认当前 AutoCAD 进程实际加载的是旧 `build/orthogonal-room/Debug/TileLayout.AutoCAD.dll`，而指定的 `build/plugin/Debug/TileLayout.AutoCAD.dll` 未被加载；该现象与 M9 的同名同版本程序集会话复用一致，不判定为产品缺陷。
- 用户关闭工作副本并选择不保存，退出整个 AutoCAD 进程；新会话只加载 36,864 字节、SHA-256 为 `0A4FCD106F52B9B1BDA4F82690891B447E5678C13C16BA4C2870628693F0D750` 的 DR3 Debug DLL，`TILEDOORRECT` 随后正确注册。
- 1300 × 1300 mm、600 × 600 mm 居中西墙门洞样例准确给出西/东/南/北 `400/300/400/300` mm 默认候选；翻转只把南/北改为 `300/400` mm，第二次翻转恢复默认。
- 取消、重选、同点、异墙、超墙段以及分别在第一点和第二点提示按 Esc 均零写入；非居中门洞拒绝无等价候选的翻转且不改变预览。
- 居中候选翻转后接受，在 `TILE_LAYOUT_DOOR_RECT` 写入 4 条分格线，X=`400/1000`、Y=`300/900`；成功后未插入其他命令，紧接一次 `U` 完整撤销 4 条结果并保留原四条边界。
- 10002 × 0.5 mm、砖宽/高 1/1 mm 样例预估 10001 条，准确在 10000 条上限及正式图层创建/修改前拒绝。
- `TILE600`、`TILELAYOUT`、`TILEORTHO` 的命令名、原提示、原图层和合法小样例兼容冒烟全部通过。
- 最终关闭工作副本并选择不保存；工作副本开始和结束均为 31,890 字节，SHA-256 均为 `646A3A7A22CF40E5EC0B9CF8621A17AFAB09BB27928772C05D9CB3F4202DDA75`。正式 DWG 未操作，未发现需要修改代码的可复现产品缺陷。
- 本阶段只补充真实宿主证据；未启动 DR5 或 M10，未修改代码、程序集版本、`dist`、V0.1.0 Release、Git 标签或远端状态，未提交、推送、发布或创建 PR。

## 29. DR5 门块/门图元辅助识别样本门与规则冻结

- 2026-07-28 从用户指定基线 `main@a2f2679` 启动；开工时工作树干净，`main` 比 `origin/main` 领先 5 个既有提交。
- 初始项目内 `inputs`、`tests/fixtures` 和 `work` 共 6 个 DWG 路径均为同一四线房间副本，不构成 DR5 正样本。用户随后在 `inputs/dr5-intake` 提供并确认 S1、S2、S3 三个独立脱敏来源；三份文件均为 `AC1032`、哈希互不相同、`INSUNITS=4`、`TILEMODE=1` 且无 XREF。
- 三份输入已设为只读，并在 `work/dr5-inspection` 建立同哈希工作副本。用户完成 AutoCAD 对象盘点并关闭不保存后，三份工作副本的大小和 SHA-256 均与复制基线一致。
- 首轮识别冻结为显式可选的只读辅助层：默认仍直接点取门洞第一点；对象模式只分析用户当前选择，不扫描全图，不按名称或图层猜门。识别层只输出两个 WCS 候选端点，继续复用 `DoorOpeningPointAdapter` 和 DR2/DR3 后续流程。
- 只有唯一的单扇平开门“铰点—门扇线—开启弧”签名且端点通过现有同墙、公差、墙段和高程验证时才为 `High`。多候选、双扇、无弧、跨墙/墙角、非均匀缩放、外部参照/代理和任何需经验补足的结果均拒绝并回退现有两点流程；Esc 保持取消。
- S2 提供两个块参照单扇正样本：北墙 1000 mm 门洞 `(3479.4031,3615.9558,0)`～`(4479.4031,3615.9558,0)`，东墙 1000 mm 门洞 `(6379.4031,2597.6302,0)`～`(6379.4031,1597.6302,0)`。用户确认 S1 只有一个双扇门，S2 只有两个单扇门，S3 包含一个双扇门和两个非门物体；因此 S1/S3 没有单扇正样本，S2 没有负样本。S3 两个非门物体可用于误识别拒绝，但具体对象类型尚未细分。
- 用户随后补充同来源 S1b/S2b/S3b：S1b-P1 因房间边界非 WCS 轴对齐改列前置拒绝，S1b-P2 后经宿主证据确认是顶层静态块；S2b 含一个非门物体负样本；S3b 两个单扇块原被人工分类为动态块，用户现已撤回该分类并确认现有图纸应当都没有动态块。新增三份 `AC1032` 文件哈希各不相同，输入只读；工作副本盘点并关闭不保存后哈希保持一致。
- 编码门冻结为至少 3 个独立脱敏来源族、每个来源至少 2 个标注正样本、每个拟支持输入容器至少出现于 2 个来源，并包含每来源至少 1 个近似负样本及全局歧义矩阵。首轮只支持动态块当前可见几何；静态块和散图元没有跨来源证据，统一回退。
- 精度复核确认 S1b-P1 门块端点 X 均为 `5505.10280507`，但东墙 `LINE` 从 X=`5505.10788556` 变化到 `5505.09693762`，横向偏移 `0.01094794 mm`、相对竖直偏转约 `0.000234637°`。现有矩形验证会在门识别前拒绝该房间；该门改列边界前置拒绝，不放宽门识别公差，也不计作正样本。首次宿主验收确认 S1b-P2 句柄 `33C` 是顶层静态块，也不计作动态正样本。
- 用户继续补充 S1c：`dr5-s1c-sanitized.dwg` 为 34,928 字节，SHA-256 `530483E6E3CFA25C61D0569A0BEE0AB0D4785E78DBC8953E9D1EB33F06271666`，`AC1032`、`INSUNITS=4`、`TILEMODE=1`、无 XREF。现有 `TILEDOORRECT` 边界验证能进入门洞第一点提示；人工标注的 1000 mm 东墙端点为 `(5179.4905,3346.8954,0)`～`(5179.4905,2346.8954,0)`。首次对象模式验收确认句柄 `2D7`、块名 `$DorLib2D$00000001`，AutoCAD API 判定为静态块，正确返回 `StaticBlock` 并回退；S1 当前没有动态正样本。
- 详细样本清单、输入契约、可信度、歧义拒绝、回退状态和验收矩阵见 `docs/door-entity-recognition-dr5.md`。
- 编码开始时依据人工分类记录为动态块正样本 `6/6`；首次 AutoCAD 2021 验收用真实 API 证据把 S1b-P2 和 S1c-P1 均改列静态块，用户随后撤回 S2/S3 的动态分类并确认现有图纸应当都没有动态块。当前确认动态正样本和来源覆盖均为 0，预登记门槛不成立。已完成的保守实现和自动测试保留，不能把它们表述为完整实机通过。

## 30. DR5 首轮动态门块辅助识别编码记录

- 2026-07-28 从 `main@a2f2679` 及四份预期 DR5 文档改动开始编码；既有样本盘点内容完整保留，没有清理或覆盖用户工作。
- `TileLayout.Core` 新增宿主无关弧模型、线弧签名识别、确定性状态和拒绝码。核心只接受唯一“铰点—门扇线—开启弧”几何，重复表达同一端点对会规范化，多端点对为歧义；不读取名称、图层、属性、典型门宽、距离、句柄或对象顺序。
- 宿主无关协调层把唯一候选继续交给原 `DoorOpeningPointAdapter`；只有唯一几何和原同墙/墙段/高程验证同时通过才为 `High`，否则为 `Ambiguous`、`Unsupported` 或 `Invalid` 并保留明确原因。
- AutoCAD 读取器只接受用户显式选择的当前数据库模型空间顶层动态 `BlockReference`，在只读事务中内存展开当前可见状态并收集 WCS `LINE/ARC`。静态块、散图元、XREF、代理、不可读/循环嵌套、非平面弧和非均匀缩放拒绝；门块没有被分解写回、重定义或修改动态属性。
- `TILEDOORRECT` 只在原第一点提示增加 `[对象(O)]`；直接点第一点仍走原第二点。对象失败回到同一第一点入口，对象选择 Esc 取消命令；对象 `High` 后复用原摘要、DR2 候选、预览、翻转、重选、接受和 `TILE_LAYOUT_DOOR_RECT` 单事务写回。
- 写回授权由原预览会话的 `Accepted` 终态显式门控。选择、识别、失败回退、预览、翻转、重选和取消仍不访问正式输出图层；接受后才调用原写事务，一次撤销语义未改变。
- 自动测试由 116 增至 140，覆盖旋转/镜像、大 WCS、端点/弧方向反转、唯一性、双扇/多候选、无弧、非门、非有限坐标、点适配失败、全部冻结拒绝码、静态/散图元/XREF/非顶层/非均匀缩放、两点默认、失败回退、Esc 和接受前零写回授权。
- 完整解决方案 Debug/Release 在 `build/dr5-solution` 隔离目录均 0 警告、0 错误重建通过；两套测试各 140/140。`build/dr5-plugin/Debug` 正式插件隔离编译通过，`TileLayout.AutoCAD.dll` 为 46,592 字节、SHA-256 `33371FE7C11AB68B908C7BDDD04699768460B9244496C01815FC3D54B877E65C`，输出未复制 Autodesk Managed DLL。
- 标准 `build/plugin/Debug` 被现有 AutoCAD 会话锁定，因此没有强制关闭宿主或覆盖已加载 DLL。核心与正式插件版本仍为 `0.1.0.0`，`dist`、发布物、Git 标签和远端未变；未提交、推送或创建 PR，未操作 `inputs` 中的 DWG，也未启动 M10。
- 自动质量门已经完成。首次 AutoCAD 2021 宿主验收确认 S1b-P2 和 S1c-P1 顶层块均为静态块，`StaticBlock` 拒绝、明确原因和两点回退符合预期；镜像比例 `1000/-1000/1000` 按绝对值正确视为均匀缩放。用户确认现有图纸应当都没有动态块，完整验收暂停。若继续动态路线，须重新取得满足矩阵的真实顶层动态门样本；若改做现有静态块，须作为新范围重新盘点和冻结，不能直接移除静态门禁。
- 路线决定：用户选择暂停动态块路线并等待未来真实样本；下一独立任务转入顶层静态门块辅助识别。从当前未提交工作树继续，先复核 S1/S2/S3 静态块的真实 API 类型、镜像/旋转/比例、属性和展开几何，重新冻结支持/拒绝矩阵及测试，再评估复用本轮核心；不启动 M10，不修改版本或发布物。

## 31. DR5-S 顶层静态门块规则与 G2 核心记录

- 2026-07-28 从 DR5 未提交工作树继续；既有动态代码、24 项测试和文档全部保留，未清理或回退。
- 使用 AutoCAD 2021 Core Console `R.47.0.0` 对 `work/dr5-inspection` 七份工作副本执行只读 API 探针。七份文件检查后与各自只读 `inputs/dr5-intake` 原件大小和 SHA-256 一致；没有保存或修改 DWG。
- S1/S2/S3 七个单扇几何件均为模型空间顶层静态 `BlockReference` / `AcDbBlockReference`，动态属性数 0；正样本覆盖约 0°/90°/180°/270°、两种负比例组合和属性标签 `A`，展开均为 4 LINE + 1 ARC。
- 真实静态签名是 50 mm 厚、975 mm 长的双线矩形门扇与 975 mm 弧，但规则只使用厚度 `T`、长边/半径 `L` 和几何关系，洞宽为 `L + T/2`；没有写死样本尺寸，也不读取块名、图层、属性值或典型门宽。
- S1/S3 双扇为非均匀 `1200/-1200/300` 且展开含椭圆；S2b/S3 非门块虽含多条线弧但不满足完整双线签名。它们已冻结为 `NonUniformScaling` 或 `NoCompleteSingleSwingSignature` 负样本。
- S1c-P1 是当前唯一同时带完整合法四线矩形并可由原 `DoorOpeningPointAdapter` 直接通过的端到端正样本。S2-P1/P2 的块导出洞口分别离人工北/东完成面 100 mm，继续由原点适配拒绝；不得选择最近墙或猜测墙厚。S1b/S3b 文件没有完整四线矩形，不能作为端到端正样本。
- 分层门槛冻结为 G1 API/规则发现、G2 宿主无关静态几何、G3 产品接入。G1 已满足；G2 新增 `StaticDoorGeometryRecognizer` 和 11 项测试后完成；G3 要求三个独立来源族各两个完整四线矩形且原点适配直通的正样本，当前未满足。
- Debug/Release 测试项目重建和两套 151/151 自动测试通过；现有 140 项全部保留，包括 `StaticBlock` 产品回退。完整解决方案 Debug/Release 在 `build/dr5s-solution` 隔离目录重建通过。
- `DoorGeometryRecognizer` 动态单线算法未修改；`DoorOpeningPointAdapter`、原摘要/预览/翻转/重选/接受、单事务写回和一次撤销流程也未改。AutoCAD 对象入口尚未调用静态核心，`StaticBlock` 门禁继续生效。
- 详细 API 表、几何公式、拒绝矩阵、测试边界和后续样本要求见 `docs/door-static-block-recognition-dr5s.md`。
- 本阶段未启动 M10，未修改程序集版本、`dist`、发布物、Git 标签或远端状态，未提交或推送。

## 32. DR5-S G3 新样本验收记录

- 2026-07-28 对 `inputs/dr5s-intake` 的 S1/S2/S3 新脱敏样本记录初始 SHA-256，在 `work/dr5s-g3-inspection` 建立同哈希副本；AutoCAD 2021 Core Console `R.47.0.0` 使用隔离配置和 `/readonly` 运行只读 API/几何联合探针，未降低 `SECURELOAD`、未保存 DWG。
- S1 的 4 条顶层边界 LINE 构成合法矩形；句柄 `339`、`33C` 均为顶层静态、均匀镜像、有 1 个属性的 4 LINE + 1 ARC 门块，静态核心各唯一导出 H/J，并由原点适配分别直通西墙和南墙。
- S2 的两个正件和 S3 的两个正件也都唯一导出 H/J；S2 的旧 100 mm 法向偏移已实际修正。但 S2 的 5 个四线组合、S3 的 15 个四线组合全部被现有 `RectangleValidator` 判为 `NonAxisAlignedLine`，不存在可原样送入点适配器的合法房间。
- S2/S3 非门块均为 `NoCompleteSingleSwingSignature`；S3 双开门是 `1200/-1200/300` 非均匀缩放并含椭圆，应在容器层以 `NonUniformScaling` 拒绝。所有真实正件仍都是镜像且有属性；非镜像/无属性只存在于负样本，三份文件也没有真实多候选静态块，不能替代正变体和 `MultipleDistinctSignatures` 宿主证据。
- G3 判定为未达到，已按门禁停止产品接入；没有修改 AutoCAD 对象读取、命令文案、预览或事务流程。三份输入复核后大小和 SHA-256 与初始值一致，详细表格及下一次样本要求见 `docs/door-static-block-recognition-dr5s.md`。

## 33. DR5-S G3 R2 修正版验收记录

- S2、S3 第二次修正版已确认更换，S1 保持原哈希。AutoCAD 2021 Core Console 使用隔离配置和 `/readonly` 对新 R2 工作副本重新运行 API、静态核心、全部四线组合和原点适配联合探针。
- S1 `339/33C`、S2 `2EA/345`、S3 `3BF/3C2` 均为模型空间顶层静态、均匀镜像、有属性的 4 LINE + 1 ARC 块；六项均为 `High`、1 个候选，并分别直通西/南、南/东、北/东完成面墙。
- S1、S2、S3 的四线矩形分别为 `367/368/369/36A`、`3C3/3C4/3C5/3C6`、`403/404/405/406`，三组均由现有 `RectangleValidator` 原样通过。S2 的旧 100 mm 偏移及 S2/S3 的非轴对齐边界均已解决。
- R2 S3 `32B` 非门仍为 `NoCompleteSingleSwingSignature`；既有真实非门、非均匀双开门和其他拒绝证据继续有效。
- G3 正样本子门槛现已通过，但同一正门几何的非镜像/无属性宿主变体和真实 `MultipleDistinctSignatures` 顶层静态块仍缺失。因此继续保留 `StaticBlock` 产品回退，未开始产品接入。
- R2 输入最终哈希：S1 `780C3D175A96D2D95096ACF0F2F81C4D35DB9CC9EBFC2AD904B57E6C1B860D7E`；S2 `07C63A9D71FEF82CF23208A62B5F8624C5A222688EE397CD6AE85FE3ACE44D0D`；S3 `4E2457008CC5B7BE6010A459AAE99BE42546F90EA59DD84A1EF7893D61653C46`。三份工作副本哈希相同，原件未改变。

## 34. DR5-S G3 R3 派生样本验收记录

- `dr5s-s1b-sanitized-a.dwg` 中顶层静态块 `387` 为非镜像、无属性、8 LINE + 2 ARC，真实产生两个不同候选并准确返回 `Ambiguous (MultipleDistinctSignatures)`；它补齐多候选拒绝和 S1 负样本，但不能作为唯一正件变体。
- `dr5s-s2b-sanitized-a.dwg` 的 `2EA/345` 仍均为 `-1000/-1000/-1000` 镜像块，且各有 1 个属性 A；两件虽继续唯一识别并直通南/东墙，但没有形成非镜像或无属性变体。
- R3 后只缺一个合法四线宿主样本：同一门几何分别以“非镜像 + 有属性 A”和“非镜像 + 属性引用 0”出现，并均唯一识别、原点适配直通。
- R3 哈希：S1-a `11EE0535D2552D609AE04ABB7677DF1F05C83F76727EF8A898671C9A07C873B9`；S2-a `614C58C7E6CAEAD8E18EDBBE8FBCFA7698BE882863356D2C76E83D8010763300`。输入和工作副本探针前后均未改变。

## 35. DR5-S G3 R4 非镜像/属性变体验收记录

- `dr5s-s2b-sanitized-b.dwg` 初始大小为 53,079 字节，SHA-256 为 `85778E9ED94FD6BCA19D48F810C18D133077003120BDB113435206E4587EDDF5`；只读探针运行于新的同哈希工作副本。
- 南门 `2EA` 已成为 `-1000/-1000/1000`、非镜像、属性 A、4 LINE + 1 ARC 的唯一正件，并由原点适配直接通过南墙。
- 东门 `3F4` 已成为 `1/1/1`、非镜像、属性引用 0 的顶层静态块，但块内只有 3 LINE + 1 ARC；遗漏的第四条 50 mm 短边仍是模型空间顶层 LINE `3E2`，所以核心正确返回 `NoCompleteSingleSwingSignature`。
- G3 仍未整体达到。仅需把 `3E2` 纳入东门块定义并复核其唯一 H/J 与东墙直通；此前不启动产品接入，不修改动态路线、版本、`dist` 或发布物。

## 36. DR5-S G3 R5 与受控产品接入记录

- 修正后的 `dr5s-s2b-sanitized-b.dwg` 为 41,043 字节，SHA-256 `5F60F5E07A0EC8D4FDA197644DB08B8A2962157FD73E1866C2BC3C176AF0FC05`；输入只复制到 R5 工作目录并由 AutoCAD 2021 Core Console 隔离 `/readonly` 读取。
- 南门 `2EA` 为非镜像、属性 A、4 LINE + 1 ARC、`High`/1 候选并直通南墙；东门 `41A` 为非镜像、属性引用 0、4 LINE + 1 ARC、`High`/1 候选并直通东墙。四条顶层边界 LINE 原样通过，属性未参与识别。
- 结合 R2 六正件、R3 多候选、R5 宿主变体和既有拒绝矩阵，G3 全部门槛达到。产品读取层显式区分动态与冻结静态路线；旧动态识别器不变，静态块必须依次通过顶层/XREF/均匀缩放、`StaticDoorGeometryRecognizer` 唯一签名及原 `DoorOpeningPointAdapter`。
- 原两点输入、预览、翻转、重选、取消和接受后单事务写回未改写。新增回归覆盖静态门禁、缺边/多候选/点适配拒绝、命令文案、预览和写回授权；完整解决方案 Debug/Release 隔离重建及两套 163/163 测试通过。
- Debug 正式插件为 48,128 字节、SHA-256 `BFD779FD6509C87F1F974916F1E1693B749B2BE75DDABAB3E6C8B220F61C601A`；核心为 62,976 字节、SHA-256 `514AAF89A955A84E4B83C4F5C3E21F5448F4DE2C7D3C16B5CB3EFADA04F81948`。Debug/Release 均未复制 Autodesk DLL，程序集版本保持 `0.1.0.0`，`dist` 未变。
- AutoCAD 2021 批量实机随后由用户按第 16 节一次连续执行并 16/16 通过；完整结果见第 37 节。动态路线继续暂停，未启动 M10，未提交、推送、发布或创建 PR。

## 37. DR5-S AutoCAD 2021 连续实机验收记录

- 用户在四份 `work/dr5s-g3-host-test` 同哈希副本连续完成清单，16/16 均符合预期；加载 DLL 哈希与构建记录一致。
- R5 东门静态无属性件和南门静态有属性件均为 `High`。东门非居中，连续两次翻转均被原 DR2 规则明确拒绝且保持默认候选；重选南门后接受，一次生成 21 条 `TILE_LAYOUT_DOOR_RECT` 分格线，紧接一次 `U` 整体撤销。
- S1/S3 旋转镜像正件、原两点路径、S1-a 两候选歧义、非门、散 LINE、缺边、非均匀双开门以及三个既有命令兼容均通过；失败、拒绝、重选和取消均为零写入。
- 四份工作副本在关闭不保存后与对应 intake DWG 的大小、SHA-256 完全一致；六份 intake DWG 的实机后最终哈希已写入 DR5-S 文档第 17 节。
- DR5-S 至此完成，未发现可复现实机产品缺陷。动态路线继续暂停；M10、版本、`dist`、发布物、标签、远端和提交状态均未改变。

## 38. OR2 四边及以上复杂正交房间复核

- 2026-07-29 从包含未提交 DR5/DR5-S 的当前工作树继续；这些改动保持原样，未清理、覆盖、回退或另建 worktree。动态门块路线继续暂停。
- 盘点确认 `TILEORTHO` 从 OR1 起的输入契约已是“4 条及以上模型空间 WCS 轴对齐 `LINE` 所成的单一、简单、无洞闭环”，验证器不按 L/U 特例分支；网格裁切按半开规则和奇偶区间输出零个、一个或多个室内片段。
- OR2 未改写核心算法、AutoCAD 命令、图层、事务、预览或门块路径；只新增多凹凸阶梯环接收/选择顺序、复杂房 16 段裁切、200 mm 狭通道、大 WCS 偏移和复杂成功文案回归。既有 L/U、多片段、顶点/贴边、公差、拒绝矩阵、10,000/10,001、门洞预览/事务及三个命令兼容回归继续运行。
- 明确支持共线分段矩形、L/U、多个局部凸出/凹入、阶梯、狭长通道和同候选线多室内片段；孔洞/柱洞、多房间、自交、T 形/悬挂、重复/重叠、斜线/圆弧/旋转、不同高程均继续明确拒绝。门洞控制复杂房、墙砖对缝和未冻结美观择优继续留待后续，未放宽公差或猜测连接关系。
- `inputs` 下 14 个 DWG 已在任务开始记录 SHA-256；本轮未打开、复制、写入或保存任何 DWG。Debug/Release 完整解决方案均重建成功，自动测试各 170/170；Autodesk Managed DLL 未复制，程序集版本、`dist`、发布物、标签和远端均未改。
- 用户已在 AutoCAD 2021 中连续执行 `docs/orthogonal-simple-room.md` 第 12.4 节步骤 1～10并确认全部符合预期：矩形/L/U/复杂阶梯/狭通道、同线多片段、拒绝、10,001 超限、三个既有命令兼容、一次撤销、关闭不保存和最终哈希均通过。OR2 未发现需要修改产品算法或适配器的可复现实机缺陷。

## 39. DOR2 复杂正交房间工程拍砖候选核心

- 2026-07-29 从包含 DR5、DR5-S 和 OR2 全部未提交改动的当前工作树继续；开始前完成两级 `AGENTS.md`、项目文档、三份规则文档、指定模型/算法/命令盘点，并记录 `inputs` 下 14 份 DWG 的 SHA-256。没有清理、覆盖、回退或另建 worktree。
- 编码前把 DOR1 结论分为硬规则、只记录的原始质量指标和未冻结规则。冻结范围是单一、简单、无洞的 WCS 正交房间，X/Y 砖轴、灰缝 0、整房单相位、显式主次区、平行继承、垂直分界重置、突出带独立/吸收、吸收后单轴不超过一砖、实际砖块分类和 10,000 最终片段上限。
- `EngineeringOrthogonalLayoutCalculator` 组合复用 DR2 的 `EngineeringRectangularLayoutCalculator`/单轴计划与 OR2 的 `OrthogonalTileGridCalculator` 扫描线裁切；没有复制矩形候选公式或正交裁切器。核心项目不引用 Autodesk DLL。
- 扩展 `LayoutCandidate` 以承载 `LayoutCandidateStructure`、区域相位、连接计划和策略未决状态；由实际 `TileFootprint` 计算指标，连续 L/阶梯砖的局部腿不作为独立窄砖。结果明确分开已满足冻结规则、策略未决与已淘汰候选，并保留诊断、淘汰原因和确定性顺序。
- L-01 可生成整房单相位及连续 L 形砖；L-03 可检查整体极值和凹区实际末排；L-04 A/B/C 可在显式分区上继承/重置并保留合法突出带；L-04 E 可枚举 `300 + 200 = 500` 吸收并拒绝 `600 + 200 = 800` 吸收；P-01 可生成多凹凸裁切、自然对缝和连续阶梯砖。
- L-04 D 的 200 mm 独立带和 L-05 的 250 mm 边界例外可作为带原始指标的“策略未决”候选生成，但第二绝对下限未冻结；L-05/P-01 的确认相位必须由调用方提供证据。多个候选不计算美观总分，不自动伪造唯一选择。
- 新增 17 项 DOR2 测试；完整解决方案在 `build/dor2-solution` 隔离目录以 Debug/Release 重建通过，两套自动测试各 187/187。覆盖样例、旋转/镜像、不同输入顺序、大 WCS、候选确定性、多候选保留和 10,000/10,001；原有 `TILE600`、`TILELAYOUT`、`TILEORTHO`、`TILEDOORRECT` 与 DR5-S 回归全部保留。

## 40. DOR3 人机协同决策与项目策略核心

- 继承 DOR2 的候选和原始指标，不增加几何算法或 Autodesk 依赖；`EngineeringOrthogonalDecisionResult`、`DecisionRequirement` 和 `EvaluatedLayoutCandidate` 将项目策略、房间语义、候选选择、输入不可信和能力边界分开。
- `DecisionRecord` 保留候选、策略版本、原因和例外接受。研究模式可保留人工例外；受控生产模式从不把人工例外标为自动合规。
- DOR3 的完整解决方案 Debug/Release 构建均为 0 警告、0 错误，自动测试各 195/195；本任务完整继承该未提交基线。

## 41. DOR4 复杂房 PaletteSet UI 接入

- 新命令 `TILEORTHOUI` 只打开“复杂正交房间工程决策”PaletteSet；初始状态明确为未载入结果，不读取/猜测房间、门洞、控制区、主次区、连接边或相位。
- 适配层 `OrthogonalDecisionPaletteSession` 按 DOR3 原始顺序呈现策略、房间语义和候选选择问题，并展示候选状态、原始理由、结构、诊断和指标。普通用户没有裸 X/Y 相位输入入口。
- 只有 `CanProceedAutomatically` 的唯一自动候选能直接请求预览。已匹配的 `DecisionRecord` 可进入已记录人工决定预览，并在研究/受控生产中都明确显示为非自动合规；问题未解决、候选未选、输入不可信、能力不支持和淘汰候选均不可预览。
- DOR4 的预览请求、取消、重置和候选切换不创建图层、实体、事务或写回授权；正式写回仍未接入，既有 `TILE600`、`TILELAYOUT`、`TILEORTHO`、`TILEDOORRECT` 流程未改变。
- 新增 6 项 Palette 会话自动测试，覆盖自动唯一、项目策略缺失、房间语义缺失、候选选择、研究/生产人工记录、取消/重置和稳定顺序。2026-07-29 已锁定模式恢复，完整解决方案 Debug/Release 均重建成功，自动测试均为 201/201。
- 2026-07-29，用户在 AutoCAD 2021 中加载 E 盘 `build/plugin/Debug/TileLayout.AutoCAD.dll` 并执行 `TILEORTHOUI`；空状态 PaletteSet 成功打开，面板未提供裸 X/Y 相位输入，命令行确认尚未载入房间结果且未生成任何对象。该证据完成最小面板冒烟；关闭重开、紧接 `U` 和测试副本前后 SHA-256 尚未独立记录，不能扩大宣称为完整宿主清单通过。
- 本轮未修改版本、`dist`、发布物、标签、远端，也未提交、推送或创建 PR。
- 2026-07-29，DOR3/DOR4 的 27 个源码、测试和文档文件已从临时 Codex 工作树逐项 SHA-256 校验后同步到本 E 盘项目；被覆盖的 7 个目标文件备份于 `work/codex-dor4-merge-backup-20260729`。从此处重新构建的插件才是正式实机加载入口。
- 本轮未新增 AutoCAD 命令、图层、预览、门块复杂房接入或写回事务。墙砖缝识别、内部孔洞/柱洞/多房间、旋转网格、自动区域猜测、第二下限和综合美观权重继续不在范围；未启动 M10，版本、`dist`、发布物、标签、远端和提交状态均未改变。

## 42. DOR5 复杂房 PaletteSet 输入绑定与只读决策展示

- 2026-07-29 从包含 DR5、DR5-S、OR2、DOR2、DOR3、DOR4 和实机记录的当前 E 盘未提交 working tree 继续；开始前完整读取两级规则和三份入口文档，未清理、覆盖、回退或使用 C 盘 worktree 作为来源。
- DOR4 起始基线已用 `build/dor5-baseline` 隔离输出重新核验：完整解决方案 Debug/Release 均成功，两套自动回归各 201/201。DOR4 实机仍只确认空面板打开和命令行零对象；关闭重开、`U`、副本前后 SHA-256 未独立验证。
- 编码前已在 DOR 文档冻结 `TILEORTHOUI` 命令、模型空间/毫米/正交简单闭环输入、项目策略与模式、控制区/门洞、房间意图、主次区、连接边、候选 `DecisionRecord`、取消/重选/重新计算边界和测试计划。
- 新增宿主无关 `OrthogonalDecisionInputSession`。它保留已验证房间、砖规格、明确策略/模式和房间语义；任何策略或语义修改都会清除陈旧候选记录，并只调用现有 `EngineeringOrthogonalDecisionCalculator` 重新计算。无 Autodesk 引用，也没有写回授权。
- `TILEORTHOUI` 现在先显示空面板，再以只读事务载入 4 条及以上模型空间 `LINE`；命令行循环允许明确设置策略、控制区、门洞、整房/主次区意图、主次矩形、连接边、候选记录、重选、重算、零写入预览请求、完成或取消。普通用户没有 `ConfirmedGridPhase` 或裸 X/Y 相位入口。
- PaletteSet 每次接收真实 `EngineeringOrthogonalDecisionResult`，按 DOR3 原始顺序显示项目策略、房间语义和候选选择 `DecisionRequirement`，保留候选状态、原始理由、诊断和指标。应用的 `DecisionRecord` 额外显示候选 ID、策略版本、模式、例外标志、原因和“非自动合规”。
- 只有 DOR3 唯一 `AutomaticUsable` 候选，或匹配策略版本且已应用的 `DecisionRecord`，才能请求零写入预览。研究和受控生产人工决定都不显示为自动合规；仅切换 Palette 候选列表不产生记录或预览许可。
- 新增 7 项 `OrthogonalDecisionInputSessionTests`，覆盖合法唯一自动候选、项目策略缺失、逐项房间语义缺失、多候选与原始顺序、研究/生产记录、无效边界、取消、重选、重新计算以及诊断/指标稳定。既有 201 项全部保留，最终 Debug/Release 自动测试均为 208/208。
- 完整解决方案最终在 `build/dor5-final/Debug` 和 `build/dor5-final/Release` 隔离目录成功重建。Debug/Release 输出均未复制 Autodesk Managed DLL；核心项目仍无 Autodesk 引用。正式插件/核心 FileVersion 和 ProductVersion 均保持 `0.1.0.0`。
- 最终 Debug 插件为 74,752 字节、SHA-256 `41C95180E4139AEA909DEF82FDAE6EC92180814AA61424BD272A2EACE53BE8EC`；Debug 核心为 113,664 字节、SHA-256 `5C20CBC6BEECEF9EBB24C9F2219E30A88D0CBC790374CB622FCE705886597589`。Release 插件/核心 SHA-256 分别为 `C00D8A7BE34541F19A8E5CED94E05B6492A3694725166D1F468022722CF7C7ED`、`C592F7D8822D5093149684AE200E322468D18061251F3DDF6180AC991D2D70AA`。
- `dist/TileLayout-0.1.0.zip` 仍为 12,531 字节、SHA-256 `322077112229CA8E0EDFB0CEE0B1F3F192A24EAC2CC23D11DCEA413CC3431141`；本轮未修改版本、`dist`、发布物、标签、远端，未提交、推送或创建 PR。
- 2026-07-29，用户已在 AutoCAD 2021 连续执行 DOR 文档第 18.9 节功能清单并确认全部符合预期：L-01 唯一自动候选和零写入预览请求、L-04 D 策略/主次语义缺失、L-04 E 多候选及研究/受控生产 `DecisionRecord`、记录失效、重选、无效边界、各取消点和四个既有命令兼容冒烟均通过。测试副本关闭不保存后为 38,893 字节，前后 SHA-256 均为 `B96AC3F50390E98E4E4037304E8197C98859AB9855DF263FAD382D1D0B8701DF`；只读来源 fixture 仍为 `646A3A7A22CF40E5EC0B9CF8621A17AFAB09BB27928772C05D9CB3F4202DDA75`。DOR5 自动与宿主验收至此完成。
- DOR5 当前“Palette 只读观察器 + 命令行字母动作循环”是为逐项验证 DOR3 真实问题、状态失效、取消边界和零写入而搭建的阶段性工程交互，不是最终用户体验。下一独立任务应保持 DOR5 的显式语义、拒绝猜测、`DecisionRecord`、非自动合规和零写入契约，同时把 `TILEORTHOUI` 改造成在 Palette 内按任务顺序引导选择房间、策略、门洞、意图、主次区、连接边、候选和原因的可发现流程；普通用户不再记忆 `P/G/D/I/M/E/S/V/R/K/F/C`，也不手工换算 WCS 坐标。

## 43. DOR6 复杂房引导式 Palette 交互重构

- 2026-07-30 从包含 DR5、DR5-S、OR2、DOR2、DOR3、DOR4、DOR5 全部未提交源码、测试、文档和实机记录的当前 E 盘 working tree 继续；编码前重新执行 DOR5 Debug/Release 测试程序集，均为 208/208，实机工作副本仍为 38,893 字节、SHA-256 `B96AC3F50390E98E4E4037304E8197C98859AB9855DF263FAD382D1D0B8701DF`。
- 编码前已在 DOR 文档第 19.1～19.9 节冻结六步陌生用户流程、中文缺失项映射、候选分组、按钮禁用原因、AutoCAD 模态选择桥接、返回/修改/重选/取消边界、接口分层和十类测试计划。
- `TILEORTHOUI` 现在只打开“复杂房地砖排版向导”并立即把命令行控制权还给 AutoCAD。普通用户在 Palette 中完成房间与砖规格、项目规则、铺贴方式、图面控制信息、方案比较/人工原因和只读汇总；原命令行字母动作循环已移除，也没有另设普通或调试入口。
- 新增宿主无关 `OrthogonalDecisionGuidedWorkflow` 和 `OrthogonalDecisionGuidedText`，继续组合 DOR5 输入会话与真实 DOR3 Palette 会话。它们负责步骤、中文下一步、禁用原因、主次区原子提交、候选三分组、人工记录、失效说明和零写入许可，不复制 DOR2/DOR3 候选算法。
- WinForms Palette 只发出语义动作；Host 把“在图中选择”按钮排队到当前活动图纸的内部模态命令，并核对待处理动作和所属文档。内部桥接只读取边界或捕捉几何点，不接收裸坐标参数，不能脱离面板独立推进。
- 项目规则、模式、房间意图、控制区、门洞、主次区或连接边变化会立即清除旧人工记录和预览许可，并显示具体原因。候选保持 DOR3 原始顺序，按“自动推荐”“需要人工确认”“不可使用”展示；研究和受控生产人工记录都明确显示“非自动合规”。
- 新增 10 项引导会话自动测试，既有 208 项回归全部保留。完整解决方案已在 `build/dor6-final/Debug` 和 `build/dor6-final/Release` 隔离目录成功重建，两套完整 MSTest 均为 218/218。
- Debug 插件为 105,984 字节、SHA-256 `A884FB113A8BC9807ED7D913DECFE5260866B1311D1DFE33232DB003384DA6D5`；Debug 核心为 113,664 字节、SHA-256 `5C20CBC6BEECEF9EBB24C9F2219E30A88D0CBC790374CB622FCE705886597589`。Release 插件为 99,328 字节、SHA-256 `58AF050178E44E66F7C5959045E850C4EADEB0E35A67B100F7B5DF8C133D8A3A`；Release 核心为 105,984 字节、SHA-256 `C592F7D8822D5093149684AE200E322468D18061251F3DDF6180AC991D2D70AA`。
- 两个 DOR6 输出目录均未复制 `AcCoreMgd.dll`、`AcDbMgd.dll` 或 `AcMgd.dll`；Debug Core 的程序集引用只有 `mscorlib`、`System`、`System.Core`。插件与核心 AssemblyVersion/FileVersion/ProductVersion 均保持 `0.1.0.0`。
- DOR6 命令及桥接区静态扫描没有 `OpenMode.ForWrite`、`AppendEntity`、`AddNewlyCreatedDBObject`、图层写入或撤销标记调用；会话测试确认选择、修改、返回、重选、取消、记录、完成和预览请求始终没有写回授权。四个既有命令未改流程。
- `dist/TileLayout-0.1.0.zip` 仍为 12,531 字节、SHA-256 `322077112229CA8E0EDFB0CEE0B1F3F192A24EAC2CC23D11DCEA413CC3431141`；版本、`dist`、发布物、标签和远端未修改，也未提交、推送或创建 PR。
- AutoCAD 2021 引导式 Palette 实机验收已于 2026-07-30 完成。第 19.12 节一次性真实用户语言清单中的自动推荐、缺失策略、主次语义、多候选人工记录、失效、取消、无效输入、跨图、整个任务取消、兼容和零写入项目均由用户确认符合预期；分阶段截图及最终文件哈希记录在第 19.13～19.21 节。
- 首次实机进入方案比较时，面板正确得到一个已淘汰候选，但顶部、候选详情和“下一步”仍错误提示补齐缺失项。第一修复版已让全部不可用状态直接展示真实最小切砖诊断和返回动作；用户复测确认三处提示一致，实际值为 `76 mm < 252 mm`。
- 用户随后确认门洞点取的是 L-01 最上方墙线的两个端点，控制区为整体包围框。程序因此把整段北墙解释成门洞，76 mm 淘汰结果符合输入语义；问题来自脱敏图缺少真实门边标记及原清单错误允许墙端点代替门洞。第二修复新增整段边界识别和中文提示，并把清单改为在北墙内部约四分之一/五分之三处点取；精确回归确认该点取恢复唯一自动候选。
- 新增 `AllUnavailableCandidatesExplainMinimumCutAndRecoveryAction` 和 `WholeWallEndpointsAreExplainedAndActualDoorPointsRecoverL01` 两项回归，当前 Debug/Release 完整测试均为 220/220。第二修复版在 `build/dor6-host-fix2` 隔离目录重建；Debug 插件为 110,080 字节、SHA-256 `943BD661FEB4C1A3967F91585FAE65F9CE2AB799213C571B27E23A8403F4CC48`，Release 插件为 102,912 字节、SHA-256 `0E0743F5BBC48E9D248C292ADCB0340AA91146784B132963BD7F1523254CDB5C`。输出未复制 Autodesk DLL，Core 哈希与 DOR6 初版一致；复测前须正常重启 AutoCAD 后加载新路径。
- 2026-07-30，用户按第二修复版复测路径在 AutoCAD 2021 中仅重选北墙内部的门洞两侧位置并提供“方案比较”截图：方案 1 位于“自动推荐”，是唯一可自动采用的方案；“需要人工确认”和“不可使用”列表为空，顶部显示“下一步：可以继续”，面板仍声明只读且不生成或修改图形对象。该证据确认 L-01 从图面选点到真实 DOR3 候选分类的恢复路径通过；加载哈希、其余第 19.12 节场景及关闭不保存后的 DWG 哈希仍须单独核对。
- 同日继续验证从左到右第二个合法房间时，用户完成主区、次区、连接边和门洞点取后得到 `InputUntrusted`，工程原因明确为所选连接边不匹配已验证的主次连接。原面板只显示泛化中文并要求阅读英文工程详情，且脱敏图没有绘制内部主次分界，陌生用户容易把可见的外轮廓短折边当成连接边。
- 第三修复版不推断或自动填写连接边，而是把该确定诊断映射为“不要选择房间外轮廓短折边”，并用用户已明确选择的两个矩形生成相对方位说明，例如“主区下边与次区上边重合的整段水平边”；顶部、连接边状态、候选说明、命令行点取提示和下一步保持一致。新增 `VisibleOuterStepIsExplainedAndSharedConnectionRecoversL04D` 回归，验证误选外轮廓短折边时明确说明、改选真实共享整段后恢复且始终零写入。
- 第三修复版完整解决方案已在 `build/dor6-host-fix3` 隔离重建，Debug/Release 自动测试均为 221/221。Debug 插件为 112,128 字节、SHA-256 `018ED1FFA9EF5F54AE5ACE01A6D0104AA277B3367A690C2DBA4312396FA290EE`；Release 插件为 104,960 字节、SHA-256 `26C64FF025F8CC3A75CD25874904E941A5BE9F8287308BE6B5D1FD101608459C`。Core 哈希不变，输出没有 Autodesk DLL；DOR5 工作副本和 `dist` 哈希仍保持原值。
- 用户重启 AutoCAD 2021、按第三修复版相对方位提示重新点取 L-04 D 连接边后提供方案比较截图：`InputUntrusted` 已消失；顶部直接说明项目规则尚未确认最小边砖尺寸并要求返回项目规则，方案 2 位于“需要人工确认（先补充项目规则）”，方案 1 位于“不可使用（已淘汰）”，自动推荐为空。命令行记录主区、次区、连接边和门洞每次选择后真实 DOR3 刷新且图纸没有变化。该证据确认连接边恢复路径和 L-04 D 项目策略缺失提示通过，尚不替代最终对象/图层计数及 DWG 哈希。
- 用户随后按面板完成 L-04 E 的研究模式、策略版本 `P-1`、第二绝对下限 200、主次区、连接边、门洞、候选选择和人工原因，并提供第 6/6 步汇总截图。汇总显示候选 4 的人工决定已记录，`DecisionRecord` 保存候选 `whole-door-default`、策略版本 `P-1`、研究模式、例外接受和原因“对比门口观感和边界砖后由项目负责人确认”，并明确标注“非自动合规”；顶部无未处理缺失项且“请求零写入预览”已启用。该证据确认人工原因到可审计记录链路通过，但尚未证明点击预览后的零写入或模式/策略变化后的记录失效。
- 用户按清单请求并取消零写入预览后，把项目模式从研究切换为受控生产并应用，再提供面板截图。红色失效提示明确显示“项目模式或策略已修改，旧的人工决定已失效”；候选选择重新成为待办，面板要求重新比较和填写原因，“取消预览请求”禁用并说明当前没有预览请求，图面外观未见新增对象。该证据确认模式变化立即清除旧 `DecisionRecord` 和预览请求；最终零写入仍以后续对象/图层计数和文件哈希为准。
- 用户随后在受控生产、`P-1` 下重新选择候选并记录原因。第一张截图顶部明确显示“受控生产人工决定已记录：非自动合规”，汇总保留 `P-1`、受控生产和已记录状态，零写入预览重新可请求；改为 `P-2` 后第二张截图显示红色“项目模式或策略已修改，旧的人工决定已失效”，候选重新待选且预览禁用。第三张截图确认期间误触发的 `PASTECLIP` 始终停在“指定插入点”并以 `*取消*` 结束，没有完成粘贴落图；最终仍须用文件哈希兜底确认。
- 用户在受控生产、`P-2` 下再次记录人工决定后发起门洞重选并按 Esc。第一张截图的命令行显示“已取消本次门洞选择；此前确认的门洞（如有）保持不变”，汇总仍保留候选 `whole-door-default`、`P-2`、受控生产和“非自动合规”，预览仍可请求。再次实际点取同墙内不同门洞边缘后，第二张截图以红色明确显示“控制门洞已修改，旧的人工决定已失效”，三个候选重新需要人工确认、预览禁用，命令行确认真实 DOR3 刷新且图纸未变化。该证据确认取消保持原值和房间语义变化失效两条边界。
- 用户最后一次性执行控制区、主区、次区和连接边取消，跨图恢复，斜边无效房间，取消整个任务，四个既有命令首次提示即取消，对象/图层前后核对、`U` 和关闭不保存，并反馈全部符合预期。`U` 的逐字命令行原文忘记留存，因此文档只记录结果符合，不补造文本。
- 关闭 AutoCAD 后，`work/dor6-test/dor5-working.dwg` 为 38,893 字节、SHA-256 `B96AC3F50390E98E4E4037304E8197C98859AB9855DF263FAD382D1D0B8701DF`，与测试前及 DOR5 受保护工作副本完全一致；只读 fixture 仍为 `646A3A7A22CF40E5EC0B9CF8621A17AFAB09BB27928772C05D9CB3F4202DDA75`。结合 221/221 双配置回归、零写入静态边界、用户对象/图层核对和最终哈希，DOR6 宿主验收完成。
- 第三修复版 Debug 插件保持 112,128 字节、SHA-256 `018ED1FFA9EF5F54AE5ACE01A6D0104AA277B3367A690C2DBA4312396FA290EE`，Release 插件保持 104,960 字节、SHA-256 `26C64FF025F8CC3A75CD25874904E941A5BE9F8287308BE6B5D1FD101608459C`；输出无 Autodesk DLL。`dist/TileLayout-0.1.0.zip` 仍为 12,531 字节、SHA-256 `322077112229CA8E0EDFB0CEE0B1F3F192A24EAC2CC23D11DCEA413CC3431141`。未修改版本、`dist`、标签或远端，也未提交、推送或创建 PR。

## 44. DOR7/DOR8 产品化路线交接

- DOR6 已安全收尾，后续不再把“增加更多实机步骤”作为默认推进方式。详细的未来顺序、自动关口和实机测试预算统一维护在 `docs/development-roadmap.md`；每阶段已实现能力的快速索引维护在 `docs/implementation-ledger.md`。
- DOR7 按交接路线解决两个直接产品风险：默认界面改为普通用户语言，内部类型只留在折叠的工程详情；宿主无关同源绘图计划同时驱动确定性 SVG/快照和 AutoCAD 零写入临时矢量预览。实现和自动质量门已完成。
- DOR7 不写入 DWG，不新增排版算法，不改变 DOR2/DOR3 契约或四个既有命令。编码期状态测试和可视快照已通过，当前只安排一次 AutoCAD 2021 集中验收，覆盖 L-01、L-04 D、L-04 E、临时预览清除、跨图恢复和零写入。
- DOR8 只有在 DOR7 的同源绘图计划和预览通过后才启动。正式写回必须复用同一计划，以专用图层、单个事务、失败回滚、一次撤销和不自动保存形成闭环；图层名、重复执行和覆盖策略需在 DOR8 编码前另行冻结。
- DOR7/DOR8 继续完整继承当前未提交 working tree；未经用户明确要求，不提交、推送、创建 PR、修改版本、`dist`、发布物、标签或远端。

## 45. DOR7 普通用户产品化交互与同源图面可视预览

- 2026-07-30 从包含 DR5、DR5-S、OR2、DOR2～DOR6 的当前未提交 E 盘 working tree 直接继续。编码前已在 DOR 主文档冻结普通用户四阶段流程、术语、界面状态、同源绘图计划、确定性快照、AutoCAD 临时预览生命周期、取消边界和测试计划；没有清理、覆盖、回退、提交、推送、创建 PR、修改版本、`dist`、发布物、标签或远端。
- `TILEORTHOUI` 已整理为“确定铺贴要求—在图中标明重点—比较排版方案—在图中预览并结束”四阶段。默认界面不显示 `RoomControlRegion`、`DecisionRequirement`、候选 ID 或 `DecisionRecord` 等内部术语；它们连同原始顺序、诊断和指标继续保留在默认折叠的“工程详情”。
- Core 新增宿主无关 `LayoutDrawingPlan`、唯一入口 `LayoutDrawingPlanBuilder.Build(EngineeringOrthogonalDecisionResult, candidateId)` 和确定性 `LayoutDrawingPlanSvgRenderer`。计划按真实候选原顺序保存房间轮廓、正式分格线、实际砖块、区域与接合边；没有 Autodesk 引用，也不重新计算排版。未来 DOR8 必须使用同一计划，不能绕过它建立第二套几何。
- L-01、L-04 D、L-04 E 的 SVG 快照分别为 5,893、4,628、4,967 字节，SHA-256 分别为 `D01CC93E862BC6B969B12F76AF1F4F35460B15C08D17FA8766EE739A22742768`、`D6ED634149FEC17977B397E79FE826010CF7CCB3CFD8418E2B670809C1AE6046`、`2110ECB01E8FF54B5B39454BF3E12B824140A025118067928DBA322DB72E0237`。
- AutoCAD 适配通过内部 `TILEORTHOUIPREVIEW` 命令读取同一计划并调用 `Editor.DrawVector`；支持显示、刷新、清除、输入失效、图面选择 Esc 恢复、返回修改、结束、整个任务取消和跨图清除/返回刷新。静态扫描中 Core Autodesk 引用数为 0，DOR7 预览路径的 `OpenMode.ForWrite`、`AppendEntity`、`AddNewlyCreatedDBObject`、图层写入和写事务命中数为 0。
- 完整解决方案已在 `build/dor7-validation/Debug` 和 `build/dor7-validation/Release` 成功构建，两套完整 MSTest 均为 229/229。Debug 插件为 122,368 字节、SHA-256 `CABFCB5F660F06DDD3CF3F439D3021A3CA2BE481DF45FFED13FAD6592EA85FD3`；Release 插件为 114,688 字节、SHA-256 `ADF96BB0BCDEA06C8ABB555EAAEBFD99F9E2F938186FA6EF82DDA0FAE16D2122`。两套输出均未复制 Autodesk DLL，程序集版本仍为 `0.1.0.0`。
- 自动门结束后，`work/dor6-test/dor5-working.dwg` 仍为 38,893 字节、SHA-256 `B96AC3F50390E98E4E4037304E8197C98859AB9855DF263FAD382D1D0B8701DF`；历史 `dist/TileLayout-0.1.0.zip` 仍为 12,531 字节、SHA-256 `322077112229CA8E0EDFB0CEE0B1F3F192A24EAC2CC23D11DCEA413CC3431141`。
- 当时状态为“自动质量门完成、AutoCAD 2021 集中实机待验”；后续集中清单、缺陷修复和末次定向复核均已完成，最终状态见第 49 节。
- 首次实机执行到 L-01 方案比较时，真实方案分类和普通用户指标正确，但自动推荐方案顶部仍错误提示填写人工原因，且人工原因区继续显示。该缺陷只在引导表达与控件显隐，不在 DOR2/DOR3 或绘图计划。
- 修复版按 `AutomaticUsable`、人工确认、缺项目规则和不可使用四类状态显示不同提示；自动方案明确可直接预览，人工原因区只在人工列表选中时显示，切换方案会清空未保存的旧原因。`build/dor7-host-fix1` Debug/Release 完整测试仍为 229/229；Debug 插件 SHA-256 为 `5570C7962AA2B692492E9800E6E374DAEB97F93A9FA8BF4FAD7AD313B92174DF`。集中实机从该修复版继续。

## 46. DOR7 L-01 东墙门洞冻结候选恢复

- 用户在 AutoCAD 2021 中保持 L-01 整体门洞影响范围，并把真实门洞两点选在最右侧东墙。截图显示唯一既有候选在西北凹口处形成 76 mm 独立窄带并被 252 mm 默认下限淘汰；没有出现已在 DOR1 冻结的“东侧 300 mm—中间整砖—西侧 576 mm”替代方案。该现象不是选点错误。
- 根因是矩形控制区先看到合法的 276 mm 自然余量，直到相位延伸并裁入完整 L 形后才得到 `276 - 200 = 76 mm`；旧实现只完成淘汰，没有在正交裁切后调用既有半砖/过渡砖分配并重新验证完整房间。
- 修复严格限定为整房单相位、门洞法向方案在正交裁切后触发既有硬下限的情形。原 76 mm 候选按 DOR3 原始顺序保留为不可使用；恢复候选使用 X 向 `576/600/600/300`，分格坐标为 `576/1176/1776`，凹口有效宽度 376 mm，完整房间下限复核通过后才进入自动推荐。没有猜测新房间语义、权重或项目策略。
- `EngineeringOrthogonalDecisionResult` 和 Palette 允许“原顺序中含淘汰候选，但仅有一个自动可用候选”直接进入自动预览；自动选中的是该唯一可用候选，不把淘汰候选隐藏或重新排序。同一候选已通过 `LayoutDrawingPlan` 生成 Debug/Release 一致的 `dor7-l01-east.svg`，5,850 字节、SHA-256 `6618900D6807053361D2DBCC786D237C9F97030FE8F52F9C19C7A538A733DE46`。
- 新增核心几何和产品状态两项回归；完整解决方案在 `build/dor7-host-fix2` Debug/Release 构建成功，两套完整 MSTest 均为 231/231。Debug 插件为 123,392 字节、SHA-256 `C20EC30260297F2316FD96A42B53DFD7F99370CBDDA15937CE4B06113AD97AFC`；Release 插件为 115,200 字节、SHA-256 `1578DB1DF578003B5F669559EFDCF575F03EE25003CC39B6E2486CE213E31AF2`。Core 无 Autodesk 引用，输出无 Autodesk DLL，DOR7 预览写入令牌为 0，版本仍为 `0.1.0.0`，`dist` 哈希未变。
- DOR7 测试 DWG 当前仍由 AutoCAD 占用，大小为 38,893 字节；关闭且选择“不保存”前不补记最终 SHA-256。东墙针对性实机复测通过后再继续原集中清单，DOR7 和 DOR8 状态不提前变更。

## 47. DOR7 L-01 北/南墙右半边门洞沿墙恢复

- 用户加载 `build/dor7-host-fix2` 后确认东墙门洞的临时预览与冻结算法一致，东墙修复实机通过。继续把局部门洞选择在北墙右半边和南墙右半边时，方案比较仍只显示 76 mm 淘汰候选，未生成第 6 节冻结的“西侧整砖、中间整砖、东侧 276 mm”方案。
- 根因是北/南门的门洞法向为 Y，但通用矩形规则还会按门洞沿墙偏向决定 X 向控制侧。门洞偏右时选择东侧控制，276 mm 余量落到西侧并被 200 mm 凹口削成 76 mm；第二修复只处理门洞法向重分配，没有处理沿墙控制侧在完整异形复核后需要切换的分支。
- 第三修复在门洞法向恢复失败后，才尝试把沿墙自然余量方案切换到另一确定控制侧并重新验证完整房间。北/南门右半边得到西→东 `600/600/600/276`、X=`600/1200/1800`；原方案 1 保留为不可使用，方案 2 是唯一自动推荐。
- 恢复条件收紧为：整房单相位、原候选因正交裁切触发硬下限、门洞确实位于控制区对应的完整房间真实边界段内，而且门洞两点不覆盖整段墙。L-04 的不可信东墙输入和 L-01 整段北墙误选继续阻止自动恢复；没有扩大房间语义或猜测策略。
- 新增北/南墙核心与产品状态两项回归，并生成两份同源 SVG：北墙右半边为 5,923 字节、SHA-256 `D316104B267E7241D7A48E0AD1ADECAC8E09D58A49BF1CDE4BD105CAB0E95284`；南墙右半边为 5,917 字节、`ADEBC1D3B9ED829888E85FCE5788FD98D0DDBA56BB4874294A16562BFBBAF536`。Debug/Release 文本逐字一致。
- `build/dor7-host-fix3` Debug/Release 完整解决方案构建成功，两套完整 MSTest 均为 233/233。Debug 插件为 123,392 字节、SHA-256 `1F19BEF28121DB80B74B9691415C7B96BCD6FF6E33E683119A5FCD6E52B7B3F9`；Release 插件为 115,712 字节、SHA-256 `2E62D7AD9F48639AD1EB5434799DB25987314B86DB25FD63937A5B2BD2016F4A`。Core Autodesk 引用、输出 Autodesk DLL 和预览写入令牌均为 0；版本及 `dist` 未变。
- 当前只需重启 AutoCAD 并加载第三修复版，分别复测北墙右半边、南墙右半边两个局部门洞。两者通过后继续原 DOR7 集中清单；最终 DWG 哈希仍待关闭不保存后读取。

## 48. DOR7 L-04 D 步骤 9～10 实机通过

- 用户执行集中清单步骤 9 的截图确认：项目规则未填写最小边砖宽度时，“自动推荐”为空，方案 2 位于“需要人工确认（先补充项目规则）”，方案 1 位于“不可使用”；顶部和待办区都明确要求返回第一阶段补充项目规则，“下一步”和预览保持禁用。
- 用户把最小边砖宽度设为 200 mm 后，方案 2 正确转为“需要人工确认”，方案 1 继续淘汰；未选择方案时原因区保持隐藏并提示先选择方案，没有提前生成记录或授权预览。
- 用户随后选择方案 2、填写并保存项目确认原因、进入第四阶段预览，并确认图面结果与算法预期一致。该场景仍属于人工确认，不代表自动合规；L-04 D 缺规则阻断、规则补齐、人工记录和同源预览链路判定通过。
- 截图命令行中的 `.sv$` 是 AutoCAD 自动保存临时文件，不是 DOR7 SVG，也不构成对当前测试 DWG 的正式写回。最终零写入仍待对象/图层前后计数和关闭不保存后的 SHA-256 共同确认。

## 49. DOR7 集中实机完成与方案比较交互补齐

- 用户已连续完成集中清单步骤 11～20，并确认全部符合当时文档预期；连同此前逐项通过的步骤 1～10，L-01、L-04 D、L-04 E、显示/刷新/清除、Esc 恢复、跨图、返回修改、结束、取消、既有命令冒烟和对象/图层零写入检查均已完成。
- AutoCAD 关闭且测试图选择“不保存”后，`work/dor7-test/dor7-working.dwg` 仍为 38,893 字节，SHA-256 `B96AC3F50390E98E4E4037304E8197C98859AB9855DF263FAD382D1D0B8701DF`，与 DOR6 保护基线一致。
- 实机同时暴露两项普通用户交互缺口：L-04 D 补齐 200 mm 后必须手动点唯一的方案 2 才知道如何继续；L-04 E 比较人工/淘汰方案时只能先读抽象指标，无法在确认前看图，且最窄边砖没有说明所在墙面或异形裁切位置。
- 修正后，缺失项消失且只有一个 `RequiresUserDecision` 候选时，Palette 自动聚焦该行并明确提示“先临时看图—再填写原因—保存人工确认记录”。自动聚焦只改变当前查看项，不创建 `DecisionRecord`。
- “比较排版方案”新增“临时查看所选方案”。真实自动或待人工确认候选可在记录前用既有 `LayoutDrawingPlan` 和 `DrawVector` 零写入查看；提示明确“仅用于比较，不代表确认采用或自动合规”。不可使用、输入不可信、能力不支持和仍缺项目规则的候选继续禁止预览，既有 `UnavailableOrUnknownCandidateCannotProducePlan` 边界不变。
- 普通方案概况新增西墙、东墙、南墙、北墙四侧计划边砖宽度；完整异形裁切后的最窄值若与外墙轴带一致则列出具体墙面，否则明确标为“异形转角或边界裁切处”，不猜测不存在的墙面归属。内部代码、诊断、指标和 DOR3 原始顺序仍只在工程详情。
- 新增 2 项回归，完整解决方案 Debug/Release 构建成功且 MSTest 均为 235/235；独立实机输出位于 `build/dor7-host-fix4`。Debug 插件为 126,464 字节、SHA-256 `FCFF2C18AB7493C250588FB92BB87CA566AF23FCB274479EE62E1FA3E0919BA4`；Release 插件为 118,272 字节、`03466FE59D2DF53472A67F81581A7AB3B733E7C41A33193A9B950943CA3856C0`。Core Debug/Release 哈希仍分别为 `35EDEA57BF4EEDDC50C58FFA0DB7268445A8234EBA80706A0E393B58FD87EB04`、`164846C6ABB8354F79B2992AD4A6DAD7F03EF089EC12532139EF2BC828274D2B`。
- Core Autodesk 引用、Debug/Release 输出 Autodesk DLL 和 DOR7 写入令牌均为 0；程序集版本仍为 `0.1.0.0`，历史 `dist` ZIP 仍为 12,531 字节、SHA-256 `322077112229CA8E0EDFB0CEE0B1F3F192A24EAC2CC23D11DCEA413CC3431141`。未提交、推送、创建 PR 或修改远端。
- 用户随后确认最小定向实机复核全部符合预期：自动聚焦、确认前临时看图、淘汰方案禁用、四侧/最窄位置说明和最终零写入保护均通过。DOR7 至此完成；DOR8 尚未启动。

## 50. DOR7-G1 通用复杂正交房间候选与项目规则重构

- 2026-08-01 从完整保留 DR5、DR5-S、OR2、DOR2～DOR7 未提交成果的当前 working tree 继续。编码前复核 DOR7 Debug/Release 235/235、AutoCAD 2021 集中实机及末次四点复核已经通过；受保护工作副本仍为 38,893 字节，SHA-256 `B96AC3F50390E98E4E4037304E8197C98859AB9855DF263FAD382D1D0B8701DF`。
- 用户确认冻结六项通用规则：推荐下限为各适用方向 `0.42T`；项目绝对下限按毫米真实参与比较；达到推荐值为自动满足；达到项目绝对下限但低于推荐值为待项目复核；低于项目绝对下限为硬淘汰；阈值等号通过。该规则统一作用于所有实际 `TileFootprint`，不再由 L-04/L-05 名称、候选来源或旧保留标志决定。
- 独立边界砖按其实际受切方向分别测量；转角砖保留 X/Y 两个测量值；连续异形砖作为一个实际 footprint 以名义包围尺寸参与项目规则，同时保留完整轮廓供局部诊断。整砖和非边界内部异形砖明确标为规则不适用。数量、连续性、位置、砖型差异和审美综合权重尚未冻结，因此不作为新增硬规则或唯一排序依据。
- `BoundaryCutMeasurement`、`TileFootprintAssessment`、`ProjectCutStatus` 把每块窄砖的轮廓、边界侧、实际尺寸、类型、阈值状态和原因带到核心输出；候选汇总同时报告低于项目绝对下限数量、低于推荐值待复核数量、是否缺项目规则及是否需要用户确认。
- 既有整房候选全部硬失败时，核心才从正交顶点余数、相邻余数间隙中点、推荐阈值接触点和项目阈值接触点生成其他全房相位。候选可解释、按相位去重；每轴最多 64 个、组合最多 1,024 个、最多保留 64 个通用非支配候选，任何截断均显式写入报告。筛选只使用冻结的硬规则和 Pareto 支配关系；不能客观唯一判定时保留多个候选、原始指标和原因，不伪造审美分数。
- 新增中性 N 矩形区域模型与共享边连接图，能确定性地把简单无洞正交房分解成无缝、无重叠、面积守恒的矩形区域；它不指定主区/次区、不自动建立相位重置，也没有接入现有命令。本阶段只为 DOR7-G2 和未来区域组合提供宿主无关接口边界。
- 用户提供的 `C:\Users\17521\Desktop\训练模型\复杂房间案例.dwg` 只作能力边界样例。只读提取得到 14 条组成简单无洞边界的模型空间 `LINE`、14 个顶点、5 个凹角，包围盒约 `11507.4981872049 × 6249.13458145653 mm`、面积 `60370338.4946013 mm²`；源文件为 32,881 字节，SHA-256 `C654BFB1A1D7C74B91DE97E5CCF80644DB7290DFC3BC52CA5E451A171C83FBDF`。另 4 条辅助线不属于边界；文件没有可恢复的门洞或控制矩形对象。
- 原会话的西墙门洞两点由用户随机点取且已经遗忘，所以 `183` 块、`44` 块边界非整砖、`3` 块连续异形砖、`7` 块窄砖和 `121.606 mm` 只保留为问题背景，不能作为精确夹具断言。固定测试改用上述 14 顶点和明确的确定性西墙测试门规则，并覆盖平移、镜像、旋转和输入顺序；生产逻辑没有样例名称或坐标特判。
- G1 初版新增 11 项泛化测试；首次实机缺陷修复再新增 2 项，连同既有 235 项，Debug/Release 完整解决方案均为 248/248。覆盖阈值等号、100 mm 与 121.606 mm 真实比较、全部砖块分类、覆盖无缝无重叠无越界、候选确定性/上限/截断、区域图、平移/镜像/旋转/输入顺序、性能冒烟，以及所有通用相位具备双轴计划并可逐项生成 Palette 友好概况。修复版 Debug Core 为 153,088 字节、SHA-256 `F31F2355A87A2BCF32C266D29C94F082D64AFAE4C3ACF3E48F061ECD2AA8D5A0`；Release Core 为 143,360 字节、`726029BC2A23A9D5DE0428637CA79ED232FFDEA8CA0A581C19DC14B6D5C90144`。
- Core Autodesk 引用、核心写入令牌和 Debug/Release 输出中的 Autodesk DLL 数均为 0。修复版 Debug/Release 插件哈希分别为 `9C5DCAA026D89905A35C861AEA5EB9F19039115F52F5A63902961537DD5E0084`、`ABE6657E1E6EB7C0C34F2616BEC697254B3F22E1D6A9D9A6A00A33851EAAAC7D`；版本保持 `0.1.0.0`，历史 `dist` ZIP 哈希保持 `322077112229CA8E0EDFB0CEE0B1F3F192A24EAC2CC23D11DCEA413CC3431141`。
- DOR7-G1 没有修改 `TILE600`、`TILELAYOUT`、`TILEORTHO`、`TILEDOORRECT` 的既有流程，没有启动 DOR7-G2 UI 重构或 DOR8 写回，也没有提交、推送、创建 PR、修改版本、`dist`、发布物、标签或远端。首次步骤 3 的 JIT 缺陷修复后，用户确认所有可见候选均可切换、方案 947 可正常零写入预览且无弹窗，并确认原步骤 4～8 全部符合预期；样例源 DWG 和保护 DWG 哈希仍分别为 `C654BFB1A1D7C74B91DE97E5CCF80644DB7290DFC3BC52CA5E451A171C83FBDF`、`B96AC3F50390E98E4E4037304E8197C98859AB9855DF263FAD382D1D0B8701DF`。本轮随机门洞精确 WCS 未在反馈中逐字留存，不把截图反推为坐标；它只作为宿主兼容证据，确定性几何继续由自动夹具承担。DOR7-G1 至此完成。

## 51. DOR7-G2 项目规则确认、多候选比较与窄砖诊断预览（核心产品复核通过）

- 2026-08-01 从完整保留 DR5、DR5-S、OR2、DOR2～DOR7、DOR7-G1 及首次 JIT 修复的当前 working tree 串行实施。用户冻结除“硬淘汰候选进入只读图面诊断”外的 G2 规则；开发阶段硬淘汰只允许文字审计，继续禁止预览、确认和未来写回。
- G2 没有另做视觉换皮，明确沿用 DOR7 四阶段产品外壳；实际 UI/交互增量集中在第一、三阶段：推荐下限 X/Y 毫米确认、项目规则版本、绝对下限明确数值/未确认门，三类可见候选分组，硬淘汰按原因筛选和每页 50 条文字审计，当前候选工程详情，以及窄砖清单、中性区域和共享边图面开关。G1 Debug 插件 `9C5DCAA0…` 不含这些控件文本，G2 Debug 插件 `CBED9CC1…` 含这些控件；实机截图也显示其已加载，不能把“外壳未重设计”记成“G2 未接入 UI/交互”。
- 复杂夹具在 100 mm 项目绝对下限下保持 1,009 个原始候选，其中待复核 12、硬淘汰 997；X/Y 相位 42/36、组合和生成替代 1,008、去重 0、支配淘汰 179、保留 12，组合上限和总体截断显式显示。997 个硬淘汰不从核心结果删除，但默认折叠且共 20 页，不再一次平铺。
- 同一 `LayoutDrawingPlan` 直接携带 G1 的稳定砖号、真实轮廓、尺寸、适用轴、边界侧、独立/连续异形类型、`BoundaryCutMeasurement` 和原因；规则缺失用黄色、待复核用橙色、当前窄砖选择用红色加强。规则缺失候选可只读看图，硬淘汰在 Palette 和 Builder 两层均禁止图面诊断。中性区域只显示 `region-N`、范围、面积和共享边，不推断主区、次区、重要区或相位重置。
- 用户在 AutoCAD 2021 完成第 30.9 节集中清单并确认所有步骤符合预期。第 7 步曾因“勾选图面显示”措辞被误认为另有按钮；截图确认实际控件是“在图中显示中性区域和共享边”复选框且已勾选，区域 5 个、共享边 4 条和零写入刷新均正确，文档已改为控件全名。用户同时指出整体界面观感和主流程没有再次改版；该现象符合冻结的四阶段外壳，G2 验收对象是上述新增控件与状态机，不宣称完成额外视觉设计。
- 2026-08-01 末次重跑 Debug/Release 完整解决方案均为 0 警告、0 错误，两套 MSTest 均为 253/253。Debug 插件/Core 分别为 144,896/156,160 字节，SHA-256 `CBED9CC15B2F2B4330B6D2E31826EC943C3D3634DA12F488410BE70EE4DA6285`、`E7596D004D12B51B77685F363D525E264DA1FB941A7E0A3DFAC86444A18D97F7`；Release 插件/Core 分别为 136,192/146,432 字节，SHA-256 `D1025FDED3DAF301BACD988A1F7D26BCCA6EF223EAC0AD1DB9CB469C750F0266`、`1550648D18913E1467197F5A5C690453EFEC4AA960DFCB4F947B16BC5791BF0B`。
- Core Autodesk 引用、G2 路径写入令牌、Debug/Release 输出 Autodesk DLL 均为 0。G2 工作副本和样例源 SHA-256 均保持 `C654BFB1A1D7C74B91DE97E5CCF80644DB7290DFC3BC52CA5E451A171C83FBDF`，保护 DOR7 DWG 保持 `B96AC3F50390E98E4E4037304E8197C98859AB9855DF263FAD382D1D0B8701DF`，历史 `dist` 保持 `322077112229CA8E0EDFB0CEE0B1F3F192A24EAC2CC23D11DCEA413CC3431141`。版本仍为 `0.1.0.0`；未提交、推送、创建 PR、修改版本、`dist`、发布物、标签或远端。
- 上述集中清单证明第一轮 G2 实现没有 JIT、统计错误或 DWG 写入回归，但不能代替普通用户产品验收。用户随后明确指出：双矩形主要区/相邻区/接合边模型无法表达本次多凹凸房间且把内部拓扑责任推给普通用户；门洞影响矩形也不应由用户建立；“项目执行/方案研究”和 `P-1` 没有清晰用户价值；项目规则字段、嵌套滚动、信息密度、底部按钮裁切、唯一待确认方案入口、普通方案原因门和逐步解决问题的路径仍未完成。
- 源码审计确认这些缺口存在：`NeutralOrthogonalRegionPartition` 当前只读展示，候选决策仍把 `ControlRegion`、`LayoutIntent.MainSecondary`、`MainSecondaryRegionDefinition` 和 `SelectedConnectionEdge` 作为显式输入；Palette 仍创建上述工程控件和默认 `P-1`。因此 G2 产品验收重新打开，前述 253/253 与实机结果只记录为“当前实现技术门通过”，不能写成 G2 完成。DOR8 正式写回仍未启动。
- 产品修正版已删除普通界面中的模式、规则版本、手工控制矩形、主次区和接合边入口。房间规则保存后内部固定使用受控生产和审计版本 `G2-AUTO-RULE-1`，门洞两点在完整正交房真实外边界上验证，再由 `NeutralOrthogonalRegionPartition` 唯一找出门洞邻接矩形；内部共享边不属于门洞外墙，所有自动区域默认共享同一全房相位。
- `TILEORTHOUI` 宿主已由 `PaletteSet` 改为可缩放、非模态、标准 WinForms 浮动对话框，四页为“房间与规则 / 门洞 / 选择方案 / 图面核对”。候选组和淘汰审计使用独立页签，不再纵向平铺；窄砖与中性区域图面开关集中到第 4 页，固定等宽底部按钮不再随窄面板换行裁切。
- 完全合规候选只需明确选择，不要求项目原因；只有 `RequiresUserDecision` 才要求可审计复核原因。规则缺失仍只读诊断，硬淘汰仍禁止图面诊断、确认和写回。新增 5 项回归后，Debug/Release 均为 258/258；Core Autodesk 引用、G2 写入令牌和两套输出 Autodesk DLL 均为 0。
- 产品修正版 Debug 插件/Core 为 154,112/156,160 字节，SHA-256 `9C2BFB5D875FB40EE786B882A8E94BF64662CEB6716C0E9B249F1D12BA5E2CBC`、`E7596D004D12B51B77685F363D525E264DA1FB941A7E0A3DFAC86444A18D97F7`；Release 插件/Core 为 144,384/146,432 字节，SHA-256 `31994A909615BA729336353E3FA88C7FA3577AD1EDDFDA1787EA7088CF1B4F94`、`1550648D18913E1467197F5A5C690453EFEC4AA960DFCB4F947B16BC5791BF0B`。程序集版本仍为 `0.1.0.0`，未修改 `dist`、发布物、标签或远端，也未提交、推送或创建 PR。

## 52. DOR7-G2 浮动窗口与图面选择易用性修正

- 用户确认第二次集中 AutoCAD 2021 产品复核 11 项全部符合预期；G2 的自动拓扑、项目规则、候选审计、同源诊断、取消/失效及静态零写入边界通过宿主验证。反馈仅集中在窗口尺寸、DPI/内容裁切和图面选择遮挡。
- 房间边界与门洞选择现在由宿主在发送内部安全命令前隐藏主窗口，并在成功、Esc、输入错误或桥接失败后恢复原位置和尺寸。门洞重选时既有预览的暂停/恢复与旧值保留语义不变。
- 浮动窗默认从 860 × 780 调整为 760 × 680，最小 640 × 560；Form/UserControl 使用 DPI 自动缩放和系统消息字体。顶部删除重复标题/长说明，待办框压缩；步骤页使用单一页面滚动，底部六按钮继续固定可见。
- “图面核对”把“窄砖诊断”和“房间结构（只读）”拆为页签；图面选项保持直接可见。新增“专注查看图面”，把主窗口收为右上角 370 × 120 小控制条，点击“返回排版窗口”恢复此前边界。
- 标准 Debug/Release 完整解决方案均为 0 警告、0 错误，两套 MSTest 均为 258/258。Debug 插件/Core 为 157,696/156,160 字节，SHA-256 `52A7D7DA852628F0EE80D0985C096A934EFDD01356169DBC6F927897918FDD29`、`E7596D004D12B51B77685F363D525E264DA1FB941A7E0A3DFAC86444A18D97F7`；Release 插件/Core 为 147,968/146,432 字节，SHA-256 `B21A2489DC3E0E071A07A7A1440FA6F859F41C13DB27C295D13CD996EFD04399`、`1550648D18913E1467197F5A5C690453EFEC4AA960DFCB4F947B16BC5791BF0B`。
- G2 UI/内部命令写入令牌、Core Autodesk 引用、两套输出 Autodesk DLL 数均为 0；工作副本/样例源和保护 DOR7 DWG 哈希未变。版本仍为 `0.1.0.0`，`dist`、发布物、标签、远端和 Git 提交状态未改变。UI 修正版只待短定向宿主复测，DOR8 未启动。
- 新版浮动对话框、门洞自动归属、固定底部按钮和第 4 页图面开关仍需一次集中 AutoCAD 2021 产品复核；完成前 G2 不标记最终完成，DOR8 不启动。

## 53. DOR7-G2 门洞跨内部自动分区线修复

- UI 修正版实机中，用户在复杂房西墙选择正确门洞仍收到“没有找到完整容纳门洞的自动邻接区域”。截图坐标和确定性复杂夹具确认：门洞沿同一真实西外墙，却跨过中性区域内部水平分界；旧适配器强制要求单个中性矩形完整包含两个端点，误把算法内部拓扑线当成物理限制。该错误不是用户选点造成。
- `DoorOpeningPointAdapter` 仍先验证两个端点属于同一段真实房间外边界；单区域无法容纳时，改为收集同一外墙侧、与门洞相交的中性区域，并要求其沿墙方向无缝连续覆盖整个门洞。转换使用这些区域共同的室内控制范围，不改变真实外轮廓、G1 分区或候选计算。
- 内部共享边仍不能作为门洞墙；跨真实凹角、落在不同真实外墙或邻接区域覆盖存在空隙时仍拒绝。新增回归 `ProjectToOrthogonalRoomWall_DoorAcrossAutomaticPartitionUsesCommonInteriorRectangle`，直接覆盖西墙门洞 `Y 3218.4086～3818.4086` 跨内部 `Y 3275.3494` 分界的场景。
- 标准 Debug/Release 完整解决方案均为 0 警告、0 错误，两套 MSTest 均为 259/259。Debug 插件/Core 为 161,280/156,160 字节，SHA-256 `E42C72D58F607E90AFE68B0A2582020E457B013CD3842D0A03BBE75D57519CD0`、`E7596D004D12B51B77685F363D525E264DA1FB941A7E0A3DFAC86444A18D97F7`；Release 插件/Core 为 151,552/146,432 字节，SHA-256 `FA5C114E4EE270593EAEBC924495FC7F32549822A58C0231B760AE6D94323B89`、`1550648D18913E1467197F5A5C690453EFEC4AA960DFCB4F947B16BC5791BF0B`。
- 工作副本/样例源 SHA-256 保持 `C654BFB1A1D7C74B91DE97E5CCF80644DB7290DFC3BC52CA5E451A171C83FBDF`，保护 DOR7 DWG 保持 `B96AC3F50390E98E4E4037304E8197C98859AB9855DF263FAD382D1D0B8701DF`。程序集版本仍为 `0.1.0.0`，未修改 `dist`、发布物、标签、远端或 Git 提交状态；DOR8 未启动。
- 当前只待用最终 Debug DLL 重测同一门洞并关闭不保存。通过前不宣称 G2 最终完成。

## 54. DOR7-G2 候选页缩放安全布局修正

- 用户实机确认：浮动窗口缩小时，第 3 页的候选列表可能被压缩到只剩一条线，方案详情和项目复核区域也可能显示不全。原因是候选页第一行使用百分比空间，而列表、详情和原因框没有可读最低高度；窗口缩放时它们会相互挤压。
- `OrthogonalDecisionPaletteControl` 现在为候选列表设置 128 px 最低高度，为候选详情设置 118 px 最低高度，为项目复核原因设置 64 px 最低高度，并对候选页启用滚动。窗口仍可缩放；当当前高度不足时，用户滚动候选页即可查看全部列表、详情和操作，不会删除或隐藏候选。
- 候选原始顺序、分组、筛选、分页、诊断、项目确认条件和 DOR8 零写入边界均未改变；第 3 页只增加布局保护。既有四个命令流程不变。
- 标准 Debug/Release 完整解决方案均为 0 警告、0 错误，两套 MSTest 均为 259/259。Debug 插件/Core 为 161,280/156,160 字节，SHA-256 `576B1A82198619C62E8762899AF56A2FC6277479679236E7E59E95394447B9BD`、`E7596D004D12B51B77685F363D525E264DA1FB941A7E0A3DFAC86444A18D97F7`；Release 插件/Core 为 151,552/146,432 字节，SHA-256 `7936A89240A09A655A8E01ACFDE8508430524576F15B636C213FD7CED47F6587`、`1550648D18913E1467197F5A5C690453EFEC4AA960DFCB4F947B16BC5791BF0B`。
- Core Autodesk 引用、G2 静态写入令牌和输出 Autodesk DLL 均为 0。样例源/工作副本与保护 DOR7 DWG 哈希保持冻结值，版本、`dist`、发布物、标签、远端和 Git 提交状态未改变；DOR8 未启动。
- 当前只需用最终 Debug DLL 将窗口拖到允许的较小尺寸，进入第 3 页切换“满足规则/待项目复核/规则缺失”三组，确认列表仍有多行、详情可滚动、复核原因可编辑；再关闭工作副本并选择“不保存”。

## 55. DOR7-G2 候选页固定操作栏与单一滚动视口

- 用户实机再次发现“临时查看所选方案”被候选详情区域遮挡。根因是候选页把列表、详情、原因框和操作行放在同一个可滚动表格中；空间不足时表格内部的百分比行与固定行互相挤压，操作行没有独立的可见性保证。
- 可保留方案页现在采用“固定页面操作栏 + 单一滚动视口”：候选列表、详情和项目复核原因位于滚动内容；“临时查看所选方案”和“保存人工确认记录”固定在页面底部操作栏；底部全局导航仍由外层窗口固定。滚动内容宽度随视口同步，避免文本框退化为零宽或被横向裁切。
- 候选事实、原始顺序、分组/筛选/分页、项目原因门、预览和零写入逻辑均未改变。用户可在小窗口中滚动内容，但关键操作不随滚动消失。
- 本地 WinForms 布局检查覆盖 640 × 560、760 × 680、1024 × 768，候选详情/原因框与操作按钮无重叠。标准 Debug/Release 完整解决方案均为 0 警告、0 错误，两套 MSTest 均为 259/259。
- Debug 插件/Core 为 162,304/156,160 字节，SHA-256 `69E99854AFA0E8FD3DC33B6D0340CA23857122AC74B3F6F0AAFB9079C7434B8E`、`E7596D004D12B51B77685F363D525E264DA1FB941A7E0A3DFAC86444A18D97F7`；Release 插件/Core 为 152,064/146,432 字节，SHA-256 `4B2B45B0D7213094DC646C71338D62D4D00F241FF54FC6156B6448EB3055CD44`、`1550648D18913E1467197F5A5C690453EFEC4AA960DFCB4F947B16BC5791BF0B`。
- Core Autodesk 引用、UI 写入令牌和输出 Autodesk DLL 均为 0；两个受保护 DWG 哈希、版本、`dist`、发布物、标签、远端和 Git 提交状态未改变。DOR8 未启动。

## 56. DOR7-G2 规则收口与 DOR7-G3 启动边界

- 用户确认 G2 继续不考虑墙砖对缝；后续目标是墙体平面阴角/阳角与地砖分格缝对齐。用户进一步明确：无法同时照顾所有角点时，优先房间中部的实际凹凸转角，不使用门洞附近角点作为更高优先级。
- G3 将把双向网格交点重合定义为最高等级但非硬条件，把一条地砖缝准确经过角点定义为主要优化目标；仅接近角点的结果只作诊断。计算沿用 Core 现有坐标公差，不把毫米级近似扩张为“已对齐”。
- G3 只扩展候选质量与相位候选来源：为实际凹凸角点生成可审计的墙角锚定相位，保留无法客观区分的候选及原始指标；继续沿用 G1 硬规则、推荐/绝对下限和既有窄砖事实，不增加未经确认的审美总分。G2、四个既有命令、同源零写入预览和 DOR8 暂缓边界保持不变。
- 本节只完成规则与任务切换记录，不代表 G3 已编码或已通过测试；下一任务从 G3-A 规则/数据契约冻结开始。

## 57. DOR7-G3-A 墙角—地砖缝数据契约冻结

- G3-A 复核确认 G1 已把全部正交边界顶点的 X/Y 余数列为最高优先级相位来源；当前缺口不是缺少数值枚举，而是缺少墙角类型、实际命中、来源合并、质量指标和截断审计。冻结复杂夹具在 100 mm 项目绝对下限下仍为 1,009 个原始候选、12 个可保留候选；只读观测中 6 个可保留候选未命中实际凹角，2 个命中 1 个，4 个命中 2 个，其中 2 个包含双向交点。
- 只有规范化逆时针房间轮廓的 270° 反射角作为优化目标。90° 角保留稳定编号、坐标和最近距离诊断，但不计入候选择优。命中必须由候选实际裁切后的 `DivisionLines` 证明，线段须从角点向房间内部延伸大于坐标公差；无限网格延长线或只与墙边重合不算。
- 候选先执行 G1 项目绝对下限硬淘汰，再保持“满足规则、待项目复核、规则缺失”状态边界。只在同状态组的 Pareto 比较中分别最大化双向网格交点命中数和至少一条分格缝准确命中的目标角数；两项作为独立原始指标，不计算审美总分，存在取舍时保留多个候选。
- 启用“墙角对缝优先”且房间存在目标反射角时，运行墙角锚定分支，不等待旧门控候选全部硬失败。生成优先组为同一角点双轴锚定、单轴锚定、其余 G1 来源；继续使用每轴 64、二维组合 1,024、通用非支配保留 64 的上限并显式报告截断。同一实际相位只生成一个候选，但合并保存全部相位来源和目标角点；关闭开关时不运行该分支。
- 准确命中继续使用 `GeometryTolerance.Coordinate = 1e-6`；不增加“接近”阈值或普通用户字段。未命中只保存最近 X/Y 分格缝距离和原因，用于诊断而不参与筛选。
- G3 数据契约将逐角保存稳定 ID、坐标、几何类型、是否优化目标、实际命中轴、双向交点、最近距离和原因；候选保存聚合原始指标及结构化相位来源；`LayoutDrawingPlan` 只投影同源事实供零写入图面诊断。四个既有命令和 G2 用户流程不变，DOR8 未启动。

## 58. DOR7-G3 首轮实现与自动质量门

- 新增 `WallCornerAssessment`/`WallCornerEvaluator`，对规范化轮廓生成稳定角点 ID，只把 270° 反射角作为优化目标。命中仅由候选实际裁切分格线证明；90° 角和未命中目标保留原始最近 X/Y 缝距离。
- 非矩形房的有界相位搜索优先同角双轴锚定，再组合单轴锚定和既有 G1 来源。相同实际相位合并所有来源；每轴 64、组合 1,024、非支配保留 64 的上限不变。
- G1 硬规则和项目状态分组仍先行；只在同组 Pareto 中增加“双向交点命中数”与“至少单轴命中数”两个独立最大化维度。没有权重、总分或跨组升级，存在取舍的候选继续并列保留。
- 复杂夹具仍产生 1,009 个有界原始候选；G3 保留 14 个非支配取舍候选。目标反射角 5 个，X/Y 锚定相位 5/3，同角双轴/单轴优先组合 5/286，合并来源 9；性能回归约 1.6 秒，10 秒安全门通过。
- `LayoutDrawingPlan` 原样投影墙角事实，确定性 SVG、Palette 文字和 AutoCAD `Editor.DrawVector` 临时标记同源。图面可选显示灰色 90° 只读角、蓝色双向交点、青色单缝命中和黄色未命中目标；关闭显示不改变计划或候选。
- Debug/Release 完整解决方案均 0 警告、0 错误，MSTest 均 268/268。Debug 插件/Core 为 166,400/172,032 字节，SHA-256 `3E0BEED6EADD095827A6B5B74389F87273FAC4AE7CA5B3E6614EBA598016755C`、`1E2A1EDD078ED79BE1548F1BCD6E78CF688D5176393CA13ADC9654A7E9848EA1`；Release 为 156,672/161,792 字节，`BC92CE8A31097F8B32B372D32B5A9AD2216AF57C351706AC7D888D8AC788C8C3`、`449B516E16DD4911AB06FA8829A9A03CCF9C0A9D5C12316B96917D5BBCB25FF2`。
- Core 参考仍只有 `mscorlib/System.Core/System`，G3 UI/临时预览写入令牌和 Debug/Release 输出 Autodesk DLL 均为 0。样例源/G2 副本、保护 DOR7 DWG 及历史 `dist` ZIP 哈希不变；版本仍为 `0.1.0.0`。四个既有命令流程不变，DOR8 未启动。

## 当前补充：G3 近似正交边界输入修复

- 针对实机图纸中“看似水平/竖直但存在微小方向偏差”的边界，`TILEORTHOUI` 增加只读近似正交诊断与归一化入口。严格 WCS 验证仍保留；只有方向偏差不超过 `0.05°`、最大端点修正不超过 `3 mm`、端点连接容差不超过 `3 mm` 时，才使用临时正交计算副本。
- 归一化只消费于当前向导的房间验证、候选计算、门洞投影和同源零写入预览；原始 LINE 不修改，`TILE600`、`TILELAYOUT`、`TILEORTHO`、`TILEDOORRECT` 既有流程不变，超限输入仍拒绝。普通用户只收到简短提醒，工程详情可逐条查看最近轴、角度偏差、端点修正和通过/超限结果。
- Debug/Release 核心测试均为 282/282，Core 与 AutoCAD 适配隔离构建均 0 警告、0 错误。DOR8、版本、dist、发布物、标签、远端和既有哈希均未改变；AutoCAD 2021 集中只读复核仍是后续关口。

## 59. DOR7-G3 可选“墙角对缝优先”

- 新增默认关闭的“墙角对缝优先（可选）”复选框。关闭时沿用 G1 基础候选生成与原始顺序；既有门洞候选全部硬失败时，仍运行有上限的 G1 X/Y 基础相位搜索（含半砖/过渡砖边界来源），但不运行墙角锚定优先或墙角排序；已有候选仍可显示墙角只读诊断。勾选时才运行有上限的墙角锚定分支，并对可保留候选做稳定的推荐排序，不删除或隐藏 G1 原始事实。
- 排序先保持 G1 状态组边界，再依次减少入口第一视觉范围内低于推荐下限的窄砖、优先 270° 墙角安全对缝、减少入口视觉盲区窄砖，最后沿用既有边界指标和原始生成序号。低于项目绝对下限继续硬淘汰，规则缺失/待项目复核不被升级，不计算审美总分。
- 每个目标墙角分别检查竖缝两侧砖宽和横缝两侧砖长；两侧均达到 `2/3T`（等号通过）才计安全双向/单缝，否则不奖励。跨度、安全状态和原因继续由 `TileAssessments`、`WallCornerAssessment`、`LayoutDrawingPlan` 同源展示，临时预览仍为 AutoCAD 零写入。
- Debug/Release 全量 MSTest 均为 282/282；Core 与 AutoCAD 适配 Debug/Release 隔离构建均 0 警告、0 错误。DOR8、四个既有命令、版本、`dist` 和远端均未改变；待一次 AutoCAD 2021 集中只读复核。

### 59.1 G3 开关行为修复

- 现状复核确认：原实现无论复选框是否勾选都运行墙角锚定相位分支，关闭时只取消排序，复杂房间因此看不到实质差异。
- 修复后关闭路径保留 G1 候选和原始顺序；只有基础候选全部硬失败时才运行 G1 有上限 X/Y 相位搜索，并尝试两轴“对向边半砖—中间整砖—门侧过渡砖”，随后按实际 footprint 复核绝对下限。勾选后才额外运行墙角锚定搜索并按入口窄砖、2/3 安全对缝、入口盲区窄砖和既有指标稳定排序。若复杂凹凸使重分配仍低于绝对下限，候选继续硬淘汰，不伪造可保留预览；切换会使旧预览与人工确认失效。
- 新增回归覆盖关闭→开启→关闭的候选搜索状态、G1 重分配来源、搜索报告和原始顺序恢复；当前自动测试在本轮完成后重新统计。
## G3 关闭墙角优先时的诊断边界补充

关闭“墙角对缝优先”只保留 G1 候选生成与原始顺序。`WallCornerAssessment`/墙角命中数仍随实际分格线计算并显示，但仅作只读事实，不反向改变垂直于门洞方向的相位、边界分配或候选排序；关闭路径不得出现 `TargetCornerAnchor` 来源。

### G3 复杂凹口边砖摘要修正

### G3 完整外包络窄余量与关闭状态普通界面修正

- 当门洞控制区的自然余量掩盖完整房间外包络的窄余量时，G1 恢复候选现在用完整房间实际宽度/高度重建该轴的半砖—中间整砖—过渡砖分配，并用同源计划生成预览；该路径不读取或奖励墙角命中。
- 关闭“墙角对缝优先”时，普通候选标题、状态、搜索统计和概况不再出现墙角对缝质量文字；角点原始事实仍仅在工程详情/显式只读诊断中保留。开启后才显示质量摘要和排序信息。
- Debug/Release MSTest 均为 288/288，AutoCAD 适配隔离构建 0 警告、0 错误；四个既有命令、零写入预览、DOR8、版本和 dist 均未改变。

- 旧摘要直接把门洞控制区的 `BoundaryBandPlan` 低/高端翻译成“西墙/东墙”，复杂凹口或控制区不在整房外包络时会造成物理墙段误标。
- 现改为从候选实际 `TileFootprint.BoundarySides` 读取完整房间最外包络的西/东/南/北边砖；同一最外侧存在多段不同宽度时全部列出。原轴带仍保留，但明确标为“排版相位参考带（仅用于生成相位，不代表完整房间墙段）”。最窄位置同样只从实际边界砖反查，不再根据控制区轴带猜测墙面。
- 新增 L 形房间、控制区位于凹口的回归；Debug/Release 全量 MSTest 均为 286/286。既有四命令、AutoCAD 零写入预览、DOR8、版本、`dist` 和远端均未改变。

## 60. DOR7-G3 墙角优先排序与 G1 阈值说明

- 勾选“墙角对缝优先”时，排序现在严格按已冻结顺序执行：硬规则状态组 → 入口第一视觉范围内低于推荐下限的窄砖 → 270° 墙角安全双向/单缝命中 → G1 门控半砖—整砖—过渡砖来源及其余稳定指标。G1 来源仍是可审计事实，不构成审美总分，也不越过绝对下限硬淘汰、项目规则缺失或待复核状态。
- 墙角锚定替代候选不再被 UI 误读为 G1 重分配。候选概况会分别说明“墙角锚定替代相位”、该轴是否有 G1 来源以及是否触发半砖—过渡砖规则；图面预览继续使用同源 `LayoutDrawingPlan`。
- `0.42T` 推荐下限和等号规则保持不变：只有对应轴自然余量低于下限才生成半砖—整砖—过渡砖。用户截图中的 348、377.555、450.353、376.582 mm 均达到 252 mm 推荐下限，因此方案 2/3 不应被强制改成半砖重分配；若要在满足下限时也强制该版式，需要另行冻结项目规则。
- 新增排序、G1 恢复优先和阈值说明回归；Debug/Release Core 与宿主无关适配自动测试均为 290/290。AutoCAD 适配 Debug/Release 隔离构建 0 警告、0 错误，输出位于 `build/plugin-verify/g3-g1-priority`。本轮仍为 AutoCAD 零写入，DOR8、四个既有命令、版本、`dist`、发布物、标签和远端不变。

## 61. DOR7-G3 门洞边界模式裁切诊断与方向比较更正

- 第 60 节中“只有自然余量低于 `0.42T` 才生成模式”的表述仅适用于旧的窄余量恢复路径，现已更正：完整房间复杂轮廓只要存在可行的半砖或整砖首带、且对侧不低于推荐下限，就独立生成 G1 模式候选；自然余量已达到推荐下限不再阻止模式尝试。墙角优先复选框不控制该 G1 门控路径。
- 348、377.555、450.353、376.582 mm 是最终 `TileFootprint` 在完整房间外墙上的实际裁切宽度；“排版相位参考带”是生成网格的名义首带。二者不一致时，不能把实际边砖直接称作半砖/整砖/过渡砖，必须以候选轴带类型和同源裁切测量为准。
- 每轴先保留门洞对向优先方向，再保留镜像方向作为较低优先级的可比较候选；候选说明会标注镜像来源。若连续相位经过凹口裁切产生低于项目绝对下限的独立切砖，新增 `DoorControlledBoundaryPatternClippedBelowAbsoluteMinimum` 诊断并硬淘汰，不隐藏、不改成可确认方案。
- 本轮新增 G3 回归覆盖：复杂夹具模式裁切硬淘汰、半砖/整砖模式的镜像保留和门洞优先方向排序。Debug/Release Core MSTest 均为 `296/296`；隔离宿主构建 0 警告、0 错误。Debug/Release `TileLayout.AutoCAD.dll` SHA-256 分别为 `93965493B55B8E49BE5B0817A15A6A78D9412CEE9312F169B4F233E0C5DCAFF8`、`1873A0CBB9DB96E95A88BF03488461B3667C7C9108C4F329808422453FD12CED`；Core 分别为 `4C035272E91A0A8BF31294B296119FBF63621A7B69AF79A9D4438C532E5FA96B`、`38CEEEBB3CEA01A73ED0ED7E3B692B8B96514FF5CB36E09D5E14BB963F1EF1C4`。DWG 写入、DOR8、版本、`dist`、发布物、标签、远端和既有四命令仍不改变。
## 62. DOR7-G3 墙角优先排序与复杂房间大边砖淘汰

- 勾选“墙角对缝优先”后的稳定排序为：硬规则状态组 → 入口视觉范围内低于推荐下限的窄砖 → 双向网格交点准确命中 → 单条地砖缝准确命中 → 安全双/单缝 → G1 门洞边界模式及既有稳定指标。这样同状态下无准确对缝方案不会抢在有准确对缝方案之前；未勾选时 G1 搜索继续运行，墙角事实只作只读诊断。
- 复杂正交房间（边数大于 4）按 X/Y 轴消费最终 `TileFootprintAssessment.Measurements`。某轴若有大于半砖且小于整砖的实际边界砖，只有同轴目标墙角准确对缝、G1 半砖/过渡分配或达到推荐下限且不超过半砖的节材边砖可以解释该大边砖；否则记录 `LargeBoundaryCutWithoutCornerOrSavingBand`，状态为 `Eliminated`，并从满足规则/待复核候选移入独立淘汰分组。矩形既有命令不走该门控。
- AutoCAD Palette 新增“大于半砖且无对缝/节材用途”淘汰分组，诊断保留轴、实际尺寸、半砖阈值和边界位置；淘汰候选仍保留原始指标和图面事实，但不能确认、预览采用或写回。显式确认的历史相位不因本新增门控被删除，继续走既有确认记录路径。
- 本轮 Debug/Release Core MSTest 均 `298/298`；AutoCAD 适配 Debug/Release 隔离构建均 0 警告、0 错误。程序集 SHA-256：Debug AutoCAD `B1529BDB6EA8E80939BDB0F821CEFD81F0A6C69C16EDA3237945C84791935FC6`、Core `C14745DF31657E7F08F123801967096DCE72FC2887E238BA3950944F59114C8B`；Release AutoCAD `7293DDF669C5C3BA6755754538991DE8BA8E65382555F140106276538BCB006C`、Core `364DDDD8DDF5A05E702FE36886766A31B1D67914C340F1CE9F78E3E856B2AE4A`。DOR8 仍暂停，整个 G3 继续 AutoCAD 零写入。

## 63. DOR7-G3 对侧整砖/准确对缝节材边砖复核门

- 复杂房间按东西、南北轴逐侧检查：实际边界砖达到推荐下限且小于半砖时，对侧墙的全部实际边界砖必须是完整 `TileFootprint.IsFullTile` 整砖，或对侧存在同轴准确命中目标墙角的地砖缝；推荐下限等号也进入该资格门，否则记录 `SmallBoundaryCutWithoutOppositeFullOrSeam`。
- 该诊断为项目复核警告：候选保留同源几何和诊断，但状态变为 `RequiresUserDecision`/项目规则复核，不进入“满足规则”自动候选。角砖或连续异形砖不能仅凭某一轴跨度等于砖尺寸而按整砖处理；等号和公差沿用 Core。
- 新增回归测试覆盖“完整整砖可放行”“对侧非完整整砖且无准确对缝时转入复核”“推荐下限等号组合”“确认相位不得绕过资格门”和 UI 满足组隔离。当前自动测试基线更新为 `304/304`；新插件需重新构建后再进行 AutoCAD 2021 只读复测。DOR8、四个既有命令、版本、dist、发布物、标签和远端不变。

### DOR7-G3 收口与 DOR8 入口

G3 算法、推荐下限等号组合资格门、候选保留/分组和同源零写入预览已冻结；当前工作树保留全部未提交改动，Debug/Release Core 自动测试为 `304/304`。下一任务 DOR8 仅负责把用户明确确认的保留候选写入专用图层，并复用 `LayoutDrawingPlan`，不允许写回层重算或改写算法事实。淘汰候选、项目规则缺失候选和未完成复核的候选不得写回；写回前必须先冻结图层、覆盖策略、事务/UNDO 和失败回滚边界。DOR8 期间四个既有命令、版本、dist、发布物、标签和远端保持不变。

## 64. DOR8-A 正式写回规则冻结（2026-08-03；项目规则模式于 2026-08-04 修订）

- 用户确认 DOR8 采用 B：`AutomaticUsable` 以及用户看到人工复核提醒并明确确认的 `RequiresUserDecision` 候选允许正式写回；不再要求填写复核原因。该决定只扩展写回授权，不把候选改标为自动合规，不改变 G3 算法或 `304/304` 基线。
- 写回必须使用当前候选的同一 `LayoutDrawingPlan`，且只写 `DivisionLines` 与 `Connections`。`NeutralConnections`、中性区域、房间轮廓、砖块轮廓、墙角诊断、窄砖标记和其他临时预览标记均不写回。
- 专用图层固定为 `TILE_LAYOUT_ORTHO_CONFIRMED`；新建图层使用颜色索引 3 和 `Continuous` 线型，实体按图层属性绘制。同一图层允许多个房间，正式线记录房间范围归属，只有同一房间范围再次写回时拒绝；不覆盖、删除既有对象，也不自动修正已有图层属性。
- 写回前必须显示一次最终确认提示。写回只允许一个原子写事务和一个 AutoCAD `UNDO` 边界，失败自动回滚且不保存 DWG。失败后保留当前同源预览，但写回资格清除，用户必须再次确认；取消、切换候选、刷新、清除和未确认状态保持零写入。
- 项目规则仍为“尚未决定”时，`RequiresProjectPolicy`、淘汰、输入不可信、能力不支持及未完成当前明确确认的候选禁止写回。用户明确选择“按图面确认/不设置数值绝对下限”后，未被其他既有硬规则淘汰且输入可信、能力支持的 `RequiresProjectPolicy` 候选可进入专项提醒、同源预览和最终确认，但仍不是 `AutomaticUsable`。`DecisionRecord` 的人工原因不再作为 DOR8 写回前置条件；实现使用独立的当前会话确认状态，避免向 G3 核心引入空原因例外语义。

## 65. DOR8-B/C 实现与待实机验收（2026-08-03～2026-08-04）

- DOR8-B 已实现正式写回入口、最终确认提示、专用图层、按房间范围的重复写回保护、单事务失败回滚及失败后重新确认。
- 对空写回计划和已有但属性不符的空目标层采用保护性拒绝，不创建空层、不静默改动既有图层属性。
- 已修复选择候选后预览状态未同步、人工复核候选仍被旧 DecisionRecord 门槛阻挡以及成功写回后入口提前置灰的问题；同一房间重复写回由正式线上的房间范围归属直接拒绝，不同房间可追加。
- 写回严格消费同一 `LayoutDrawingPlan` 的 `DivisionLines + Connections`；允许 `AutomaticUsable`、提醒后确认的 `RequiresUserDecision`，以及明确按图面确认模式下提醒后确认的 `RequiresProjectPolicy`，均不要求填写复核原因。
- 正式写回通过 `SendStringToExecute("TILEORTHOUIWRITE")` 排入 AutoCAD 正式命令队列，并在命令内使用单事务；正式写回命令不使用 `NoHistory` 以保留 AutoCAD 撤销边界；写回成功后的取消路径不再发出临时预览清除请求。Core Debug/Release 均 `316/316`；当前适配 Debug 编译为 0 警告、0 错误；未修改 G3 算法、四个既有命令、版本、dist、发布物、标签或远端。
- 最新修复补充 AutoCAD 数据库默认属性、同一事务内模型空间目标层单次遍历判重、写入循环数量核验，以及确认结果返回前的取消/切换保护；当前 AutoCAD 进程仍锁定旧 DLL，因此本轮使用隔离输出验证，未结束或覆盖现场进程。
- 用户已于 2026-08-04 确认规则修正为“同一图层允许多个房间，按房间范围判重”。新写回线使用 `TILE_ORTHO_ROOM` XData 归属标记；同范围拒绝，不同范围追加；DOR8 早期无标记旧实体使用相同正式线几何兼容判重。
- 尚未完成 AutoCAD 2021 副本实机验证；一次 `UNDO`、原图保护、SHA-256 和取消冒烟必须按 [docs/dor8-formal-writeback.md](docs/dor8-formal-writeback.md) 逐项记录后才可收口。
- 2026-08-04 性能与交互补充：选择 `AutomaticUsable`/`RequiresUserDecision` 候选后自动显示同源预览；明确选择按图面确认后，`RequiresProjectPolicy` 也自动显示同源预览和最小边砖专项提醒；同候选复用 `LayoutDrawingPlan`，缓存未变化的候选/诊断列表与只读详情，快速切换时合并预览命令；临时图形正常清除不再强制完整 `Regen`。Core Debug/Release 仍为 `316/316`，AutoCAD 实机耗时仍待验证。

## 66. DOR9-A 灰缝、闭合多段线与抹灰完成面（2026-08-04）

- DOR9-A 规则已由用户冻结：灰缝默认 `1.5 mm`、允许 `0` 且必须有限非负；砖间灰缝为全宽，砖墙灰缝为半宽，墙角对缝按灰缝中心；灰缝只增加网格节距，不改变名义砖尺寸，推荐/绝对下限不计灰缝占位。
- 首期向导接受一个闭合、单外环、共面、WCS 近正交、无 bulge 的 LWPOLYLINE 或传统二维 POLYLINE；拒绝 Polyline3d、圆弧、自交、洞、多外环和混合输入。原始 LINE/POLYLINE 均只读，旧四命令保留原有 `0 mm` 兼容行为。
- 抹灰默认 `0 mm`；正值从原始完成面外侧线向房间内部统一偏移。代码顺序固定为“原始边界 → 抹灰完成面 → 以完成面边界计算门洞/控制区域/主次区/连接边和排版”；偏移失败清除旧预览、不生成候选、不写回。完成面轮廓在非零厚度时作为同源 `LayoutDrawingPlan.DivisionLines` 成员写入正式图层，重复写回仍按原始房间范围/身份判定。
- 已实现灰缝节距、实际砖体轮廓、墙角中心对缝、完成面偏移、凹多边形边界段同步、闭合多段线适配、UI 默认值/输入校验/旧预览失效以及正式写回边界；正式写回仍只消费 `DivisionLines + Connections`。
- 自动验证：Core Debug/Release 测试均 `333/333`；Core Debug/Release、AutoCAD Debug 和 AutoCAD Release 独立验证构建均 0 警告、0 错误。标准输出遵守当前 AutoCAD 进程占用边界，未停止或清理任何文件。
- 用户已于 2026-08-04 确认 DOR9 修复后的 AutoCAD 2021 实机测试完成；DOR8 专项实机证据仍按其专项清单单独核对。静态写入边界发布前复核、最终工作树盘点及拟暂存文件清单确认仍待完成。未获明确授权前不提交、不推送、不创建标签、不改版本号、不改 `dist`、不发布或同步 GitHub。

## 67. DOR9-B 发布前 UI、交互与性能收口（2026-08-04）

- 用户已确认 DOR9 修复后的 AutoCAD 2021 实机测试完成；本轮不改变 G3/DOR9 算法契约、四个既有命令、同源 `LayoutDrawingPlan`、零写入状态边界或正式写回对象边界。
- 向导展示投影改为结果变化时重建：需求文案、满足规则/待复核/规则缺失候选组、淘汰候选筛选与分页均复用只读缓存；候选顺序、状态、资格门和候选计算不变。
- WinForms 完整刷新、会话重置和候选/诊断列表更新采用批量布局/批量绘制；工程详情未展开时不生成全文；候选切换、取消任务和刷新异常均保证忙碌状态复位，降低重复重绘和界面卡死风险。
- Core Debug/Release 自动测试均 `333/333`；Core 与 AutoCAD 适配 Debug/Release 独立构建均 0 警告、0 错误。静态写入边界发布前复核、working tree 盘点和拟暂存文件清单仍待完成；未获明确授权前不提交、不推送、不创建标签、不改版本号、不改 `dist`、不发布或同步 GitHub。
