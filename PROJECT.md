# PROJECT — DEV-001 AutoCAD瓷砖自动排版插件

## 1. 基本信息

- 项目编号：DEV-001
- 项目名称：AutoCAD瓷砖自动排版插件
- 创建日期：2026-07-19
- 项目目录：`E:\Workspace\Development\DEV-001-AutoCAD瓷砖自动排版插件`
- 当前状态：M9、工程起铺控制和 OR1 正交简单房间均已完成并通过自动与 AutoCAD 2021 最小实机验证；DR1 门洞控制的正交矩形规则与 DR2 宿主无关矩形核心/候选模型基座已完成，Debug/Release 自动测试各 97/97 通过；正交异形房间的第二绝对下限、综合美观指标和墙砖缝输入语义仍未冻结，AutoCAD 门洞交互尚未接入；V0.1.0 历史发布保持不变，M10 V0.2.0 发布检查未开始
- 主要使用者：瓷砖排版与土建创优工作使用者
- 关联业务来源：`Project-002-F地块创优策划`

## 2. 背景

当前瓷砖排版依赖人工在 CAD 图纸中布置分格。V0.1 不追求复杂优化，只验证 AutoCAD 插件能否可靠读取一个最简单的四线矩形房间，并按固定规则自动生成 600 mm 地砖分格。

## 3. 项目目标

开发一个可在 Autodesk AutoCAD 2021 中加载的 Managed .NET 插件，完成以下闭环：

1. 用户选择四条 `LINE`。
2. 插件验证其构成轴对齐闭合矩形。
3. 识别西南角和房间宽高。
4. 按固定 600 mm 模数生成分格。
5. 在新图层中写回结果。
6. 整次操作可一次撤销。

## 4. 非目标

- 不开发独立 CAD 查看器或独立 EXE。
- 不支持非 AutoCAD 宿主。
- 不处理旋转房间、自定义 UCS、异形边界或多个房间。
- 不计算灰缝、排版优化、材料损耗或施工下料。
- 不在 V0.1 建设复杂 UI、自动更新或商业授权系统。

## 5. 运行环境

- 操作系统：Windows 10 x64。
- 宿主：Autodesk AutoCAD 2021 简体中文，版本 `R24.0.47.0.0`。
- 语言：C#。
- 目标框架：.NET Framework 4.8。
- 预计开发工具：Visual Studio，具体版本与组件待环境检查。
- AutoCAD SDK/API：AutoCAD 2021 Managed .NET API。

## 6. V0.1 固定需求

### 6.1 输入

- 模型空间中的四条 `LINE`。
- 四线首尾闭合，构成矩形。
- 矩形边与 WCS X/Y 轴平行。
- 图纸单位为毫米。
- 四条线代表房间完成面内边界。

### 6.2 排版规则

- 瓷砖：600 × 600 mm。
- 灰缝：0 mm，完全忽略。
- 起铺点：房间西南角。
- 起铺定义：第一块砖西南角与房间西南角重合。
- 顺序：从西向东、从南向北。
- 东侧和北侧余量直接保留为切砖尺寸。
- 不对窄砖警告，不移动、不居中、不优化。

### 6.3 计算定义

对于房间宽度 `W`、高度 `H`：

- 竖向内部分格线：`westX + 600 × n`，且结果严格小于 `eastX`。
- 横向内部分格线：`southY + 600 × n`，且结果严格小于 `northY`。
- 东侧余量：`W mod 600`。
- 北侧余量：`H mod 600`。
- 尺寸整除时，对应方向不产生边缘余量。

### 6.4 输出

- 新图层：`TILE_LAYOUT_600`。
- 图层中生成矩形内部的瓷砖分格线。
- 命令行报告房间宽高、完整 600 mm 模数数量、东侧余量和北侧余量。
- 正式命令：`TILE600`。

### 6.5 原图保护

- 不修改、移动、删除或改变原四条墙线属性。
- 插件不自动保存图纸。
- 全部新增对象属于一次可撤销操作。

## 7. 验收标准

### 7.1 技术探针验收

- AutoCAD 2021 可执行 `NETLOAD` 并加载插件。
- 插件命令可选择四条 `LINE`。
- 正确读取端点并计算矩形宽高和西南角。
- 能在测试图层创建一条线。
- 一次 `UNDO` 可撤销本次新增内容。

### 7.2 V0.1 功能验收

- 600 mm 整除房间不产生额外边缘分格。
- 非整除房间的东、北余量计算正确。
- 小于 600 mm 的房间仍能正确输出结果而不崩溃。
- 非闭合、非矩形、非轴对齐或选择数量错误时拒绝生成，并给出明确提示。
- 重复执行不修改原墙线。
- 核心算法测试不依赖启动 AutoCAD。

## 8. 里程碑

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M0 | 项目目录、需求基线、Git 初始化 | 已完成 |
| M1 | AutoCAD 2021 `NETLOAD` 最小技术探针 | 已完成：2026-07-19 实机加载、选线、尺寸计算、写回和一次撤销均通过 |
| M2 | 独立核心矩形与 600 mm 网格算法及测试 | 已完成：2026-07-19，核心不引用 Autodesk 程序集，24 项自动测试通过 |
| M3 | AutoCAD 选择、验证、图层、写回与撤销集成 | 已完成：2026-07-20，自动验证及全部宿主实机验收通过 |
| M4 | 脱敏 DWG 实机验收与错误处理 | 已完成：2026-07-20，脱敏副本链、16 项实机矩阵和最终自动回归全部通过，无需产品代码修复 |
| M5 | V0.1.0 打包、说明、哈希与发布检查 | 已完成：Release 27/27、最小包、SHA-256 和一次 AutoCAD 冒烟全部通过 |
| M6 | V0.2 参数化核心契约与安全计数 | 已完成：2026-07-20，Debug/Release 各 48/48，完整解决方案双配置重建通过 |
| M7 | `TILELAYOUT` AutoCAD 适配 | 已完成：2026-07-20，参数提示、共享选择/事务路径、`TILE_LAYOUT` 和三类宿主无关消息已接入，Debug/Release 各 51/51 |
| M8 | V0.2 自动质量门 | 已完成：2026-07-20，锁定恢复、Debug/Release 各 51/51、完整解决方案双配置备用目录重建及产物保护检查均通过 |
| M9 | V0.2 脱敏 DWG 实机验收 | 已完成：2026-07-21，AutoCAD 2021 数值、方向、取消/错误、图层、超限、撤销、原图保护和不保存均通过 |
| M10 | V0.2.0 发布检查 | 未开始 |
| SC1 | 工程起铺角控制 | 已完成：施工语义、核心、AutoCAD 适配、Debug 56/56、隔离插件编译和 AutoCAD 2021 最小实机均通过 |
| OR1 | 正交简单房间边界与裁切 | 已完成：需求/接口、`TILEORTHO`、聚焦代码审查、Debug/Release 81/81、完整解决方案隔离重建和 AutoCAD 2021 最小实机均通过 |
| DR1 | 门洞控制的正交矩形工程排版 | 已完成规则阶段：两个样例、0.42 硬下限、大小砖分配、核心算法和 AutoCAD 交互计划已冻结；宿主无关实现见 DR2 |
| DR2 | 宿主无关矩形门洞核心与候选模型基座 | 已完成：DR1 算法、默认/居中翻转候选、实际砖块/分类、诊断和未加权指标输入；Debug/Release 97/97，完整解决方案双配置隔离重建通过 |

## 9. 技术决策

- 采用 AutoCAD 2021 Managed .NET 插件路线。
- 使用 C# 与 .NET Framework 4.8。
- 核心算法与 AutoCAD 适配分离。
- 先完成技术探针，再创建完整解决方案。
- 第一版不引入第三方几何或优化依赖。

详细记录见 `docs/adr/0001-autocad-2021-managed-net-plugin.md`。

## 10. 风险

- AutoCAD 具体行业版本未知，可能影响 API 或插件加载。
- `NETLOAD`、可信路径或安全加载策略可能阻止程序集加载。
- 开发机可能缺少 Visual Studio、.NET Framework 4.8 Developer Pack 或 AutoCAD 2021 SDK。
- DWG 中的 UCS、单位、端点微误差和墙线语义可能与假设不一致。
- 未签名程序集或安装包可能触发 Windows/安全软件提示。

## 11. 环境与输入确认

- Windows 10 x64、AutoCAD 2021 简体中文 `R24.0.47.0.0` 已确认；
- `NETLOAD`、Visual Studio 2022、MSBuild、VSTest 和 .NET Framework 4.8 开发组件均已确认可用；
- 最终只读脱敏 DWG 夹具已建立并完成 M4 实机验收；
- 四条边界已确认是模型空间、WCS 轴对齐、闭合且代表本次测试房间边界的独立 `LINE`；
- M4、M5 范围内无待输入项；真正发布授权仍需用户另行明确。

## 12. 版本与发布

- 技术探针版本：`0.0.1`。
- 首个可用原型：`0.1.0`。
- 正式发布物进入 `dist`，包含插件包、说明、已知限制和 SHA-256。
- 未经明确授权，不推送远程仓库、不创建 Release、不部署。

## 13. M2 完成记录

- 已建立 `TileLayout.sln`、`TileLayout.Core` 和 `TileLayout.Core.Tests`。
- 公共模型包括三维点、三维线段、WCS 轴对齐矩形、验证结果和分格结果。
- 坐标公差集中为 `1e-6 mm`，等于公差时视为相等，超过公差时拒绝或保留实际余量。
- 四线验证覆盖数量错误、非有限坐标、不同高程、退化线、非轴对齐、非闭合、重复边和缺失边。
- 网格算法固定从西南角开始，先由西向东生成竖向内部分格线，再由南向北生成横向内部分格线；东侧、北侧直接截断。
- 2026-07-19：锁定模式依赖恢复、Debug/Release 完整解决方案重建和两套各 24 项核心测试均通过；M1 探针成功写入标准 Debug/Release 输出目录，未复制 Autodesk DLL。
- 首次验证时 AutoCAD 正在占用原 `build\probe\Debug` 探针 DLL；用户正常关闭 AutoCAD 后，标准输出目录重建通过。未强制结束 AutoCAD 进程。
- M3 已于 2026-07-20 完成；正式 DLL 加载、完整宿主行为和一次撤销均已由用户实机验证通过。

## 14. M3 完成记录

- 已新增正式 `TileLayout.AutoCAD` 项目，输出 `TileLayout.AutoCAD.dll`；M1 `TileLayout.AutoCAD.Probe.dll` 保留且未改为正式插件。
- 正式命令名冻结为 `TILE600`，专用图层为 `TILE_LAYOUT_600`。
- 命令限制为当前图纸模型空间和毫米单位，只接受恰好四条模型空间 `LINE`。
- 四条墙线只以只读方式打开，端点复制为核心 `Point3D`/`LineSegment3D` 后调用 `RectangleValidator` 和 `TileGridCalculator`；AutoCAD 项目没有重复矩形或 600 mm 网格算法。
- 核心验证失败发生在创建图层之前；图层创建和全部分格线写入处于同一事务，异常时不提交。
- 成功消息报告宽高、完整列/行、东/北余量及分格线数；插件代码不调用 DWG 保存操作。
- 增加 3 项不引用 Autodesk 程序集的适配文本纯函数测试。2026-07-19，Debug/Release 两套测试均为 27/27 通过，其中原 M2 24 项继续通过。
- 2026-07-19，NuGet 锁定模式恢复及 Debug/Release 完整解决方案重建通过；正式插件两个输出目录均未复制 `AcCoreMgd.dll`、`AcDbMgd.dll` 或 `AcMgd.dll`。
- 自动验证期间 AutoCAD 未运行，未发生 DLL 锁定；没有启动或关闭 AutoCAD，也未读取、修改或保存任何 DWG/DOCX。
- 2026-07-20 用户实机确认：正式 DLL 可加载，`TILE600` 可执行；3600 × 3000 与 4250 × 3100 分格线生成正确；500 × 500 正确报告 0 列、0 行、东/北余量各 500 mm 和 0 条内部分格线。
- 2026-07-20 用户继续实机确认：选择数量错误、非闭合和非轴对齐输入均被拒绝，给出正确提示且无错误生成；模型空间和毫米单位限制正确；原墙线不变；一次 `U`/`UNDO` 完整撤销；插件未自动保存图纸。
- M3 自动检查与宿主实机检查全部通过。详细记录见 `docs/autocad-integration-m3.md`；M4 和 M5 后续也均已完成。

## 15. M4 启动与完成记录

- 2026-07-20 已完整复核 Git 基线、全局/项目规则、V0.1 需求、M1/M2/M3 文档、ADR、项目配置、全部现有源码和自动测试；未提交 M2/M3 文件是用户认可的工作区状态，继续保留。
- M4 启动时 `inputs` 只有策划书、`tests/fixtures` 只有说明文件；随后用户提供 `inputs/test.dwg`，所有结果均来自实际文件和宿主操作，没有伪造 AutoCAD 实机结果。
- 已建立 `docs/dwg-acceptance-m4.md`，规定原始输入、脱敏工作副本和最终夹具的隔离方式、SHA-256 证据、初学者实机步骤、16 项验收矩阵及缺陷处理门。
- M4 已检查匿名图层、实际非原点坐标、统一/不同高程、邻近对象选择、错误回滚、既有/锁定输出图层、重复执行、一次撤销和不自动保存。
- 全部实机结果、原始 DWG 前后哈希和宿主证据均已记录；未发现可复现产品缺陷，未修改 M2/M3 产品源码。
- 2026-07-20 自动质量门：锁定模式恢复通过；Debug/Release 各 27/27 测试通过；因 AutoCAD 正在运行，正式插件和完整解决方案使用 `build/plugin-m4-verify`、`build/solution-m4-verify` 备用目录完成 Debug/Release 重建，输出未复制 Autodesk DLL。未启动或关闭 AutoCAD。
- 2026-07-20 已只读核验 `inputs/test.dwg`：31,890 字节、只读、SHA-256 为 `646A3A7A22CF40E5EC0B9CF8621A17AFAB09BB27928772C05D9CB3F4202DDA75`；已复制到被 Git 忽略的 `work/m4-redaction/test-working.dwg`，初始哈希一致，并仅对工作副本解除只读属性。原始文件未打开或修改。
- 2026-07-20 用户确认工作副本无敏感文字、无外部参照/底图，已抽查边界为 `LINE`，`INSUNITS=4`、`TILEMODE=1`；已建立只读 `tests/fixtures/m4-real-room-sanitized.dwg` 及同名说明，最终夹具大小 31,890 字节且 SHA-256 与原始基线一致。DWG 未获 Git 授权，继续由 `.gitignore` 排除。
- 2026-07-20 M4-01 基线通过：四条边界均为模型空间图层 `0` 的独立 `LINE`，句柄 `29a/29b/298/297`，统一高程 Z=0，组成 5600 × 8600 mm 轴对齐闭合矩形；颜色/线型/线宽均为 `ByLayer`；人工预期为完整列/行 9/14、东/北余量 200/200 mm、内部线 23 条。
- 2026-07-20 M4-03 数值和输出通过：四条墙线颜色/线型/线宽均为 `ByLayer`；`TILE600` 实机报告 5600 × 8600 mm、完整列/行 9/14、东/北余量 200/200 mm，并生成 23 条；`_QSELECT` 独立确认正好 23 个项目，后续 M4-15 交叉确认执行前同名图层不存在。
- 2026-07-20 M4-15 与墙线保护通过：一次 `U` 同时移除本次创建的 `TILE_LAYOUT_600` 图层和 23 条分格线；四条墙线句柄、图层、模型空间、起终点、长度、方向和 Z 均与 M4-01 完全一致。非当前/锁定墙线图层变体随后由 M4-02 验证通过。
- 2026-07-20 M4-13 通过：同一房间连续执行两次后，`_QSELECT` 确认同一输出图层共有 46 个项目。首次 M4-14 流程受中间 `_QSELECT` 干扰，不作产品判定；随后按干净流程复测通过。
- 2026-07-20 M4-14 复测通过：关闭且不保存、干净重开后连续执行两次 `TILE600`，第二次成功后未插入其他命令而立即执行一次 `U`；`_QSELECT` 确认输出从 46 条恢复为 23 条，第一次结果和输出图层保留。
- 2026-07-20 M4-04 通过：把既有 `TILE_LAYOUT_600` 设为红色、`Continuous`、默认线宽后再次执行，项目由 23 增至 46；命令复用同一图层且执行前后颜色、线型和线宽保持不变。
- 2026-07-20 M4-12 通过：AutoCAD 2021 Managed API 允许向锁定的 `TILE_LAYOUT_600` 追加实体，命令完整成功，项目由 46 精确增至 69，无部分写入；执行后仍保持锁定、红色、`Continuous`、默认线宽。V0.1 未规定锁定输出图层必须拒绝，因此不判定为产品缺陷、不修改代码。
- 2026-07-20 M4-09 通过：取消选择时正确提示未完成选择；选择五条时明确报告本次选择 5 条并拒绝，且无输出图层。
- 2026-07-20 M4-10 通过：三条后按 Enter、非闭合四线、含斜线的非轴对齐四线分别得到准确提示，均未生成任何对象；用户最终确认无输出图层残留。
- 2026-07-20 用户明确确认全部选择/几何错误测试后不存在 `TILE_LAYOUT_600`，M4-10 完整通过。M4-05 实际非原点坐标也已通过：西南角 `(915.5972, 1270.2809, 0)` 的 5600 × 8600 mm 房间生成结果与人工计算完全一致。
- 2026-07-20 M4-06 通过：干净重开后把四条墙线整体移动到统一 Z=100，`TILE600` 仍正确报告 5600 × 8600 mm、9/14、余量 200/200 mm并生成 23 条；`_QSELECT` 和属性面板确认全部输出线 Z=100。
- 2026-07-20 M4-07 通过：干净重开后只把一条墙线移动到 Z=100，`TILE600` 准确提示四条线必须位于同一 WCS 高程，未生成任何对象且无输出图层残留。
- 2026-07-20 M4-02 通过：干净重开后把四条墙线移入非当前且锁定的 `WALL_TEST_ANON`；`TILE600` 仍正确生成 23 条，用户确认墙线图层、句柄、坐标、Z 和锁定状态均与预期一致，输入对象未被改写。
- 2026-07-20 M4-08 通过：在房间内临时加入圆和未分解矩形并一并框选时，选择器只找到四条 `LINE`，命令完成后 `_QSELECT` 确认 23 条输出；用户确认两个邻近对象仍存在且未变化。
- 2026-07-20 M4-11 通过：布局空间和 `INSUNITS=Inches` 均在选择前被拒绝并提示明确，无输出残留。M4-16 通过：关闭不保存并重开后 `INSUNITS=4`，无输出图层、临时圆或临时矩形，原四线仍在；原始只读 DWG 哈希仍与初始基线一致。
- 文件系统复核当前 AutoCAD 进程版本为 `R24.0.47.0.0`，DWG 文件头为 `AC1032`（AutoCAD 2018 DWG 格式系列）。用户最终确认 `TILEMODE=1` 并执行 World UCS。
- 2026-07-20 M4 最终回归：NuGet 锁定模式恢复通过；Debug/Release 自动测试各 27/27 通过；完整解决方案 Debug/Release 在 `build/solution-m4-final` 备用目录重建通过，未复制 Autodesk DLL；`git diff --check` 通过。

## 16. M5 发布检查记录

- 2026-07-20 开始 M5 前确认分支为 `main`、Git 基线为 `546663c`；M2、M3、M4 已验证但未提交的改动完整保留，没有清理、回退或覆盖。
- AutoCAD 2021 仍在运行，因此完整解决方案 Release 使用 `build/solution-m5/Release` 备用输出目录重建通过，没有覆盖标准插件目录或强制关闭宿主。
- NuGet 锁定模式恢复通过；Release 自动测试 27/27 通过。
- `TileLayout.AutoCAD.dll` 和 `TileLayout.Core.dll` 的 AssemblyVersion/FileVersion 均为 `0.1.0.0`。
- 已建立 `dist/TileLayout-0.1.0/` 和 `dist/TileLayout-0.1.0.zip`；压缩包只含两个运行 DLL 与 `使用说明.md`，不含 Autodesk DLL、探针、测试组件、PDB、DWG、缓存或日志。
- 压缩包最终 SHA-256 为 `322077112229CA8E0EDFB0CEE0B1F3F192A24EAC2CC23D11DCEA413CC3431141`，包外 `dist/TileLayout-0.1.0-sha256.txt` 同时记录压缩包和三个包内文件的校验值。
- 安装、`NETLOAD`、`TILE600`、支持范围、已知限制和验证摘要已写入包内说明；完整发布检查见 `docs/release-v0.1.0.md`。
- M5 没有发现可复现产品缺陷，未修改产品源码、未增加无关测试。
- 2026-07-20 用户完成唯一一次发布包冒烟：从 Release 交付包加载正式 DLL 成功，在脱敏夹具执行 `TILE600` 正确生成 23 条，并可一次撤销。未重复 M4 错误矩阵。
- M5 全部验收项已通过；没有未验证项。
- 2026-07-20 经用户明确授权，已创建私有仓库 `PermissionDog001/DEV-001-AutoCAD-Tile-Layout`，推送 `main`，在验收提交 `b31bc13` 上创建 `v0.1.0` 标签，并发布包含 ZIP 与 SHA-256 清单的正式 GitHub Release；尚未公开发布或部署。
- M4-01 至 M4-16 全部通过，未发现违反 V0.1 的可复现产品缺陷，因此未修改 M2/M3 产品源码。原始 `inputs/test.dwg` 最终仍为只读、31,890 字节，SHA-256 与初始基线一致；DWG 未获 Git 授权，继续忽略。

## 17. V0.2 范围冻结记录

- 2026-07-20 用户确认 V0.2 主目标为“自定义方砖/矩形砖规格，其他几何和优化能力暂缓”。
- 新命令冻结为 `TILELAYOUT`，按次输入砖宽和砖高，默认均为 600 mm；宽沿 WCS X、高沿 WCS Y，不自动旋转。
- 新命令固定写入 `TILE_LAYOUT`；现有 `TILE600` 和 `TILE_LAYOUT_600` 保持 V0.1 行为，作为兼容性回归基线。
- 房间输入继续限制为模型空间、毫米单位、同一高程、WCS 轴对齐的四条独立 `LINE`；西南角起铺、灰缝 0、东/北直接截断规则保持不变。
- 单次新命令最多允许生成 10,000 条内部分格线；超限必须在创建图层前拒绝，避免溢出、卡死或海量对象分配。
- V0.2 不处理旋转矩形、多段线输入、异形、洞口/柱、居中/窄砖优化、灰缝、多房间、旧结果替换、参数窗口或参数持久化。
- 开发顺序冻结为 M6 参数化核心、M7 AutoCAD 适配、M8 自动质量门、M9 脱敏 DWG 实机验收、M10 V0.2.0 发布检查；M7 完成前保持串行。
- 完整需求见 `docs/requirements-v0.2.md`，技术方案和验收顺序见 `docs/technical-plan-v0.2.md`。
- 本次范围任务只修改需求和项目状态文档，未修改产品源码、测试、构建配置、程序集版本或发布物。

## 18. M6 完成记录

- 新增宿主无关、不可变的 `TileLayoutParameters`，统一验证砖宽和砖高必须有限且严格大于 `GeometryTolerance.Coordinate`。
- `TileGridCalculator` 新增参数化入口；既有固定 600 × 600 mm 入口作为兼容包装，与参数化入口共用单一算法来源。
- `TileLayoutResult` 现在保留实际参数，同时保持既有房间、完整列/行、余量和只读分格线契约。
- 参数化入口先用数值路径统计内部线数，通过 10,000 条上限后才创建集合；10,001 条和极小合法规格的超大结果会抛出专用异常，不进行海量分配或开放式循环。
- 新增参数验证、五个冻结样例、矩形砖方向、小数/偏移/高程/公差、超限边界、极小规格和固定 600 mm 逐线兼容/不限额测试；测试总数由 27 增至 48。
- 2026-07-20 锁定模式恢复、核心测试项目 Debug/Release 重建及两套 48/48 测试均通过；完整解决方案 Debug/Release 使用 `build/solution-m6` 备用输出目录重建通过，未复制 Autodesk Managed DLL。
- 验证期间 AutoCAD 2021 正在运行，未被启动、关闭或操作；未修改 DWG、`dist`、V0.1.0 Release、程序集版本或远端状态。
- M7 尚未开始：没有新增 AutoCAD 命令、参数提示、`TILE_LAYOUT` 图层、宿主消息或实机写回。详细验收见 `docs/core-parameterization-m6.md`。

## 19. M7 完成记录

- 新增正式命令 `TILELAYOUT`，按砖宽、砖高顺序提示，每次执行默认均为 600 mm；砖宽沿 WCS X、砖高沿 WCS Y，不自动旋转。
- 零值、负值及不满足 M6 参数契约的尺寸会显示明确错误并继续提示；任一参数取消后在进入四线选择前停止。
- `TILE600` 与 `TILELAYOUT` 共用模型空间和毫米单位检查、四条模型空间 `LINE` 过滤、只读核心快照、矩形验证、事务写回、异常回滚和原图保护提示，没有复制完整命令实现。
- `TILE600` 继续调用不限额的固定 600 × 600 mm 核心入口并写入 `TILE_LAYOUT_600`；既有成功、验证、选择、环境和失败消息保持兼容。
- `TILELAYOUT` 调用 M6 参数化核心并固定写入 `TILE_LAYOUT`。核心在生成结果集合前执行 10,000 条检查，适配层仅在计算成功后才创建或复用图层；既有同名图层只读取其 ID，不修改属性。
- 新增宿主无关的参数化成功、参数错误和超限消息测试；连同既有回归，Debug/Release 自动测试各 51/51 通过。
- 锁定模式恢复及完整解决方案 Debug/Release 重建通过。验证时 AutoCAD 2021 正在运行，因此输出使用 `build/solution-m7/Debug` 和 `build/solution-m7/Release` 备用目录，未覆盖已加载 DLL，未强制关闭宿主；输出未包含 Autodesk Managed DLL。
- 本里程碑未操作任何 DWG，未修改 `dist`、V0.1.0 Release、程序集版本、Git 标签或远端状态，也未提交或推送。
- M7 没有宣称完成宿主实机验收。参数默认/重输/取消、矩形砖方向、`TILE_LAYOUT` 属性保持、超限图层前拒绝、一次撤销、失败回滚、原墙线保护和不自动保存证据当时留给 M9；这些宿主证据现已由 M9 补充。详细记录见 `docs/autocad-adapter-m7.md`。

## 20. M8 完成记录

- 2026-07-20 已完整复核 M6/M7 需求、方案、实现、项目配置和全部自动测试；`TILE600` 兼容、`TILELAYOUT` 参数/取消顺序、共享选择与事务路径、`TILE_LAYOUT` 复用、10,000 条图层前拒绝及三类宿主无关消息均符合冻结设计。
- 未发现由 M6/M7 引入且可通过当前自动环境复现的产品缺陷；M8 没有修改产品源码或增加回归测试。
- NuGet 锁定模式恢复通过；Debug/Release 测试项目重建通过，自动测试各 51/51 通过。
- AutoCAD 2021 正在运行，因此完整解决方案 Debug/Release 使用新的 `build/solution-m8` 和 `build/obj-solution-m8` 隔离目录重建通过；未覆盖标准插件 DLL，未启动、关闭或操作宿主。
- 两套完整输出均未复制 `AcCoreMgd.dll`、`AcDbMgd.dll` 或 `AcMgd.dll`；核心项目只引用 `System` 和 `System.Core`。
- 核心与正式插件的 AssemblyVersion/FileVersion 均保持 `0.1.0.0`；`dist/TileLayout-0.1.0.zip` 哈希和本地 `v0.1.0` 标签保持既有基线，没有执行远端写入。
- 原始 `inputs/test.dwg` 仍为只读、31,890 字节，SHA-256 为 `646A3A7A22CF40E5EC0B9CF8621A17AFAB09BB27928772C05D9CB3F4202DDA75`；没有执行任何 DWG 操作。
- M8 详细证据见 `docs/automated-quality-gate-m8.md`。其后 M9 AutoCAD 2021 脱敏 DWG 实机验收已完成，M10 V0.2.0 发布检查仍未开始。

## 21. M9 完成记录

- 2026-07-20 至 2026-07-21，使用 `work/m9-acceptance/m9-working.dwg` 在 AutoCAD 2021 简体中文 `R24.0.47.0.0` 完成 V0.2 实机验收；原始 `inputs/test.dwg` 未被打开或修改。
- 首次会话复用了已加载的旧 `build/plugin/Debug` 同版本程序集，导致 `TILELAYOUT` 未注册；用户正常退出且不保存后，新会话只加载 `build/solution-m9/Debug` 隔离 DLL。该宿主加载状态已用文件锁核对，不判定为产品缺陷。
- 当前 V0.2 DLL 下，`TILE600` 仍为 9/14、余量 200/200 mm、23 条并写入 `TILE_LAYOUT_600`；`TILELAYOUT 600×600` 统计相同并写入 `TILE_LAYOUT`，逐线自动回归继续证明两者几何一致。
- 600 × 1200 为 9/7、16 条；交换为 1200 × 600 后为 4/14、18 条，确认宽沿 WCS X、高沿 WCS Y 且不自动旋转；800 × 800 与 700 × 1200 的统计及第一条竖线关键坐标均实机一致。
- 零值/负值重输、砖宽取消、砖高取消、选择取消和含斜线的无效四线均正确停止且无输出残留。
- 0.5 × 9000 mm 预估 11,199 条，在 `TILE_LAYOUT` 不存在和已存在两种状态下都于图层创建/修改前拒绝；既有对象数和图层属性不变。
- `TILE_LAYOUT` 预设红色、`Continuous`、0.30 mm、锁定后可完整追加，属性和锁定状态保持；多规格累计对象数与逐次新增数完全一致。
- `TILELAYOUT` 与 `TILE600` 成功结果均可一次 `U` 撤销；原墙线句柄 `29a/29b/298/297`、图层和端点与 M4 基线一致。
- 关闭工作副本并选择“不保存”后，原始输入、只读夹具和 M9 工作副本均为 31,890 字节，SHA-256 均为 `646A3A7A22CF40E5EC0B9CF8621A17AFAB09BB27928772C05D9CB3F4202DDA75`。
- 用户明确要求停止低收益重复实机步骤。未在宿主生成恰好 10,000 条，未人为注入 `AppendEntity`/`Commit` 异常，也未再重复一次界面重开核对；自动边界、无残留失败场景和关闭后同哈希证据均已记录，不伪造未执行结果。
- M9 启动时重新完成锁定恢复、Debug/Release 各 51/51 自动测试、`build/solution-m9` 完整解决方案双配置隔离重建和 Autodesk DLL 泄漏检查，全部通过。
- 详细证据见 `docs/dwg-acceptance-m9.md`。M10 未开始；未修改程序集版本、`dist`、V0.1.0 Release、Git 标签或远端状态，未提交或推送。

## 22. SC1 工程起铺控制记录

- 2026-07-21 在完整保留 M6–M9 未提交工作树的前提下启动；M9 证据继续作为唯一宿主基线，M10 没有开始。
- 已比较“四个房间角”“分别选择 X/Y 起排方向”和“任意基准点偏移”。当前采用四角方案：一次操作覆盖两个方向的四种组合，施工表达直接，且余量和坐标确定。
- `TileLayoutParameters` 增加 WCS 起铺角，旧的宽/高构造器仍默认西南；`TileLayoutResult` 在保持西南角兼容属性的同时提供四侧实际余量。
- 核心对东侧起铺使用 `eastX - TileWidth × n`，对北侧起铺使用 `northY - TileHeight × n`；始终先竖线后横线，并按起铺方向排序。
- `TILELAYOUT` 交互冻结为砖宽、砖高、`SW/SE/NW/NE` 起铺角、四线选择；默认 `SW`。角选择取消发生在边界选择和任何写入之前。
- `TILE600` 保持固定西南角、600 × 600 mm、原提示和 `TILE_LAYOUT_600`，不增加角选择。
- Debug 测试项目重建通过，56/56 自动测试通过；正式 AutoCAD 项目在 `build/start-control/Debug` 隔离输出编译通过，均为 0 警告、0 错误，且未复制 Autodesk Managed DLL。
- 自动阶段没有把编译结果冒充宿主证据；随后用户在新 AutoCAD 2021 会话加载 `build/start-control/Debug` 隔离 DLL，并使用脱敏工作副本完成最小实机。
- `TILELAYOUT 600×600 NE` 报告 9/14、西/南余量 200/200 mm、23 条；首条竖线 X=`5915.5972`，首条横线 Y=`9270.2809`，均与预期一致。
- 起铺角 Esc 在选择前取消且无输出；`TILE600` 无新增提示，仍为 9/14、东/北余量 200/200 mm、23 条，并可在无中间命令时一次 `U` 全部撤销。
- 最终关闭不保存后，原始输入、只读夹具和工作副本均为 31,890 字节，SHA-256 均为 `646A3A7A22CF40E5EC0B9CF8621A17AFAB09BB27928772C05D9CB3F4202DDA75`。未发现可复现产品缺陷。
- 任意基准点暂缓；非矩形房间只完成边界/依赖评估。下一独立里程碑建议先做单一、无洞、无自交的 WCS 正交闭合 `LINE` 区域，前提是先形成可用 Git 基线并重新冻结非矩形起铺锚点。
- 详细定义见 `docs/start-control.md`，非矩形风险和建议见 `docs/non-rectangular-room-assessment.md`。
- 未修改程序集版本、`dist`、V0.1.0 Release、Git 标签或远端状态，未提交或推送。

## 23. OR1 正交简单房间边界与裁切记录

- 2026-07-21 从唯一 Git 基线 `main@6aa51d9` 启动，工作树干净且仅本地领先 `origin/main` 1 个既有提交；保持串行，未创建并行任务或 worktree。
- 首个里程碑冻结为同一模型空间、同一 WCS 高程、4 条及以上 WCS X/Y 轴对齐独立 `LINE`，且必须形成单一、无洞、无自交、无重叠、无 T 形分叉和无分离环的简单闭环。
- 比较包围盒四角、边界顶点和任意 WCS 点后，采用“WCS 包围盒四角网格锚点”。它复用 `SW/SE/NW/NE` 的方向组合，但只定义网格相位，允许位于凹房间外，不再宣称是第一块砖的房内角；边界顶点和任意基准点暂缓。
- 新增 `AxisAlignedOrthogonalPolygon`、验证结果/错误、确定性端点归并和端点图验证。链式公差歧义拒绝；相邻共线碎段合并；规范化环固定为逆时针并从最小 X/Y 顶点开始，与选择顺序和线方向无关。
- 新增正交网格裁切：半开顶点规则、奇偶区间、凹角多片段、区间合并、共线边界扣除和小于等于集中公差的数值碎片丢弃。先计最终片段再创建结果，恰好 10,000 条允许，超过即拒绝。
- 新增独立 `TILEORTHO` 和 `TILE_LAYOUT_ORTHO`。交互为砖宽、砖高、包围盒网格锚点、4 条及以上 `LINE`；验证和片段上限均在图层创建前完成，原线只读，全部输出仍在一个事务中写入。`TILE600` 和 `TILELAYOUT` 保持既有四线矩形入口和消息。
- NuGet 锁定模式恢复通过；聚焦审查后 Debug/Release 测试项目及完整解决方案隔离重建通过；自动测试均为 81/81。正式插件双配置隔离编译通过，全部隔离输出未包含 Autodesk Managed DLL。
- 2026-07-21 聚焦代码审查复核了拓扑、公差归并、共线合并、扫描线顶点规则、边界扣除、最终片段上限、AutoCAD 只读输入和单事务写回。审查发现绝对 WCS 坐标直接参与鞋带面积公式时，大坐标偏移会因浮点消减把合法房间误判为零面积；现改为相对首顶点计算并增加 `1e12` WCS 偏移回归。未发现其他与冻结契约冲突的问题。
- 核心与正式插件程序集版本仍为 `0.1.0.0`；`dist/TileLayout-0.1.0.zip` 哈希仍为 `322077112229CA8E0EDFB0CEE0B1F3F192A24EAC2CC23D11DCEA413CC3431141`。
- 自动阶段没有把编译结果冒充宿主证据。随后用户在独立脱敏工作副本加载隔离 Debug DLL：共线分段矩形准确报告 3600 × 3000、6/5、0/0 和 9 条；L 形准确报告 1800 × 1800、3/3、0/0 和 4 条，且凹口内及共线边界无误生成；100 mm 断口被准确拒绝且既有结果不变。
- 共线分段矩形成功后未插入其他命令，立即一次 `U` 移除全部 9 条结果，原 5 条边界保留。最终关闭工作副本并选择不保存；原始输入、只读夹具和工作副本均为 31,890 字节，SHA-256 均为 `646A3A7A22CF40E5EC0B9CF8621A17AFAB09BB27928772C05D9CB3F4202DDA75`。
- 任意斜边、旋转网格、洞口/柱、多外环和多房间通缝不进入 OR1；M10 未启动，未修改版本、`dist`、V0.1.0 Release、Git 标签或远端状态，未提交或推送。

## 24. DR1 门洞控制的正交矩形工程排版规划

- 2026-07-22 在 `main@a9f6767` 上完成正交矩形业务规则整理；本阶段只修改规划文档，没有修改产品源码、测试、构建配置、DWG、版本或发布物。
- 门洞按整个洞口处理，双扇门不区分开启门扇；门洞控制区域决定大小砖分配，但不等同于几何起铺角。
- 任一边界砖采用对应砖尺寸 `0.42` 倍的全局绝对下限。自然末排余量满足下限时保持单边方案；低于下限时把“一块整砖 + 窄余量”重分配为“半砖 + 较大过渡砖”，中间继续排整砖。
- 进深方向将较大过渡砖放在门洞墙一侧、半砖放在对面；沿门方向将半砖放在离门洞较近一侧、过渡砖放在较远一侧。门洞居中时两种镜像均可，程序使用固定优先级并允许预览翻转。
- 核心计划新增宿主无关的门洞、轴向边界带和工程排版结果模型，不扩充现有四角枚举；四面门洞通过同一局部坐标算法作对称变换。
- AutoCAD 首版建议先实现可靠的门洞两端点确认、识别摘要和 `[接受/翻转/重选/取消]` 预览流程；门块/图元自动识别以后作为可回退的增强能力。
- 详细规则、两个样例、算法步骤和验收矩阵见 `docs/door-controlled-rectangular-layout.md`。
- 后续先完成 L 形、“凹”字形和“凸”字形正交房间的门洞控制规则发现，再进入宿主无关 DR2；这两项后续状态分别见第 25、26 节。
- M10 未启动，版本、`dist`、V0.1.0 Release、Git 标签和远端状态保持不变。

## 25. DOR1 门洞控制的正交异形房间第一轮规则发现

- 2026-07-23 完成 L-01、L-03、L-04 A～E、L-05和多凹凸P-01截图/DWG口述样例的第一轮整理；每个样例均记录几何及门洞、人工尺寸关系、确认规则、待确认问题、算法假设和预期验收结果。
- 已确认候选至少包括“整房单一网格相位后裁切”和“门洞主区矩形规则 + 次区连接组合”两类；房型本身不能预先决定必须全局排版还是分区排版。
- `0.42T` 是普通独立边界砖带的默认下限；连续异形砖的局部腿不逐腿检查。突出深度低于下限时先尝试与相邻砖合并且单轴总宽不超过一砖，无法吸收时才进入受控窄带例外。
- L-04和L-05确认存在第二绝对下限例外，200 mm和250 mm样例可以直接生成；但下限的准确数值、固定毫米/比例形式和等号规则尚未提供，不得在代码中猜测常量。
- 候选整体质量需要同时观察内部非整砖、内部过渡/断缝、边界砖质量、长缝连续、门口观感和关键墙砖对缝。墙砖对缝不是硬约束，也不能仅靠对缝数量抵消明显更差的中央版面。
- P-01进一步确认同一全局网格可以组合“转折自然对缝、合法边界砖裁切、连续异形砖收口”；左下150 × 100 mm缺口属于一块连续异形砖，不是两条独立违规窄砖。
- 详细证据、尺寸、截图哈希、跨样例矩阵和待确认项见 `docs/door-controlled-orthogonal-layout.md`。
- 下一独立编码任务冻结为“DR1矩形门洞核心 + DR2候选模型基座”：只修改宿主无关核心和自动测试，先冻结候选、实际砖块、诊断及排序输入模型；不接AutoCAD、不识别墙砖图元、不猜测第二绝对下限、不启动M10。该任务现已完成，见第 26 节。
- DR1自动测试和候选模型验收后，再串行进入异形候选生成：全局相位裁切、主次区组合、突出带吸收、实际砖块分类和可解释排序。综合美观仍无法唯一判定时应保留多个候选及原因，不得用未经确认的权重伪造唯一答案。
- 本轮只更新规则和开发计划文档；未修改产品源码、测试、构建配置、DWG、版本、`dist`、Git标签或远端状态，未提交或推送。

## 26. DR2 宿主无关矩形门洞核心与候选模型基座

- 2026-07-23 从用户指定的规则基线 `main@81aff90` 启动；开工时工作树干净，`main` 比 `origin/main` 领先 3 个既有提交。
- 新增 `DoorOpening`、`EngineeringRectangularLayoutParameters`、`EngineeringRectangularLayoutCalculator` 和 `EngineeringRectangularLayoutResult`。门洞范围使用沿墙 WCS 坐标表达，并可报告到沿墙两端的净距；核心不引用 Autodesk 程序集。
- 每个轴由 `BoundaryBandPlan` 明确标注门洞法向或沿墙角色、控制边、施工起铺边、自然余量、半砖/过渡砖所在边、内部整砖数和从 WCS 低端到高端的全部砖带宽度。
- 自然余量为 0、恰好或高于 `0.42T` 时保持自然方案；只有 `0 < r < 0.42T` 且至少存在一块整砖时才使用 `T + r = 0.5T + (0.5T + r)`。无法重分配的极小轴返回带结构化淘汰原因的失败候选，不静默生成。
- 门洞法向固定为门侧过渡砖、对侧半砖；沿墙方向固定为近门侧半砖、远门侧过渡砖。东、西、南、北门洞共用同一方位映射；居中默认北优先于南、西优先于东，并只为不对称的沿墙方案增加翻转候选。
- 新增可复用 `LayoutCandidate`、`AxisBoundaryBand`、`TileFootprint`、内部/边界分类、候选诊断和淘汰原因。矩形砖块轮廓按需生成，避免仅为候选模型预先分配大量砖对象。
- `LayoutCandidateMetrics` 只保存后续排序所需的原始指标：内部非整砖块数/面积、内部过渡缝长度、边界非整砖、低于默认下限的边界砖、最小边界带、相位重置、连续异形砖、首视线和关键对缝；没有总分、权重或唯一美观排序。
- 第二绝对下限仍未配置；当前只通过通用策略诊断码和原始指标建立扩展边界，不猜测固定毫米值、比例或等号规则。
- 新增 16 项测试方法，使完整自动测试由 81 增至 97。覆盖两个矩形人工样例、四面门洞旋转/镜像、`0.42T` 等号和略低边界、自然余量、整除、半砖重分配、矩形砖、居中默认/翻转、极小房间淘汰、实际砖块分类、候选指标、大 WCS、门洞范围和 10,000 条上限。
- Debug/Release 测试项目均为 0 警告、0 错误，自动测试各 97/97 通过。AutoCAD 2021 正在运行，因此完整解决方案 Debug/Release 写入 `build/dr2-solution` 隔离目录重建，未覆盖已加载 DLL；隔离输出未包含 Autodesk Managed DLL。
- 本阶段没有修改 `TileGridCalculator`、`OrthogonalTileGridCalculator` 或 AutoCAD 源码；`TILE600`、`TILELAYOUT`、`TILEORTHO` 继续由原路径构建，未新增命令、图层或 DWG 写回。
- 未读取、修改或生成 DWG，未启动 M10，未修改版本、`dist`、V0.1.0 Release、Git 标签或远端状态，未提交或推送。
