# AGENTS.md — DEV-001 AutoCAD瓷砖自动排版插件

本项目继承 `E:\Workspace\AGENTS.md` 的全部规则。本文件只记录 DEV-001 的专属约束。

## 1. 项目目标

开发一个运行于 Autodesk AutoCAD 2021 的 Managed .NET 插件。V0.1 在模型空间中读取用户选择的四条 `LINE`，识别与 WCS 坐标轴平行的矩形房间，从西南角起铺 600 × 600 mm 地砖，并在专用新图层中生成分格线。

## 2. V0.1 固定边界

- 宿主：Autodesk AutoCAD 2021，具体行业版本待实机确认。
- 平台：Windows x64。
- 语言：C#。
- 目标框架：.NET Framework 4.8。
- 输入：模型空间内四条 `LINE`，组成闭合、轴对齐矩形。
- 单位：毫米。
- 瓷砖：600 × 600 mm 正方形地砖。
- 灰缝：按 0 mm 处理。
- 起铺：第一块砖西南角与房间西南角重合。
- 方向：从西向东、从南向北。
- 边界：东侧、北侧直接截断；不居中、不平移、不优化窄砖。
- 输出：专用图层 `TILE_LAYOUT_600` 中的分格线。
- 命令名：暂定 `TILE600`。
- 原图保护：不得修改四条墙线；生成操作必须可通过一次 AutoCAD `UNDO` 撤销；插件不自动保存 DWG。

V0.1 不处理旋转房间、自定义 UCS、门洞、柱、地漏、异形空间、多房间联排、墙砖、灰缝、材料损耗优化或独立 EXE。

## 3. 开工顺序

每次编码任务开始前：

1. 查看 `git status`，保留用户已有改动。
2. 读取本文件、`README.md`、`PROJECT.md` 和相关 `docs`。
3. 确认当前阶段是环境探针、核心算法、CAD 集成、测试还是发布。
4. 搜索现有实现与测试，避免重复建设。
5. 只修改完成当前验收目标所需的文件。

## 4. 第一技术关口

在完整排版功能之前，先验证：

1. AutoCAD 2021 中存在并可使用 `NETLOAD`。
2. 插件程序集能够加载。
3. 命令能够选择四条 `LINE`。
4. 能读取端点并计算矩形长宽、西南角。
5. 能在测试图层绘制一条线。
6. 整个操作可一次撤销。

技术探针未通过前，不建设完整参数窗口、安装器或复杂架构。

## 5. 代码架构原则

- 排版核心与 AutoCAD 适配分离。
- 核心几何和网格算法不得引用 AutoCAD 程序集。
- AutoCAD 项目负责命令、选择、事务、图层、坐标转换和写回。
- 任何宿主对象在转换为核心模型后再进入算法层。
- 第一版使用确定性算法，不引入优化器、AI 或不必要的第三方几何库。

计划模块：

- `TileLayout.Core`：矩形模型、验证、600 mm 网格计算和结果模型。
- `TileLayout.AutoCAD`：AutoCAD 2021 命令与适配。
- `TileLayout.Core.Tests`：不启动 AutoCAD 即可运行的核心测试。

## 6. AutoCAD 引用规则

- 使用 AutoCAD 2021 对应的 Managed .NET 程序集。
- 常用引用预计包括 `AcCoreMgd.dll`、`AcDbMgd.dll`、`AcMgd.dll`。
- AutoCAD 程序集引用应设置 `Copy Local = false`，不得把 Autodesk 程序集复制进发布包。
- 不把本机 AutoCAD 安装绝对路径硬编码到源码；开发机路径通过项目属性、环境或受控配置处理。
- 未确认许可前，不提交或分发 Autodesk SDK 二进制文件。

## 7. 测试要求

- 核心算法优先使用自动单元测试。
- 至少覆盖：整除尺寸、东西余量、南北余量、双向余量、小于 600 mm、无效四线、非闭合和非轴对齐矩形。
- 数值计算使用明确公差；公差值必须集中定义并有测试。
- CAD 集成通过脱敏 DWG 实机测试，输入原件保存在 `inputs`，测试副本进入 `tests/fixtures`。
- 修复缺陷时尽量增加回归测试。

## 8. 质量命令

当前尚未创建解决方案，正式命令将在技术环境探针后写入 `README.md`。在此之前不得编造通过结果。

建立解决方案后至少提供：

- 恢复依赖命令；
- 编译命令；
- 核心测试命令；
- 插件打包命令；
- AutoCAD 实机加载步骤。

## 9. 数据、日志与发布

- `inputs` 中的用户 DWG 和策划资料只读保留，默认不提交 Git。
- `tests/fixtures` 只放脱敏、稳定、可复现的测试副本。
- 日志不得包含完整图纸内容、密钥或不必要的绝对个人路径。
- `build` 是可再生成的构建目录，不手工修改。
- `dist` 只存放经过检查的插件包、安装包、发布说明和 SHA-256。
- 未经用户明确要求，不提交、推送、创建 PR、发布或部署。

## 10. 完成定义

只有满足当前任务验收标准、相关测试通过、原始 DWG 未被修改、无无关改动混入，并明确说明未验证项时，才能声明完成。

## 11. Codex 任务切换与并行协作

本项目使用者为编程初学者。Codex 必须主动管理任务上下文和并行风险，不要求用户自行判断 Git、线程或 worktree 细节。

### 11.1 新任务提醒

出现以下任一情况时，Codex 应在完成当前验收和状态记录后，主动建议是否新开 Codex 任务：

- 一个里程碑或独立技术关口已经完成，例如 M1 转入 M2；
- 下一项工作与当前主题明显不同，需要新的目标、验收标准或工具链；
- 当前任务积累了较长的诊断、安装、试错或实机往返记录，继续追加会降低可读性；
- 即将进行大范围重构、发布或另一条独立工作流。

切换前必须更新 `README.md`、`PROJECT.md` 或相关验收文档，写明已完成、未完成、验证结果和下一步，并检查 `git status`。新任务应依赖这些持久文件和 Git 状态，不依赖旧聊天中的隐含记忆。

向用户建议新任务时，必须用非技术语言说明：为什么现在适合切换、新任务要做什么、旧任务是否已经安全收尾。用户明确同意后再创建或引导创建任务。

### 11.2 并行任务启用条件

只有同时满足以下条件时才建议同项目并行：

- 两项工作可以独立验收，彼此不存在未解决的先后依赖；
- 修改文件不重叠，或使用独立 Git worktree 隔离；
- 已存在可用的 Git 基线提交，两个任务从明确的同一状态开始；
- 已指定一个主任务负责接口决策、结果审查和最终集成；
- 并行收益明显高于创建、审查和合并 worktree 的额外成本。

以下情况默认不并行：需求或接口尚未冻结、两个任务会修改同一项目文件、一个任务必须等待另一个任务输出、正在处理首次环境探针或高风险实机写回。

对初学者首次启用并行时最多建议两个任务。Codex 必须明确列出每个任务的目标、允许修改的文件、验收标准、依赖关系、采用 worktree 的原因以及最终如何合并。未经用户明确同意，不创建新的并行任务。

### 11.3 当前项目的建议节点

- M1 完成后：适合新开 M2 任务，因为环境探针已经独立验收并记录；
- M2 初始模型、接口和公差规则冻结前：核心算法工作保持串行；
- M2 接口冻结且已有 Git 基线后：可评估把“矩形验证测试”和“600 mm 网格测试/文档”拆成两个 worktree，但文件不得重叠；
- M3 CAD 集成开始时：核心算法与 AutoCAD 适配存在直接依赖，关键接口接入阶段保持串行；
- M5 代码冻结后：回归测试与发布说明/打包检查可在边界清楚时并行。
