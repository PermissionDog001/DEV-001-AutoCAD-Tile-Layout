# 正交简单房间边界与裁切

> 状态：已完成；自动质量门和 AutoCAD 2021 最小实机验证均通过
> 日期：2026-07-21
> Git 基线：`main` 提交 `6aa51d9`
> 边界：独立能力，不启动 M10，不修改版本或发布物

## 1. 首个最小里程碑

新增独立命令 `TILEORTHO`，支持由 4 条及以上模型空间 `LINE` 组成的单一 WCS 正交简单房间。已有 `TILE600` 和 `TILELAYOUT` 的选择数量、矩形语义、图层及消息保持不变，继续作为已验收兼容入口。

`TILEORTHO` 的首个里程碑只接受：

- 同一当前图纸、同一模型空间、同一 WCS 高程的独立 `LINE`；
- 每条线与 WCS X 或 Y 轴平行，图纸单位为毫米；
- 单一简单闭合环，无洞、无自交、无重复或部分重叠、无 T 形分叉、无悬挂边、无分离环；
- 选择顺序和每条 `LINE` 的起终点方向任意；
- 共线分段矩形、L 形凹多边形，以及满足同一契约的其他正交简单环。

任意斜边、旋转网格、洞口/柱、多外环、多房间通缝、灰缝、居中和窄砖优化不进入本里程碑。

## 2. 起铺锚点决策

### 2.1 方案比较

| 方案 | 优点 | 首个里程碑的问题 | 决策 |
|---|---|---|---|
| 区域包围盒四角 | 只有四个确定状态；选择顺序无关；矩形时与 SC1 坐标完全一致；无需新增点选 | 凹房间的包围盒角可能在室外，不能再宣称是第一块砖的房内角 | 采用，但改称“包围盒网格锚点” |
| 边界顶点 | 锚点一定在边界上 | 凹房间可能有很多顶点；必须新增顶点拾取、吸附、无效点和同点歧义规则 | 暂缓 |
| 任意 WCS 基准点 | 可扩展到走廊、中心线和跨房间通缝 | 必须同时冻结房外点、负向模数、双侧余量、点捕捉和跨房间基准 | 暂缓 |

### 2.2 冻结语义

`TILEORTHO` 继续复用 `TileLayoutStartCorner` 的 `SW/SE/NW/NE` 四个离散方向，但其语义冻结为区域 WCS 包围盒的网格相位锚点：

- `SW`：候选竖线从包围盒西侧按砖宽递增，候选横线从南侧按砖高递增；
- `SE`：从东侧向西、从南侧向北；
- `NW`：从西侧向东、从北侧向南；
- `NE`：从东侧向西、从北侧向南。

锚点允许位于房间外，也不要求是边界顶点。它只决定无限 WCS 网格的相位和两个生成方向，不承诺“第一块完整砖”的角与该点重合。矩形输入时，该语义与 SC1 的四角结果逐线一致。

命令交互冻结为“砖宽 → 砖高 → 包围盒网格锚点 → 选择 4 条及以上 `LINE`”。默认砖宽/高均为 600 mm，默认锚点为 `SW`。输出图层固定为 `TILE_LAYOUT_ORTHO`。

## 3. 核心公共接口

核心继续不引用 Autodesk 程序集，新增以下宿主无关契约：

- `AxisAlignedOrthogonalPolygon`：规范化后的逆时针简单环顶点、WCS 包围盒和统一高程；首尾顶点不重复保存；
- `OrthogonalRoomValidator.Validate(IReadOnlyCollection<LineSegment3D>)`：建立确定性的端点图，验证并返回一个规范化房间；
- `OrthogonalRoomValidationResult` / `OrthogonalRoomValidationError`：稳定错误分类和可读消息；
- `OrthogonalTileGridCalculator.Calculate(AxisAlignedOrthogonalPolygon, TileLayoutParameters)`：生成并裁切参数化 WCS 网格；
- `OrthogonalTileLayoutResult`：保留房间、参数、包围盒完整模数、包围盒余量和最终室内片段。

现有 `RectangleValidator`、`TileGridCalculator` 和 `TileLayoutResult` 不改语义。

## 4. 确定性端点归并与闭环验证

验证顺序冻结为：数量、有限坐标、统一高程、非退化、WCS 轴对齐、坐标归并、重叠/相交、顶点度数、单一连通环、规范化。

### 4.1 公差归并

- 继续使用 `GeometryTolerance.Coordinate = 1e-6 mm`；
- X、Y 坐标分别排序并建立“相邻差小于等于公差”的候选连通组；
- 若一个候选组的最大值与最小值之差超过公差，视为链式吸附歧义并拒绝，不按输入顺序贪心选择；
- 合法组统一替换为 `min + (max - min) / 2`；统一高程也使用全部端点 Z 的最小/最大中点；
- 以上规则只依赖排序后的数值集合，因此与选择顺序和 `LINE` 方向无关。

归并后若线退化则拒绝。相同或反向重复线、同一轴线上的部分重叠线均拒绝；只在共同端点首尾相接的共线碎段允许进入端点图。

### 4.2 图与规范化

- 每个归并后顶点的度数必须恰好为 2；小于 2 表示断口/悬挂，大于 2 表示分叉；
- 全部边必须从一个确定起点遍历一次且只形成一个连通闭环；两个或更多分离闭环拒绝；
- 非相邻边相交、接触或端点落在另一边内部均拒绝；
- 遍历后删除度数为 2 的共线中间顶点，相当于确定性合并相邻共线碎段；
- 规范化环统一为逆时针，并从 `(X 最小，再 Y 最小)` 的顶点开始；因此同一几何在任意选择顺序和线方向下得到逐点相同的结果；
- 规范化后至少四边、面积严格大于公差。

## 5. 网格线裁切规则

先按房间包围盒和锚点生成严格位于包围盒内部的候选竖线与横线，再分别裁切为零个、一个或多个室内区间。

- 竖线只与水平边求交，横线只与竖直边求交；
- 顶点命中采用半开规则 `[较小坐标, 较大坐标)`，同一顶点只计一次；
- 交点排序后按奇偶规则两两组成室内区间；凹角和 U 形等情况可产生多个互不连续片段；
- 与凹口边界共线的候选线只保留实际室内一侧，不重复生成房间边界；
- 区间若重叠或间距小于等于公差则合并，但不得跨越大于公差的室外缺口；
- 最终长度小于等于 `GeometryTolerance.Coordinate` 的数值碎片丢弃且不计数；大于公差的窄小合法片段保留；
- 输出顺序确定：先按锚点方向排列候选竖线，再排列候选横线；同一候选线上的片段始终按 WCS 小坐标到大坐标排序，片段端点也按小坐标到大坐标保存。

## 6. 片段上限与资源安全

`TILEORTHO` 单次最终室内片段上限冻结为 10,000，与既有参数化命令数值一致，但按最终片段而不是候选网格线计数。

1. 先用包围盒数值路径计算候选竖/横线数量；候选数已超过 10,000 时直接拒绝；
2. 候选数安全后逐线裁切并只累计片段数；累计达到 10,001 时立即抛出 `TileLayoutLimitExceededException`；
3. 只有完整计数不超过上限后才创建最终只读结果集合；
4. 恰好 10,000 个最终片段允许，10,001 个拒绝；图层创建发生在核心计算成功之后。

## 7. AutoCAD 事务边界

`TILEORTHO` 沿用已验证的模型空间、毫米单位、只读输入快照、单事务图层创建/复用、实体写入、异常不提交、原线保护和不自动保存规则。

- 选择器只过滤 `LINE`，允许 4 条及以上；
- 所有边界验证和 10,000 片段检查均在创建/修改 `TILE_LAYOUT_ORTHO` 前完成；
- 原边界始终只读打开；
- 图层和全部片段在同一个命令事务中提交，紧接命令的一次 `U`/`UNDO` 可整体撤销；
- 已有同名图层只复用，不改颜色、线型、线宽或锁定状态；
- 插件不调用保存命令。

## 8. 自动测试与实机边界

自动测试至少覆盖：

- 共线分段矩形、L 形、选择顺序/方向置换的逐点确定性；
- 公差内归并、公差外断口、链式吸附歧义；
- 重复/反向重复、部分重叠、T 形分叉、分离环、自交/非相邻接触；
- 相邻共线碎段合并和规范化环方向/起点；
- 顶点正中网格线、凹角、单线多室内区间、区间不跨凹口合并、极短数值碎片；
- 包围盒四锚点相位、矩形与既有 SC1 逐线一致；
- 最终片段恰好 10,000 允许、10,001 拒绝，且拒绝路径不海量分配。

AutoCAD 2021 实机只使用脱敏工作副本，由用户一次操作一个步骤：

1. 一个共线分段矩形；
2. 一个 L 形；
3. 一个断口或自交拒绝；
4. 成功结果紧接一次 `U`/`UNDO`；
5. 关闭工作副本并选择不保存。

自动测试和编译结果不冒充上述宿主证据。完成自动阶段后再生成逐步实机清单和预期坐标。

## 9. 自动实现与验证记录

2026-07-21 已完成：

- 新增规范化 `AxisAlignedOrthogonalPolygon`、稳定验证结果/错误、`OrthogonalRoomValidator`、`OrthogonalTileGridCalculator` 和 `OrthogonalTileLayoutResult`；核心仍只引用 `System` 与 `System.Core`；
- 端点坐标按排序后的公差组归并，链式歧义明确拒绝；端点图要求全部度数为 2 且只有一个连通环；相邻共线碎段被规范化为逆时针、固定起点的确定性顶点序列；
- 扫描线使用半开顶点规则、奇偶配对、室内区间合并和共线边界扣除；最终片段按确定顺序输出；
- 10,000 上限先检查候选数量，再两遍裁切：第一遍只计最终片段并可提前拒绝，第二遍才创建最终集合；
- 新增 `TILEORTHO`，输出图层为 `TILE_LAYOUT_ORTHO`；现有 `TILE600`、`TILELAYOUT` 的命令注册、四线矩形入口、图层和原消息保持不变；
- NuGet 锁定模式恢复通过；Debug/Release 测试项目和完整解决方案隔离重建通过；两种配置自动测试均为 80/80；
- 正式插件 Debug/Release 隔离编译通过，输出位于 `build/orthogonal-room`；完整解决方案隔离输出位于 `build/orthogonal-solution`；均未复制 `AcCoreMgd.dll`、`AcDbMgd.dll` 或 `AcMgd.dll`；
- 核心和正式插件程序集版本保持 `0.1.0.0`；`dist/TileLayout-0.1.0.zip` SHA-256 仍为 `322077112229CA8E0EDFB0CEE0B1F3F192A24EAC2CC23D11DCEA413CC3431141`；
- 未操作 DWG，未启动 M10，未修改 `dist`、V0.1.0 Release、Git 标签或远端状态，未提交或推送。

随后在形成新 Git 基线前完成聚焦代码审查。审查复核拓扑、公差归并、共线合并、扫描线半开顶点规则、共线边界扣除、最终片段上限、AutoCAD 只读输入和单事务写回，发现一项数值问题：绝对 WCS 坐标直接参与鞋带面积公式时，基点约为 `1e12`、房间仍为 3600 × 3000 mm 的合法矩形会因浮点消减被误判为零面积。面积计算现改为相对首顶点坐标，并显式拒绝非有限面积；新增回归后 Debug/Release 自动测试均为 81/81。未发现其他与冻结契约冲突的问题。

自动测试具体覆盖：共线分段矩形、L/U 形、顺序与方向确定性、公差内吸附、链式歧义、公差外断口、重复/重叠、T 形、自交、分离环、顶点命中、多室内区间、共线边界扣除、合法窄片段、包围盒东北锚点位于 L 形房外时的相位、四锚点矩形逐线兼容，以及最终 10,000/超限梳齿房型。

## 10. AutoCAD 2021 最小实机清单

只在新的脱敏工作副本中执行。先正常关闭任何已加载旧同名程序集的 AutoCAD 会话且不保存，再启动新会话并 `NETLOAD`：

```text
build\orthogonal-room\Debug\TileLayout.AutoCAD.dll
```

确认 `INSUNITS=4`、`TILEMODE=1`、WCS 世界坐标。以下样例全部用独立 `LINE` 绘制，坐标单位为 mm，Z=0；不要使用未分解多段线。

### 10.1 共线分段矩形

按任意顺序绘制五条线：

```text
(0,0)       → (1800,0)
(1800,0)    → (3600,0)
(3600,0)    → (3600,3000)
(3600,3000) → (0,3000)
(0,3000)    → (0,0)
```

执行 `TILEORTHO`，砖宽/高和包围盒锚点都直接回车使用默认值，选择五条线。预期：包围盒 3600 × 3000，X/Y 完整模数 6/5，东/北余量 0/0，在 `TILE_LAYOUT_ORTHO` 生成 9 条最终片段。

### 10.2 L 形

绘制六条线：

```text
(5000,0)    → (6800,0)
(6800,0)    → (6800,600)
(6800,600)  → (5600,600)
(5600,600)  → (5600,1800)
(5600,1800) → (5000,1800)
(5000,1800) → (5000,0)
```

执行 `TILEORTHO`，仍使用 600 × 600 和 `SW`。预期：包围盒 1800 × 1800，X/Y 完整模数 3/3，生成 4 条最终片段；代表片段为 X=5600、Y=0→600，以及 Y=600、X=5000→5600。凹口内和与凹口边界重合的部分不得生成。

成功消息出现后不要插入 `_LIST`、`QSELECT` 等命令，立即只执行一次 `U`。预期：本次 4 条片段整体消失，六条原边界仍在；若图层是本次首次创建，也应恢复命令前状态。

### 10.3 断口拒绝

绘制四条线：

```text
(10000,0)    → (11800,0)
(11800,0)    → (11800,1200)
(11800,1200) → (10000,1200)
(10000,1100) → (10000,0)
```

执行 `TILEORTHO` 并选择四条线。预期：报告断口/端点度数错误，不新增片段，也不创建或修改输出图层。

### 10.4 不保存

完成后关闭工作副本并选择“不保存”。核对只读夹具与工作副本的初始哈希没有被插件改写。上述宿主结果已由用户逐项确认，详细证据见下一节。

## 11. AutoCAD 2021 最小实机验证记录

2026-07-21，用户在 AutoCAD 2021 新会话中加载 `build/orthogonal-room/Debug/TileLayout.AutoCAD.dll`，并在独立脱敏工作副本 `work/or1-acceptance/or1-working.dwg` 中逐步完成以下验证：

- `NETLOAD` 成功，命令行显示 `TileLayout 0.1.0 正式插件已加载`；`INSUNITS=4`、`TILEMODE=1`，并切换为 World UCS；
- 五线共线分段矩形使用默认 600 × 600 mm 和 `SW`，准确报告包围盒 3600 × 3000 mm、X/Y 完整模数 6/5、东/北余量 0/0，并生成 9 条最终片段；
- 成功后未插入任何检查命令，立即执行一次 `U`；9 条分格片段目视全部消失，原 5 条边界 `LINE` 仍完整保留，证明单命令撤销和原线保护通过；
- 六线 L 形的包围盒为 1800 × 1800 mm、东北缺口为 1200 × 1200 mm。默认参数准确报告 X/Y 完整模数 3/3、东/北余量 0/0，并生成 4 条最终片段；用户目视确认东北凹口内无分格线，也没有与凹口边界重合的重复线；
- 四线断口样例在西北角保留 100 mm 缺口。命令准确报告“边界存在断口、悬挂边或 T 形分叉；每个归并端点必须恰好连接两条线”，未生成任何对象；断口内部无新增线，既有 L 形 4 条结果也未变化；
- 用户关闭 `or1-working.dwg` 并明确选择“不保存”。关闭后原始输入、只读夹具和工作副本均为 31,890 字节，SHA-256 均为 `646A3A7A22CF40E5EC0B9CF8621A17AFAB09BB27928772C05D9CB3F4202DDA75`。

本次实机严格限制为一个共线分段矩形、一个 L 形、一个断口拒绝、一次紧接命令的撤销和关闭不保存，没有在宿主重复自动测试已覆盖的公差、拓扑矩阵、四锚点或 10,000 片段压力边界。未发现可复现产品缺陷。
